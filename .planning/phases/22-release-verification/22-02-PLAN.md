---
phase: 22-release-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pickred-entry.spec.js
  - tests/bigtwo-entry.spec.js
  - tests/fighting-entry.spec.js
  - tests/mahjong.spec.js
  - package.json
autonomous: true
requirements: [PLAT-06]

must_haves:
  truths:
    - "Pick Red room page loads without crashing on the 3100 stack"
    - "Big Two room page loads without crashing on the 3100 stack"
    - "Fighting room page loads without crashing on the 3100 stack"
    - "Mahjong room page has room entry test with mocked API"
    - "All new UI spec files are registered in test:ui:critical script"
  artifacts:
    - path: "tests/pickred-entry.spec.js"
      provides: "Pick Red UI smoke test (page load, no crash, hub discovery)"
      contains: "pickred room page loads"
    - path: "tests/bigtwo-entry.spec.js"
      provides: "Big Two UI smoke test (page load, no crash, hub discovery)"
      contains: "bigtwo room page loads"
    - path: "tests/fighting-entry.spec.js"
      provides: "Fighting UI smoke test (page load, no crash)"
      contains: "fighting room page loads"
    - path: "tests/mahjong.spec.js"
      provides: "Extended with room entry test using mocked API"
      contains: "mahjong room page loads without crash"
    - path: "package.json"
      provides: "Updated test:ui:critical script with new spec files"
      contains: "pickred-entry.spec.js"
  key_links:
    - from: "tests/pickred-entry.spec.js"
      to: "http://127.0.0.1:3100/pickred/test-room"
      via: "page.goto"
      pattern: "pickred/test-room"
    - from: "tests/bigtwo-entry.spec.js"
      to: "http://127.0.0.1:3100/bigtwo/test-room"
      via: "page.goto"
      pattern: "bigtwo/test-room"
    - from: "tests/fighting-entry.spec.js"
      to: "http://127.0.0.1:3100/fighting/test-room"
      via: "page.goto"
      pattern: "fighting/test-room"
    - from: "package.json test:ui:critical"
      to: "tests/pickred-entry.spec.js"
      via: "script file list"
      pattern: "pickred-entry\\.spec\\.js"
---

<objective>
Create Playwright UI smoke tests for Pick Red, Big Two, and Fighting room pages, extend the existing Mahjong spec with a room entry test, and register all new spec files in the test:ui:critical script.

Purpose: Verify each new game's frontend page loads without crashing on the deployed 3100 stack with mocked API responses.

Output: 3 new spec files (pickred-entry.spec.js, bigtwo-entry.spec.js, fighting-entry.spec.js), 1 extended spec (mahjong.spec.js), updated package.json scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-release-verification/22-CONTEXT.md
@.planning/phases/22-release-verification/22-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tests/racing-entry.spec.js (exact analog for new specs):
```javascript
const { test, expect } = require("playwright/test");
const FRONTEND_BASE_URL = String(process.env.FRONTEND_BASE_URL || "http://127.0.0.1:3100").replace(/\/+$/, "");

// Pattern: Mock /api/me, mock game room API, goto page, check no crash
await page.route("**/api/me", async (route) => {
  await route.fulfill({
    contentType: "application/json",
    body: JSON.stringify({
      user: { id: "test-user-1", displayName: "TestPlayer" },
      session: { id: "test-user-1", kind: "user", displayName: "TestPlayer" }
    })
  });
});
```

From tests/mahjong.spec.js (existing spec to extend):
```javascript
const { test, expect } = require("playwright/test");
const FRONTEND_BASE_URL = String(process.env.FRONTEND_BASE_URL || "http://127.0.0.1:3100").replace(/\/+$/, "");

// Existing tests:
// "mahjong room page loads without crash" -- goto /games/mahjong, check status < 500
// "mahjong game card is visible on hub page" -- goto /, check "麻將" visible
```

Page routes (from filesystem):
- Pick Red: `/pickred/[roomNo]` -> `/pickred/test-room`
- Big Two: `/bigtwo/[roomNo]` -> `/bigtwo/test-room`
- Fighting: `/fighting/[roomNo]` -> `/fighting/test-room`

API routes (from network-contract):
- Pick Red: `**/api/pickred/rooms/*`
- Big Two: `**/api/bigtwo/rooms/*`
- Fighting: `**/api/fighting/rooms/*`

package.json test:ui:critical (current):
```
playwright test tests/hub-entry.spec.js tests/admin-console.spec.js tests/room-ui.spec.js tests/arcade-party.spec.js tests/board-games.spec.js tests/reversi.spec.js tests/sokoban.spec.js tests/undercover.spec.js --workers=1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pick Red, Big Two, and Fighting UI smoke specs</name>
  <files>tests/pickred-entry.spec.js, tests/bigtwo-entry.spec.js, tests/fighting-entry.spec.js</files>
  <read_first>
    - tests/racing-entry.spec.js (exact analog: imports, API mock pattern, page load test, canvas check, HUD check)
    - tests/mahjong.spec.js (simpler pattern: hub discovery test)
    - pages/pickred/[roomNo].js (confirm page route exists)
    - pages/bigtwo/[roomNo].js (confirm page route exists)
    - pages/fighting/[roomNo].js (confirm page route exists)
  </read_first>
  <action>
**tests/pickred-entry.spec.js** -- Create new file following racing-entry.spec.js pattern:

1. Imports: `const { test, expect } = require("playwright/test");`, `const FRONTEND_BASE_URL = String(process.env.FRONTEND_BASE_URL || "http://127.0.0.1:3100").replace(/\/+$/, "");`

2. Test "pickred room page loads without crashing":
   - `test.slow(); page.setDefaultTimeout(30000);`
   - Mock `**/api/me` with test user (same pattern as racing-entry)
   - Mock `**/api/pickred/rooms/*` GET with room data: `{ room: { roomNo: "test-room", gameKey: "pickred", familyKey: "card", state: "waiting", config: { maxPlayers: 2 }, players: [{ userId: "test-user-1", seatIndex: 0, displayName: "TestPlayer", ready: false }], viewer: { seatIndex: 0, displayName: "TestPlayer", ready: false } } }`
   - `await page.goto(\`${FRONTEND_BASE_URL}/pickred/test-room\`);`
   - `await page.waitForLoadState("networkidle");`
   - Assert body text does not match `/application error/i` or `/500/i`

3. Test "pickred game card is visible on hub page":
   - `page.setDefaultTimeout(15000);`
   - `await page.goto(\`${FRONTEND_BASE_URL}/\`);`
   - `const card = page.getByText("撿紅點").first();`
   - `await expect(card).toBeVisible({ timeout: 10000 });`

**tests/bigtwo-entry.spec.js** -- Same pattern, swap gameKey/route/title:

1. Same imports
2. Test "bigtwo room page loads without crashing":
   - Mock `**/api/me` (same)
   - Mock `**/api/bigtwo/rooms/*` GET with: `{ room: { roomNo: "test-room", gameKey: "bigtwo", familyKey: "card", state: "waiting", config: { maxPlayers: 4 }, players: [{ userId: "test-user-1", seatIndex: 0, displayName: "TestPlayer", ready: false }], viewer: { seatIndex: 0, displayName: "TestPlayer", ready: false } } }`
   - `await page.goto(\`${FRONTEND_BASE_URL}/bigtwo/test-room\`);`
   - Same assertions

3. Test "bigtwo game card is visible on hub page":
   - `page.getByText("大老二").first()`

**tests/fighting-entry.spec.js** -- Same pattern:

1. Same imports
2. Test "fighting room page loads without crashing":
   - Mock `**/api/me` (same)
   - Mock `**/api/fighting/rooms/*` GET with: `{ room: { roomNo: "test-room", gameKey: "fighting", familyKey: "light-3d", state: "waiting", config: { maxPlayers: 2, roundCount: 3 }, players: [{ userId: "test-user-1", seatIndex: 0, displayName: "TestPlayer", ready: false }], viewer: { seatIndex: 0, displayName: "TestPlayer", ready: false } } }`
   - `await page.goto(\`${FRONTEND_BASE_URL}/fighting/test-room\`);`
   - Same assertions

3. Test "fighting game card is visible on hub page":
   - `page.getByText("打斗").first()`
  </action>
  <verify>
    <automated>npx playwright test tests/pickred-entry.spec.js tests/bigtwo-entry.spec.js tests/fighting-entry.spec.js --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - tests/pickred-entry.spec.js exists and contains `pickred room page loads without crashing`
    - tests/pickred-entry.spec.js contains `**/api/pickred/rooms/*`
    - tests/pickred-entry.spec.js contains `撿紅點`
    - tests/bigtwo-entry.spec.js exists and contains `bigtwo room page loads without crashing`
    - tests/bigtwo-entry.spec.js contains `**/api/bigtwo/rooms/*`
    - tests/bigtwo-entry.spec.js contains `大老二`
    - tests/fighting-entry.spec.js exists and contains `fighting room page loads without crashing`
    - tests/fighting-entry.spec.js contains `**/api/fighting/rooms/*`
    - tests/fighting-entry.spec.js contains `打斗`
    - All 3 spec files pass with `npx playwright test ... --workers=1` (requires running stack)
  </acceptance_criteria>
  <done>Pick Red, Big Two, and Fighting UI smoke specs pass page load tests</done>
</task>

<task type="auto">
  <name>Task 2: Extend Mahjong spec and update package.json UI test scripts</name>
  <files>tests/mahjong.spec.js, package.json</files>
  <read_first>
    - tests/mahjong.spec.js (full file: understand existing tests, add room entry test)
    - tests/racing-entry.spec.js (pattern for room entry test with API mocking)
    - package.json (line 21: current test:ui:critical script)
  </read_first>
  <action>
**tests/mahjong.spec.js** -- Add 1 new test after existing tests (after line 19):

Test "mahjong room page loads without crash with mocked room API":
- `test.slow(); page.setDefaultTimeout(30000);`
- Mock `**/api/me` with test user (same pattern as racing-entry)
- Mock `**/api/mahjong/rooms/*` GET with: `{ room: { roomNo: "test-room", gameKey: "mahjong", familyKey: "card", state: "waiting", config: { maxPlayers: 4 }, players: [{ userId: "test-user-1", seatIndex: 0, displayName: "TestPlayer", ready: false }], viewer: { seatIndex: 0, displayName: "TestPlayer", ready: false } } }`
- `await page.goto(\`${FRONTEND_BASE_URL}/games/mahjong\`);` (note: mahjong uses /games/mahjong route, not /mahjong/)
- `await page.waitForLoadState("networkidle");`
- Assert body text does not match `/application error/i` or `/500/i`

Note: The existing mahjong.spec.js uses `/games/mahjong` as the route (not `/mahjong/test-room`). Keep this consistent. The new test should use the same route pattern but with API mocking.

**package.json** -- Update `test:ui:critical` script (line 21): append ` tests/pickred-entry.spec.js tests/bigtwo-entry.spec.js tests/fighting-entry.spec.js` to the end of the existing file list.

Current value:
```
playwright test tests/hub-entry.spec.js tests/admin-console.spec.js tests/room-ui.spec.js tests/arcade-party.spec.js tests/board-games.spec.js tests/reversi.spec.js tests/sokoban.spec.js tests/undercover.spec.js --workers=1
```

New value:
```
playwright test tests/hub-entry.spec.js tests/admin-console.spec.js tests/room-ui.spec.js tests/arcade-party.spec.js tests/board-games.spec.js tests/reversi.spec.js tests/sokoban.spec.js tests/undercover.spec.js tests/pickred-entry.spec.js tests/bigtwo-entry.spec.js tests/fighting-entry.spec.js --workers=1
```
  </action>
  <verify>
    <automated>npx playwright test tests/mahjong.spec.js --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - tests/mahjong.spec.js contains `mahjong room page loads without crash with mocked room API`
    - tests/mahjong.spec.js contains `**/api/mahjong/rooms/*`
    - package.json test:ui:critical contains `pickred-entry.spec.js`
    - package.json test:ui:critical contains `bigtwo-entry.spec.js`
    - package.json test:ui:critical contains `fighting-entry.spec.js`
    - `npx playwright test tests/mahjong.spec.js --workers=1` exits 0 (requires running stack)
  </acceptance_criteria>
  <done>Mahjong spec extended with room entry test, package.json updated with all new UI specs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| playwright -> app frontend | Tests visit real pages at 127.0.0.1:3100 with mocked API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | T (Tampering) | tests/*.spec.js | accept | Test files are dev-only, not deployed. API mocking prevents real data access. |
| T-22-04 | I (Information Disclosure) | tests/*.spec.js | accept | Tests use mock users with no real credentials. No PII in test data. |

**Rationale:** UI smoke tests use mocked API responses and test users. No real authentication or data access occurs. Test files are development-only artifacts.
</threat_model>

<verification>
After both tasks complete:
1. `npx playwright test tests/pickred-entry.spec.js --workers=1` exits 0
2. `npx playwright test tests/bigtwo-entry.spec.js --workers=1` exits 0
3. `npx playwright test tests/fighting-entry.spec.js --workers=1` exits 0
4. `npx playwright test tests/mahjong.spec.js --workers=1` exits 0
5. `grep -c 'pickred-entry.spec.js' package.json` returns 1
6. `grep -c 'bigtwo-entry.spec.js' package.json` returns 1
7. `grep -c 'fighting-entry.spec.js' package.json` returns 1
</verification>

<success_criteria>
- All new UI spec files pass page load tests
- Mahjong spec extended with room entry test
- package.json test:ui:critical script includes all 3 new spec files
- No existing tests broken by additions
</success_criteria>

<output>
After completion, create `.planning/phases/22-release-verification/22-02-SUMMARY.md`
</output>
