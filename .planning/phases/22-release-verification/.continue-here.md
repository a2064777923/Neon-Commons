---
context: phase
phase: 22-release-verification
task: complete
total_tasks: 6
status: complete
last_updated: 2026-05-07T09:04:27.799Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| API route function vs string | `API_ROUTES.fightingRooms.list` is a function, not a string. Must call with `()` to get URL. | advisory | Always verify route type before using in fetch calls |
| Dockerfile peer deps | `@pixi/react` requires React 19 but project uses React 18. Use `--legacy-peer-deps`. | advisory | Already fixed in Dockerfile |
| Lobby page guard clause | `pages/games/[gameKey].js` only allowed PARTY and BOARD game keys. Card and light-3d families rejected. | advisory | Already fixed — guard now includes CARD_GAME_KEYS and LIGHT3D_GAME_KEYS |

<current_state>
Phase 22 (Release Verification) is COMPLETE. All 3 plans executed, all 5 new games verified.

Post-execution fixes applied:
1. Lobby page guard clause — added CARD_GAME_KEYS and LIGHT3D_GAME_KEYS
2. API route function calls — added `()` to invoke route functions
3. Dockerfile — added `--legacy-peer-deps` for npm install
4. Docker — rebuilt and redeployed container

All 5 game lobby pages verified working via Playwright:
- pickred OK, bigtwo OK, mahjong OK, racing OK, fighting OK
</current_state>

<completed_work>

Completed Tasks:
- 22-01 Task 1: Pick Red and Big Two logic tests — Done (fb0e0ef)
- 22-01 Task 2: Mahjong, Racing, Fighting logic tests — Done (abb4d37)
- 22-02 Task 1: Pick Red, Big Two, Fighting UI specs — Done (d2caadc)
- 22-02 Task 2: Mahjong spec + package.json update — Done (df97c5e)
- 22-03 Task 1: Catalog flip 4 games to live — Done (3506c60)
- 22-03 Task 2: Hub discovery test assertions — Done (inline)
- Post-fix: Lobby page guard clause + API routes + Dockerfile — Done (c97887e, 19524f3, b0c8f64)
</completed_work>

<remaining_work>
None — Phase 22 is complete.
</remaining_work>

<decisions_made>
- Flipped all 4 remaining games (pickred, bigtwo, mahjong, racing) from coming-soon to live with isShipped: true in single commit per D-04
- Added CARD_GAME_KEYS and LIGHT3D_GAME_KEYS to catalog.js exports for lobby page
- Fixed API route function calls — `API_ROUTES.fightingRooms.list` is a function, needs `()` to get URL
- Added `--legacy-peer-deps` to Dockerfile to resolve @pixi/react peer dependency conflict
</decisions_made>

<blockers>
None
</blockers>

## Infrastructure State
- Docker app container: rebuilt and running with latest code
- PostgreSQL: running (port 5432)
- Redis: running
- Frontend: http://127.0.0.1:3100
- Backend: http://127.0.0.1:3101

## Required Reading (in order)
1. `.planning/phases/22-release-verification/22-CONTEXT.md` — all user decisions for this phase
2. `.planning/phases/22-release-verification/22-01-SUMMARY.md` — logic test execution details
3. `.planning/phases/22-release-verification/22-02-SUMMARY.md` — UI test execution details
4. `.planning/phases/22-release-verification/22-03-SUMMARY.md` — catalog flip details

<context>
Phase 22 is the final verification gate for v1.3 milestone. All 5 new games (Pick Red, Big Two, Mahjong, Racing, Fighting) have:
- Logic smoke tests (full-round + reconnection) in test-logic/
- UI smoke tests (Playwright page load) in tests/
- Catalog entries set to launchState "live" with isShipped: true
- Hub discovery test assertions updated
- Lobby pages working for all game families

v1.3 milestone is ready to ship.
</context>

<next_action>
Phase 22 is complete. Suggested next steps:
- /gsd-verify-work 22 to run full verification
- /gsd-plan-phase 23 if continuing to next phase
- /gsd-complete-milestone to close v1.3
</next_action>
