---
phase: 20-racing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/shared/network-contract.js
  - lib/games/catalog.js
  - test-logic/racing-logic.test.js
autonomous: true
requirements:
  - PLAT-01
  - PLAT-05

must_haves:
  truths:
    - "three and cannon-es are installed as project dependencies"
    - "cannon-es can be required in Node.js 18 without ERR_REQUIRE_ESM"
    - "racing entries exist in SOCKET_EVENTS, API_ROUTE_PATTERNS, and API_ROUTES"
    - "racing entry exists in GAME_CATALOG with gameKey 'racing', familyKey 'light-3d'"
    - "test-logic/racing-logic.test.js exists with placeholder tests"
  artifacts:
    - path: "package.json"
      provides: "three and cannon-es dependencies"
      contains: "cannon-es"
    - path: "lib/shared/network-contract.js"
      provides: "racing socket events and API route patterns"
      contains: "racing"
    - path: "lib/games/catalog.js"
      provides: "racing game catalog entry"
      contains: "racing"
    - path: "test-logic/racing-logic.test.js"
      provides: "test scaffold for racing logic"
      contains: "racing"
  key_links:
    - from: "lib/shared/network-contract.js"
      to: "lib/games/catalog.js"
      via: "racing gameKey consistent across both"
      pattern: "racing"
---

<objective>
Install three and cannon-es dependencies, verify cannon-es CJS compatibility in Node.js 18, add racing network contract entries (socket events + API routes), register racing in the game catalog, and create the test scaffold.

Purpose: De-risk the cannon-es CJS compatibility issue (the highest-risk item from research) before building any backend code. Establish the network contract and catalog entries that all downstream plans depend on.

Output: Updated package.json, network-contract.js, catalog.js, and test-logic/racing-logic.test.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-racing/20-CONTEXT.md
@.planning/phases/20-racing/20-RESEARCH.md

@lib/shared/network-contract.js
@lib/games/catalog.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and verify cannon-es CJS compatibility</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json
    - .planning/phases/20-racing/20-RESEARCH.md (Pitfall 2 section)
  </read_first>
  <action>
    1. Run `npm install three@0.184.0 cannon-es@0.20.0` to add both dependencies.
    2. Immediately test CJS compatibility by running:
       `node -e "const CANNON = require('cannon-es'); console.log(typeof CANNON.World)"`
    3. If the require succeeds and prints "function", record success. If it fails with ERR_REQUIRE_ESM:
       - Check if there is a CJS build available: `ls node_modules/cannon-es/dist/` or `cat node_modules/cannon-es/package.json | grep -i main`
       - If no CJS build exists, create a thin async wrapper in lib/racing/physics.js that uses dynamic `import()`:
         ```javascript
         let CANNON;
         async function loadCannon() {
           if (!CANNON) { CANNON = await import("cannon-es"); }
           return CANNON;
         }
         module.exports = { loadCannon };
         ```
       - All downstream code must use `await loadCannon()` instead of `require("cannon-es")`
    4. Verify three installs cleanly: `node -e "const THREE = require('three'); console.log(typeof THREE.Scene)"` (this should work since three has CJS builds).
  </action>
  <verify>
    <automated>node -e "const CANNON = require('cannon-es'); console.log(typeof CANNON.World)" 2>&1 | grep -q "function" && echo "PASS: cannon-es CJS works" || echo "FAIL: need dynamic import wrapper"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "three" and "cannon-es" in dependencies
    - node_modules/three and node_modules/cannon-es directories exist
    - cannon-es can be loaded in Node.js (either via require or documented dynamic import fallback)
    - If dynamic import is needed, lib/racing/physics.js exists with loadCannon() export
  </acceptance_criteria>
  <done>
    three and cannon-es installed; cannon-es CJS compatibility verified or dynamic import wrapper created; test output confirms loadability.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add racing network contract and catalog entries</name>
  <files>lib/shared/network-contract.js, lib/games/catalog.js</files>
  <read_first>
    - lib/shared/network-contract.js (full file)
    - lib/games/catalog.js (full file)
    - .planning/phases/20-racing/20-CONTEXT.md (Socket Events, API Routes, Game Family sections)
  </read_first>
  <action>
    **In lib/shared/network-contract.js:**

    1. Add racing socket events to SOCKET_EVENTS (following the mahjong/bigtwo pattern):
       ```javascript
       racing: Object.freeze({
         subscribe: "racing:subscribe",
         ready: "racing:ready",
         input: "racing:input",
         update: "racing:update",
         error: "racing:error",
         chat: "racing:chat"
       })
       ```

    2. Add racing API route patterns to API_ROUTE_PATTERNS:
       ```javascript
       racingRooms: Object.freeze({
         list: "/api/racing/rooms",
         detail: "/api/racing/rooms/:roomNo",
         join: "/api/racing/rooms/:roomNo/join"
       })
       ```

    3. Add racing API route builders to API_ROUTES:
       ```javascript
       racingRooms: Object.freeze({
         list: () => API_ROUTE_PATTERNS.racingRooms.list,
         create: () => API_ROUTE_PATTERNS.racingRooms.list,
         detail: (roomNo) => buildPath(API_ROUTE_PATTERNS.racingRooms.detail, { roomNo }),
         join: (roomNo) => buildPath(API_ROUTE_PATTERNS.racingRooms.join, { roomNo })
       })
       ```

    **In lib/games/catalog.js:**

    4. Update the existing `miniracers` entry to become the `racing` entry. Change:
       - key: "racing" (not "miniracers")
       - title: "賽車"
       - shortTitle: "Racing"
       - strapline: "3D 賽道、即時物理、多人競速"
       - description: "支援 2-4 人即時 3D 賽車房，cannon-es 物理引擎驅動，含圈數計時、觀戰模式與斷線恢復。"
       - players: "2-4 人"
       - route: "/games/racing"
       - accent: "royal"
       - features: ["3D 賽道", "即時物理", "圈數計時", "觀戰模式"]
       - audience: "適合多人即時競速"
       - familyKey: "light-3d"
       - hubOrder: 10 (first in light-3d family)
       - detailRoutePrefix: "/racing"
       - supportsShareLink: true
       - guestMode: "login-only"
       - launchState: "coming-soon" (per PLAT-05, staged launch)
       - discoveryTags: ["3D", "競速", "多人"]
       - isShipped: false

    5. Add `getRacingDefaultConfig` function:
       ```javascript
       function getRacingDefaultConfig() {
         return {
           visibility: "public",
           maxPlayers: 4,
           lapCount: 3
         };
       }
       ```

    6. Add "racing" to getGameLimits:
       ```javascript
       if (gameKey === "racing") {
         return { minPlayers: 2, maxPlayers: 4 };
       }
       ```

    7. Export getRacingDefaultConfig from module.exports.
  </action>
  <verify>
    <automated>node -e "const {SOCKET_EVENTS,API_ROUTE_PATTERNS,API_ROUTES} = require('./lib/shared/network-contract'); const {getGameMeta,getGameLimits} = require('./lib/games/catalog'); console.log('socket:', SOCKET_EVENTS.racing?.subscribe); console.log('route:', API_ROUTE_PATTERNS.racingRooms?.list); console.log('meta:', getGameMeta('racing')?.title); console.log('limits:', JSON.stringify(getGameLimits('racing')));"</automated>
  </verify>
  <acceptance_criteria>
    - SOCKET_EVENTS.racing has subscribe, ready, input, update, error, chat events
    - API_ROUTE_PATTERNS.racingRooms has list (/api/racing/rooms), detail, join patterns
    - API_ROUTES.racingRooms has list(), create(), detail(roomNo), join(roomNo) methods
    - getGameMeta("racing") returns entry with title "賽車", familyKey "light-3d"
    - getGameLimits("racing") returns { minPlayers: 2, maxPlayers: 4 }
    - launchState is "coming-soon" (staged launch per PLAT-05)
  </acceptance_criteria>
  <done>
    Racing network contract entries (socket events + API routes) and catalog entry are registered. getGameLimits returns correct player limits. launchState set to "coming-soon" for staged rollout.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create racing test scaffold</name>
  <files>test-logic/racing-logic.test.js</files>
  <read_first>
    - test-logic/mahjong-logic.test.js (test pattern reference)
    - .planning/phases/20-racing/20-RESEARCH.md (Validation Architecture section)
  </read_first>
  <action>
    Create test-logic/racing-logic.test.js with the following test groups:

    1. **Catalog integration tests:**
       - `getGameMeta('racing')` returns entry with title "賽車", key "racing", familyKey "light-3d"
       - `getGameLimits('racing')` returns `{ minPlayers: 2, maxPlayers: 4 }`

    2. **Network contract tests:**
       - `SOCKET_EVENTS.racing` has all required events (subscribe, ready, input, update, error, chat)
       - `API_ROUTE_PATTERNS.racingRooms` has list, detail, join routes
       - `API_ROUTES.racingRooms` has list, create, detail, join methods

    3. **Physics tests (placeholder - will be filled in Plan 20-02):**
       - Placeholder test: "cannon-es World can be created" - create a CANNON.World, set gravity, step once
       - Placeholder test: "car body can be created with Box shape" - create a car body at a position
       - Placeholder test: "track walls are static bodies" - create a wall body with mass 0

    4. **Delta-state tests (placeholder - will be filled in Plan 20-02):**
       - Placeholder test: "computeDelta returns tick and cars array"
       - Placeholder test: "delta only includes changed cars"

    5. **Lap detection tests (placeholder - will be filled in Plan 20-02):**
       - Placeholder test: "lap count increments on valid crossing"

    Use the same test patterns as mahjong-logic.test.js:
    - `const { describe, it } = require("node:test");`
    - `const assert = require("node:assert/strict");`
    - Import from lib modules
  </action>
  <verify>
    <automated>node --test test-logic/racing-logic.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test-logic/racing-logic.test.js exists
    - File contains describe blocks for: Catalog integration, Network Contract, Physics, Delta-state, Lap detection
    - Catalog and Network Contract tests pass (they test real catalog/contract entries from Task 2)
    - Physics/Delta/Lap tests are placeholders that will pass (using cannon-es from Task 1)
  </acceptance_criteria>
  <done>
    test-logic/racing-logic.test.js created with catalog, network contract, and placeholder physics/delta/lap tests. Catalog and contract tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | External packages (three, cannon-es) added to project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | npm supply chain | accept | three and cannon-es are well-maintained, widely-used packages; versions pinned in package.json |
</threat_model>

<verification>
- `npm ls three cannon-es` shows both installed
- `node -e "require('cannon-es')"` succeeds (or dynamic import wrapper documented)
- `node --test test-logic/racing-logic.test.js` passes all tests
- `grep -c "racing" lib/shared/network-contract.js` returns > 0
- `grep -c "racing" lib/games/catalog.js` returns > 0
</verification>

<success_criteria>
1. three@0.184.0 and cannon-es@0.20.0 are in package.json dependencies
2. cannon-es loads in Node.js 18 (CJS require or documented dynamic import)
3. Racing entries exist in SOCKET_EVENTS, API_ROUTE_PATTERNS, API_ROUTES
4. Racing catalog entry has gameKey "racing", familyKey "light-3d", launchState "coming-soon"
5. test-logic/racing-logic.test.js passes with catalog and contract tests green
</success_criteria>

<output>
After completion, create `.planning/phases/20-racing/20-01-SUMMARY.md`
</output>
