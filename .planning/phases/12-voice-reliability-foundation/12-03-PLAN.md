---
phase: 12-voice-reliability-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - lib/party/manager.js
  - backend/handlers/party/rooms/[roomNo]/index.js
  - pages/party/[roomNo].js
  - pages/undercover/[roomNo].js
  - test-logic/party-voice-recovery.test.js
  - test-logic/backend-contract.test.js
  - test-logic/session-recovery.test.js
  - tests/arcade-party.spec.js
  - tests/undercover.spec.js
  - package.json
autonomous: true
requirements:
  - VOICE-01
  - VOICE-02
user_setup: []
must_haves:
  truths:
    - Room payloads and room UI expose lightweight diagnostics for stable relay mode and recovery without leaking raw ICE or TURN internals.
    - Logic and browser coverage prove sticky relay, reconnect-muted recovery, and grace-expiry behavior instead of only the happy path.
    - Live-ops and release rerun helpers on `3100/3101` include voice reliability regressions without changing the canonical release boundary.
  artifacts:
    - lib/party/manager.js
    - backend/handlers/party/rooms/[roomNo]/index.js
    - pages/party/[roomNo].js
    - pages/undercover/[roomNo].js
    - test-logic/party-voice-recovery.test.js
    - test-logic/backend-contract.test.js
    - test-logic/session-recovery.test.js
    - tests/arcade-party.spec.js
    - tests/undercover.spec.js
    - package.json
  key_links:
    - lib/party/manager.js -> backend/handlers/party/rooms/[roomNo]/index.js
    - backend/handlers/party/rooms/[roomNo]/index.js -> test-logic/backend-contract.test.js
    - pages/party/[roomNo].js -> tests/arcade-party.spec.js
    - pages/undercover/[roomNo].js -> tests/undercover.spec.js
    - test-logic/party-voice-recovery.test.js -> package.json
    - package.json -> verify-release flow on `3100/3101`
---

# Phase 12 Plan 03: Add diagnostics and regression coverage for voice degradation and recovery

<objective>
Make voice reliability changes observable and release-safe on the canonical stack.

Purpose: Plans 12-01 and 12-02 will create the contract and runtime behavior, but the milestone only benefits if operators and future developers can see what mode the room is in and if the repo can catch regressions automatically. This plan adds lightweight room diagnostics, targeted relay/recovery regression cases, and release-path coverage on `3100/3101`.
Output: player-safe diagnostics for relay and recovery state, plus logic/browser/release suites that lock sticky relay and muted recovery behavior down.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-voice-reliability-foundation/12-CONTEXT.md
@.planning/phases/12-voice-reliability-foundation/12-RESEARCH.md
@.planning/phases/12-voice-reliability-foundation/12-VALIDATION.md
@.planning/phases/12-voice-reliability-foundation/12-01-PLAN.md
@.planning/phases/12-voice-reliability-foundation/12-02-PLAN.md
@package.json
@lib/party/manager.js
@backend/handlers/party/rooms/[roomNo]/index.js
@pages/party/[roomNo].js
@pages/undercover/[roomNo].js
@test-logic/backend-contract.test.js
@test-logic/session-recovery.test.js
@tests/arcade-party.spec.js
@tests/undercover.spec.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Add lightweight room diagnostics for relay mode and recovery state</name>
  <files>lib/party/manager.js, backend/handlers/party/rooms/[roomNo]/index.js, pages/party/[roomNo].js, pages/undercover/[roomNo].js, test-logic/backend-contract.test.js</files>
  <read_first>.planning/phases/12-voice-reliability-foundation/12-02-PLAN.md, lib/party/manager.js, backend/handlers/party/rooms/[roomNo]/index.js, pages/party/[roomNo].js, pages/undercover/[roomNo].js, test-logic/backend-contract.test.js</read_first>
  <action>Serialize one additive `room.voiceDiagnostics` object that exposes the exact public fields `mode`, `stickyRelay`, `runtimeState`, `lastReasonCode`, `lastTransitionAt`, `lastRecoveredAt`, `resumeMutedOnRecovery`, and `reconnectGraceSeconds`. Do not serialize raw ICE candidates, SDP blobs, `iceServers`, or TURN credentials inside diagnostics. In `pages/party/[roomNo].js` and `pages/undercover/[roomNo].js`, surface these diagnostics through existing voice cards or panels using lightweight non-modal copy and stable selectors `data-voice-mode`, `data-voice-runtime-state`, and `data-voice-recovery`. The UI should be truthful but small: for relay mode it should indicate stable mode or degraded mode; for recovery it should indicate joined-as-listening or reconnecting; it must not interrupt gameplay with modal confirmation.</action>
  <verify>node --test test-logic/backend-contract.test.js</verify>
  <acceptance_criteria>
    - serialized party room payloads contain `room.voiceDiagnostics.mode`
    - diagnostics never echo `iceServers`, SDP, or candidate payloads
    - party and Undercover voice panels render `data-voice-mode` and `data-voice-recovery`
    - `node --test test-logic/backend-contract.test.js` exits 0
  </acceptance_criteria>
  <done>Players and later operator tooling now have a lightweight, safe diagnostic surface for relay fallback and muted recovery state.</done>
</task>

<task type="code">
  <name>Task 2: Promote relay/recovery regressions into helper commands and the canonical release gate</name>
  <files>test-logic/party-voice-recovery.test.js, test-logic/session-recovery.test.js, tests/arcade-party.spec.js, tests/undercover.spec.js, package.json</files>
  <read_first>.planning/phases/12-voice-reliability-foundation/12-VALIDATION.md, package.json, test-logic/session-recovery.test.js, tests/arcade-party.spec.js, tests/undercover.spec.js</read_first>
  <action>Create `test-logic/party-voice-recovery.test.js` with deterministic manager-level cases for `startup-timeout => relay-required`, `persistent-disconnect => relay-required`, sticky relay persisting for the room visit, reconnect within grace producing muted auto-resume eligibility, reconnect after grace clearing auto-resume, and operator-blocked voice still preventing join even if relay is available. Extend `tests/arcade-party.spec.js` and `tests/undercover.spec.js` so the browser suite asserts `data-voice-mode="relay-required"` and muted recovery behavior in both general party and Undercover flows. Update `package.json` so `test:logic:liveops` includes `test-logic/party-voice-recovery.test.js` and `test:ui:liveops` includes `tests/undercover.spec.js` alongside `tests/arcade-party.spec.js`. Preserve `test:logic:critical`, `test:ui:critical`, and `verify:release` as the canonical ship boundary. Finish by proving the deployed-stack rerun still passes on `3100/3101`.</action>
  <verify>npm run deploy:3100 && npm run test:logic:liveops && FRONTEND_BASE_URL=http://127.0.0.1:3100 npm run test:ui:critical && npm run verify:release -- --skip-deploy</verify>
  <acceptance_criteria>
    - `test-logic/party-voice-recovery.test.js` exists and covers relay promotion, sticky relay, muted recovery, and grace expiry
    - `tests/arcade-party.spec.js` and `tests/undercover.spec.js` assert `data-voice-mode="relay-required"` or equivalent relay-mode selectors
    - `package.json` `test:logic:liveops` includes `test-logic/party-voice-recovery.test.js`
    - `package.json` `test:ui:liveops` includes `tests/undercover.spec.js`
    - `npm run deploy:3100 && npm run test:logic:liveops && FRONTEND_BASE_URL=http://127.0.0.1:3100 npm run test:ui:critical && npm run verify:release -- --skip-deploy` exits 0
  </acceptance_criteria>
  <done>The repo can now catch relay fallback and muted recovery regressions on the same deployed-stack release path used for shipping.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Diagnostics cross the backend/frontend boundary and may later feed admin tooling, so they must remain truthful and safe to expose.
- Test suites and package scripts are the release evidence for voice reliability; if they omit sticky relay or muted recovery cases, the milestone's core risk remains unguarded.
- `verify:release` is the canonical deployed-stack boundary, so voice reliability coverage must integrate with it rather than creating a parallel ship story.
</trust_boundaries>

<stride_register>
- threat_id: T-12-07
  category: Repudiation
  component: room relay/recovery diagnostics
  disposition: mitigate
  mitigation_plan: expose stable public mode/reason/timestamp diagnostics in room payloads and UI so fallback and recovery state remain auditable.
- threat_id: T-12-08
  category: Availability
  component: regression coverage
  disposition: mitigate
  mitigation_plan: add deterministic logic and browser coverage for relay promotion, sticky relay, muted recovery, and grace expiry, then wire those cases into existing helper commands.
- threat_id: T-12-09
  category: Information Disclosure
  component: voice diagnostics surface
  disposition: mitigate
  mitigation_plan: keep diagnostics to mode/reason/timestamps only and explicitly exclude raw ICE server, candidate, and SDP details from player-visible payloads.
</stride_register>
</threat_model>

<verification>
- [ ] `node --test test-logic/backend-contract.test.js`
- [ ] `npm run deploy:3100`
- [ ] `npm run test:logic:liveops`
- [ ] `FRONTEND_BASE_URL=http://127.0.0.1:3100 npm run test:ui:critical`
- [ ] `npm run verify:release -- --skip-deploy`
- [ ] helper commands and release reruns now cover relay fallback and muted recovery
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Diagnostics stay lightweight and safe while remaining useful for future admin tooling
- Phase 12 voice reliability is release-proven on the canonical `3100/3101` stack instead of living only in local happy-path behavior
</success_criteria>

<output>
After completion, create `.planning/phases/12-voice-reliability-foundation/12-03-SUMMARY.md`
</output>
