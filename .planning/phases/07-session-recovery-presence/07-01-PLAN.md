---
phase: 07-session-recovery-presence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/shared/network-contract.js
  - lib/auth.js
  - lib/game/room-manager.js
  - lib/party/manager.js
  - lib/board/manager.js
  - backend/handlers/me.js
  - backend/handlers/rooms/[roomNo]/index.js
  - backend/handlers/party/rooms/[roomNo]/index.js
  - backend/handlers/board/rooms/[roomNo]/index.js
  - test-logic/hub-room-entry.test.js
  - test-logic/client-network-contract.test.js
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
user_setup: []
must_haves:
  truths:
    - Room payloads and `/api/me` expose one additive recovery contract for real users and scoped guests instead of forcing pages to infer continuity from ad hoc booleans.
    - The shared recovery contract stays compatible with the shipped room-entry flow and does not introduce a new authentication or invite endpoint.
    - Existing consumers that still read `connected` remain functional while Phase 7 introduces richer presence metadata.
  artifacts:
    - lib/shared/network-contract.js
    - backend/handlers/me.js
    - backend/handlers/rooms/[roomNo]/index.js
    - backend/handlers/party/rooms/[roomNo]/index.js
    - backend/handlers/board/rooms/[roomNo]/index.js
    - test-logic/hub-room-entry.test.js
    - test-logic/client-network-contract.test.js
  key_links:
    - lib/auth.js -> backend/handlers/me.js
    - lib/game/room-manager.js -> backend/handlers/rooms/[roomNo]/index.js
    - lib/party/manager.js -> backend/handlers/party/rooms/[roomNo]/index.js
    - lib/board/manager.js -> backend/handlers/board/rooms/[roomNo]/index.js
    - lib/shared/network-contract.js -> test-logic/client-network-contract.test.js
---

# Phase 7 Plan 01: Standardize the recovery and presence contract

<objective>
Define the additive backend and serialization contract that all later recovery work depends on.

Purpose: Phase 7 should not begin with UI heuristics or manager-local assumptions. The browser, detail handlers, and room serializers need one explicit contract for seat continuity and presence before reconnect state transitions are implemented.
Output: shared recovery/presence fields in the serialized room and session payloads, plus node coverage that locks the shape.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-session-recovery-presence/07-CONTEXT.md
@.planning/phases/07-session-recovery-presence/07-RESEARCH.md
@.planning/phases/07-session-recovery-presence/07-VALIDATION.md
@.planning/milestones/v1.0-phases/03-hub-room-expansion-framework/03-02-SUMMARY.md
@lib/auth.js
@lib/shared/network-contract.js
@lib/game/room-manager.js
@lib/party/manager.js
@lib/board/manager.js
@backend/handlers/me.js
@backend/handlers/rooms/[roomNo]/index.js
@backend/handlers/party/rooms/[roomNo]/index.js
@backend/handlers/board/rooms/[roomNo]/index.js
@test-logic/hub-room-entry.test.js
@test-logic/client-network-contract.test.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Add an explicit recovery and presence payload shape to session and room serializers</name>
  <files>lib/shared/network-contract.js, lib/auth.js, lib/game/room-manager.js, lib/party/manager.js, lib/board/manager.js, backend/handlers/me.js</files>
  <read_first>.planning/phases/07-session-recovery-presence/07-CONTEXT.md, .planning/phases/07-session-recovery-presence/07-RESEARCH.md, lib/auth.js, lib/shared/network-contract.js, lib/game/room-manager.js, lib/party/manager.js, lib/board/manager.js, backend/handlers/me.js</read_first>
  <action>Introduce one additive Phase 7 recovery contract that can be reused by every room page. Add shared contract constants or helper serializers as needed so room/session payloads can expose exact fields such as `presenceState`, `recoveryEligible`, and `reconnectGraceEndsAt` without forcing each page to derive them differently. Use the current `connected` boolean as the compatibility baseline in this plan; the richer `reconnecting` transition will arrive in Plan 02. Keep `connected` in the payload for brownfield compatibility. Extend `/api/me` so valid real-user and scoped guest sessions expose the recovery metadata the browser needs before loading room detail pages, but do not invent a new recovery-only endpoint or new cookie. Preserve the existing guest scope guarantees in `lib/auth.js`.</action>
  <verify>node --test test-logic/hub-room-entry.test.js test-logic/client-network-contract.test.js</verify>
  <acceptance_criteria>
    - room and session payloads expose an explicit `presenceState` field without removing the existing `connected` boolean
    - recovery-related fields are additive and available to both real-user and scoped guest consumers where applicable
    - `lib/shared/network-contract.js` or an equivalent shared contract location defines the Phase 7 payload expectations instead of leaving them page-local
    - `node --test test-logic/hub-room-entry.test.js test-logic/client-network-contract.test.js` exits 0
  </acceptance_criteria>
  <done>The backend and serialization layers expose one shared recovery contract that later plans can implement and consume safely.</done>
</task>

<task type="code">
  <name>Task 2: Align the three room detail handlers to the same recovery contract and lock the shape with tests</name>
  <files>backend/handlers/rooms/[roomNo]/index.js, backend/handlers/party/rooms/[roomNo]/index.js, backend/handlers/board/rooms/[roomNo]/index.js, test-logic/hub-room-entry.test.js, test-logic/client-network-contract.test.js</files>
  <read_first>backend/handlers/rooms/[roomNo]/index.js, backend/handlers/party/rooms/[roomNo]/index.js, backend/handlers/board/rooms/[roomNo]/index.js, backend/handlers/me.js, test-logic/hub-room-entry.test.js, test-logic/client-network-contract.test.js</read_first>
  <action>Ensure card, party, and board detail routes all expose the same recovery/presence contract shape for the viewer and player lists, even though their underlying managers serialize different gameplay payloads. Add or extend node tests so Phase 7 locks the exact additive behavior: a valid session can discover whether a seat is recoverable, scoped guests still stay room-bound, and the three detail handlers no longer drift in recovery field naming. Keep the routes public exactly as shipped; the contract should be stricter, not more permissive.</action>
  <verify>node --test test-logic/hub-room-entry.test.js test-logic/client-network-contract.test.js && npm run check</verify>
  <acceptance_criteria>
    - card, party, and board room detail handlers expose the same recovery field names for viewer/session continuity
    - guest scope is still enforced at the handler/session layer rather than delegated to page code
    - recovery metadata is present on room payloads before any client-side reconnect logic is attempted
    - `node --test test-logic/hub-room-entry.test.js test-logic/client-network-contract.test.js && npm run check` exits 0
  </acceptance_criteria>
  <done>The room-detail surface is consistent enough that later reconnect logic and UI work can rely on one contract instead of per-family conditionals.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Browser room pages rely on `/api/me` and room detail routes to decide whether a session may resume a live seat.
- Guest and real-user sessions share one auth cookie name, so contract drift could blur their recovery boundaries.
- Card, party, and board room serializers are independent modules; inconsistent recovery fields would create family-specific bugs.
</trust_boundaries>

<stride_register>
- threat_id: T-07-01
  category: Spoofing
  component: recovery eligibility inference
  disposition: mitigate
  mitigation_plan: expose recovery eligibility from backend-owned session/detail payloads instead of trusting browser-local inference.
- threat_id: T-07-02
  category: Information Disclosure
  component: room presence metadata
  disposition: mitigate
  mitigation_plan: add only room-safe presence fields needed for recovery and host visibility; do not expose extra auth or socket internals.
- threat_id: T-07-03
  category: Tampering
  component: cross-family payload consistency
  disposition: mitigate
  mitigation_plan: lock the shared contract in node tests across `/api/me` and all three room detail handlers.
</stride_register>
</threat_model>

<verification>
- [ ] `node --test test-logic/hub-room-entry.test.js test-logic/client-network-contract.test.js`
- [ ] `npm run check`
- [ ] card, party, and board room detail payloads share one recovery field vocabulary
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 has one additive backend-owned recovery contract that later plans can implement and consume safely
</success_criteria>

<output>
After completion, create `.planning/phases/07-session-recovery-presence/07-01-SUMMARY.md`
</output>
