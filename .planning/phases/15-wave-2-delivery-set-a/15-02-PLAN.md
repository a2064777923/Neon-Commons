---
phase: 15-wave-2-delivery-set-a
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - components/game-hub/GameIcon.js
  - pages/games/[gameKey].js
  - pages/board/[roomNo].js
  - styles/GameLobby.module.css
  - styles/BoardRoom.module.css
  - test-logic/admin-control-plane.test.js
  - tests/hub-entry.spec.js
  - tests/board-games.spec.js
autonomous: true
requirements:
  - WAVE-02
user_setup: []
must_haves:
  truths:
    - The new title must feel native to the existing board lobby and shared board-room shell.
    - Mobile-readable Flying Chess UI and truthful room-entry/admin rollout behavior are part of the ship bar, not optional polish.
    - Regression coverage must prove that existing families still work after `flyingchess` goes live.
  artifacts:
    - pages/games/[gameKey].js
    - pages/board/[roomNo].js
    - styles/GameLobby.module.css
    - styles/BoardRoom.module.css
    - tests/board-games.spec.js
    - tests/hub-entry.spec.js
  key_links:
    - pages/games/[gameKey].js -> styles/GameLobby.module.css
    - pages/board/[roomNo].js -> styles/BoardRoom.module.css
    - pages/board/[roomNo].js -> tests/board-games.spec.js
    - pages/games/[gameKey].js -> tests/hub-entry.spec.js
---

# Phase 15 Plan 02: Ship the Flying Chess UI, entry truth, and regression coverage

<objective>
Consume the new Flying Chess runtime in the shipped browser surfaces and lock it with regression coverage.

Purpose: once the backend-owned runtime exists, Phase 15 still needs truthful lobby/room UI, mobile-safe interaction, and release-facing regression coverage so the first real Wave 2 title ships without breaking the existing arcade.
Output: live Flying Chess iconography, board-lobby integration, shared board-room rendering with stable hooks, and browser/contract coverage that protects existing families.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-wave-2-delivery-set-a/15-CONTEXT.md
@.planning/phases/15-wave-2-delivery-set-a/15-RESEARCH.md
@.planning/phases/15-wave-2-delivery-set-a/15-UI-SPEC.md
@.planning/phases/15-wave-2-delivery-set-a/15-VALIDATION.md
@components/game-hub/GameIcon.js
@pages/games/[gameKey].js
@pages/board/[roomNo].js
@styles/GameLobby.module.css
@styles/BoardRoom.module.css
@tests/board-games.spec.js
@tests/hub-entry.spec.js
@test-logic/admin-control-plane.test.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Add Flying Chess lobby and board-room rendering that follows the Phase 15 UI contract</name>
  <files>components/game-hub/GameIcon.js, pages/games/[gameKey].js, pages/board/[roomNo].js, styles/GameLobby.module.css, styles/BoardRoom.module.css</files>
  <read_first>.planning/phases/15-wave-2-delivery-set-a/15-UI-SPEC.md, pages/games/[gameKey].js, pages/board/[roomNo].js, styles/GameLobby.module.css, styles/BoardRoom.module.css, components/game-hub/GameIcon.js</read_first>
  <action>Extend the shipped board-family UI instead of creating new routes. Add a dedicated `flyingchess` icon in `components/game-hub/GameIcon.js`. In `pages/games/[gameKey].js`, add `themeFlyingchess`, Flying Chess-specific realtime copy, truthful board-summary chips, and room-create copy that supports `2`, `3`, and `4` player rooms without promising bot fill if runtime support is absent. In `pages/board/[roomNo].js`, add the `flyingchess` render branch inside the shared board shell with a centered board, seat/progress summary, explicit roll-versus-move phase messaging, and stable hooks `data-flyingchess-board`, `data-flyingchess-phase`, `data-flyingchess-roll`, `data-flyingchess-dice`, `data-flyingchess-seat`, `data-flyingchess-piece`, `data-flyingchess-target`, and `data-flyingchess-progress`. Update `styles/GameLobby.module.css` and `styles/BoardRoom.module.css` with a Flying Chess-specific theme that follows the approved amber / sky / coral / mint accent contract and preserves mobile readability down to phone widths.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - `components/game-hub/GameIcon.js` contains a dedicated `if (gameKey === "flyingchess")`
    - `pages/games/[gameKey].js` contains `themeFlyingchess`
    - `pages/board/[roomNo].js` contains all required `data-flyingchess-*` hooks from the UI-SPEC
    - `styles/BoardRoom.module.css` contains a Flying Chess theme block
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Players can discover, create, and play Flying Chess inside the shipped board UI language with stable mobile-safe affordances.</done>
</task>

<task type="code">
  <name>Task 2: Lock live-entry, admin rollout truth, and board smoke coverage for the first Wave 2 title</name>
  <files>test-logic/admin-control-plane.test.js, tests/hub-entry.spec.js, tests/board-games.spec.js</files>
  <read_first>.planning/phases/15-wave-2-delivery-set-a/15-VALIDATION.md, tests/hub-entry.spec.js, tests/board-games.spec.js, test-logic/admin-control-plane.test.js, .planning/phases/14-wave-2-launch-contract/14-02-SUMMARY.md</read_first>
  <action>Extend regression coverage so `flyingchess` is treated as a real live title instead of staged metadata. In `test-logic/admin-control-plane.test.js`, lock that admin capability/rollout surfaces now include the live managed board title. In `tests/hub-entry.spec.js`, prove the hub and family entry surfaces expose truthful live navigation for `flyingchess` instead of a dead coming-soon CTA. In `tests/board-games.spec.js`, add a full smoke path that creates a Flying Chess room on `3100`, readies enough seats, rolls, moves a piece through at least one legal turn, verifies recovery/reload keeps the room route stable, and closes the room through the existing admin cleanup path. Keep existing Gomoku and Chinese Checkers smoke behavior intact.</action>
  <verify>node --test test-logic/admin-control-plane.test.js && FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/board-games.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `test-logic/admin-control-plane.test.js` contains Flying Chess rollout/capability expectations
    - `tests/hub-entry.spec.js` contains Flying Chess live-entry coverage
    - `tests/board-games.spec.js` contains a Flying Chess create / join / play smoke path
    - `node --test test-logic/admin-control-plane.test.js && FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/board-games.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>The first Wave 2 title is now regression-locked across admin rollout truth, hub entry, and board-room smoke behavior.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- The browser shell consumes backend match state for a new board title; if the UI invents its own move semantics, player actions and room recovery will drift.
- Hub and admin surfaces already expose launch truth for staged titles; promoting `flyingchess` to live must not leave stale coming-soon or rollout assumptions behind.
- Board smoke tests run against the canonical `3100/3101` split runtime, so selectors and room flows must stay stable enough for release verification.
</trust_boundaries>

<stride_register>
- threat_id: T-15-05
  category: Spoofing
  component: turn-state and move affordances
  disposition: mitigate
  mitigation_plan: render roll/move affordances directly from backend-owned `turnPhase`, legal-piece, and target data rather than client-derived guesses.
- threat_id: T-15-06
  category: Tampering
  component: mobile board interactions
  disposition: mitigate
  mitigation_plan: use stable `data-flyingchess-*` hooks and explicit touch-sized controls so later styling changes cannot silently remove core actions.
- threat_id: T-15-07
  category: Availability
  component: live-entry and rollout truth
  disposition: mitigate
  mitigation_plan: extend hub and admin regression tests so `flyingchess` is proven live where intended and never rendered as a dead CTA.
- threat_id: T-15-08
  category: Repudiation
  component: smoke and release verification
  disposition: mitigate
  mitigation_plan: add deterministic board smoke coverage on `3100` so recovery, room close, and first-turn interaction have replayable evidence in CI/manual runs.
</stride_register>
</threat_model>

<verification>
- [ ] `npm run check`
- [ ] `node --test test-logic/admin-control-plane.test.js`
- [ ] `FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/board-games.spec.js --workers=1`
- [ ] `npm run test:logic:critical`
- [ ] `FRONTEND_BASE_URL=http://127.0.0.1:3100 npm run test:ui:critical`
- [ ] `npm run verify:release -- --skip-deploy`
- [ ] existing shipped families continue to pass hub and board smoke flows after Flying Chess goes live
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `flyingchess` is visually and behaviorally native to the shipped board-family surfaces
- Hub/admin rollout truth and board-room smoke coverage protect the first Wave 2 title from regressing existing families
</success_criteria>

<output>
After completion, create `.planning/phases/15-wave-2-delivery-set-a/15-02-SUMMARY.md`
</output>
