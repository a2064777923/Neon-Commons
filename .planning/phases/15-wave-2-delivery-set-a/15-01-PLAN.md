---
phase: 15-wave-2-delivery-set-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/games/catalog.js
  - lib/board/flyingchess.js
  - lib/board/manager.js
  - test-logic/board-config.test.js
  - test-logic/flyingchess-logic.test.js
  - test-logic/backend-contract.test.js
autonomous: true
requirements:
  - WAVE-02
user_setup: []
must_haves:
  truths:
    - `flyingchess` ships through the existing board-family runtime instead of a new room stack.
    - The backend owns the classic rule table, move legality, room serialization, and recovery-safe match state.
    - Human multiplayer flow must work for 2-4 players even if bot fill is deferred.
  artifacts:
    - lib/games/catalog.js
    - lib/board/flyingchess.js
    - lib/board/manager.js
    - test-logic/flyingchess-logic.test.js
    - test-logic/backend-contract.test.js
  key_links:
    - lib/games/catalog.js -> lib/board/manager.js
    - lib/board/flyingchess.js -> lib/board/manager.js
    - lib/board/manager.js -> test-logic/flyingchess-logic.test.js
    - lib/board/manager.js -> test-logic/backend-contract.test.js
---

# Phase 15 Plan 01: Build the Flying Chess runtime on the shared board-family contract

<objective>
Turn `flyingchess` from staged Wave 2 metadata into a real board-family runtime.

Purpose: Phase 14 already locked discovery and rollout truth, but the repo still lacks a real first Wave 2 room-based title. This plan adds the backend-owned runtime contract: catalog promotion, board config, pure rule logic, room-manager integration, and contract tests.
Output: a playable `flyingchess` room runtime that reuses board-family create/join/recovery flows and exposes truthful match state to the shared board shell.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-wave-2-delivery-set-a/15-CONTEXT.md
@.planning/phases/15-wave-2-delivery-set-a/15-RESEARCH.md
@.planning/phases/15-wave-2-delivery-set-a/15-UI-SPEC.md
@.planning/phases/15-wave-2-delivery-set-a/15-VALIDATION.md
@docs/architecture/backend-contract.md
@docs/api/api-reference.md
@lib/games/catalog.js
@lib/board/manager.js
@backend/handlers/board/rooms/index.js
@backend/handlers/board/rooms/[roomNo]/index.js
@test-logic/board-config.test.js
@test-logic/chinesecheckers-logic.test.js
@test-logic/reversi-logic.test.js
@test-logic/backend-contract.test.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Promote `flyingchess` into the live board catalog and shared board config helpers</name>
  <files>lib/games/catalog.js, test-logic/board-config.test.js</files>
  <read_first>.planning/phases/15-wave-2-delivery-set-a/15-CONTEXT.md, .planning/phases/15-wave-2-delivery-set-a/15-RESEARCH.md, .planning/phases/15-wave-2-delivery-set-a/15-UI-SPEC.md, lib/games/catalog.js, test-logic/board-config.test.js</read_first>
  <action>Update `lib/games/catalog.js` so `flyingchess` is no longer staged metadata-only. Set `route: "/games/flyingchess"`, `detailRoutePrefix: "/board"`, `supportsShareLink: true`, `guestMode: "invite-private-only"`, `launchState: "live"`, and `isShipped: true`. Add `flyingchess` to `BOARD_GAME_KEYS`. Extend shared board helpers so `getBoardDefaultConfig("flyingchess")` returns `{ visibility: "public", maxPlayers: 4, turnSeconds: 20 }`, `getGameLimits("flyingchess")` returns `{ minPlayers: 2, maxPlayers: 4 }`, `getBoardPlayerOptions("flyingchess")` returns `[2, 3, 4]`, and `getBoardConfigSummary("flyingchess", config)` returns exact human-readable chips for player count, turn timer, and classic-rule posture. Extend `test-logic/board-config.test.js` to lock those helper values and normalization behavior.</action>
  <verify>node --test test-logic/board-config.test.js</verify>
  <acceptance_criteria>
    - `lib/games/catalog.js` contains `route: "/games/flyingchess"`
    - `lib/games/catalog.js` contains `const BOARD_GAME_KEYS = Object.freeze(["gomoku", "chinesecheckers", "reversi", "flyingchess"])`
    - `lib/games/catalog.js` contains `getBoardPlayerOptions("flyingchess")` behavior for `2`, `3`, and `4`
    - `test-logic/board-config.test.js` contains Flying Chess board-config assertions
    - `node --test test-logic/board-config.test.js` exits 0
  </acceptance_criteria>
  <done>`flyingchess` is a real board-family title in shared catalog/config helpers instead of a coming-soon placeholder.</done>
</task>

<task type="code">
  <name>Task 2: Add pure Flying Chess rules and integrate them into the board-room manager</name>
  <files>lib/board/flyingchess.js, lib/board/manager.js, test-logic/flyingchess-logic.test.js, test-logic/backend-contract.test.js</files>
  <read_first>.planning/phases/15-wave-2-delivery-set-a/15-CONTEXT.md, .planning/phases/15-wave-2-delivery-set-a/15-RESEARCH.md, lib/board/manager.js, test-logic/chinesecheckers-logic.test.js, test-logic/reversi-logic.test.js, test-logic/backend-contract.test.js</read_first>
  <action>Create `lib/board/flyingchess.js` as the pure rules owner for classic Flying Chess with these exact rule constants: `4` pieces per seat, `52` ring cells, `6` home-lane steps, start offsets `[0, 13, 26, 39]`, takeoff on roll `6`, same-color jump `+4`, flight jump `+12`, collisions send rival pieces back to airport, rolling `6` grants an extra roll, and entering final home requires the exact count. Integrate those helpers into `lib/board/manager.js` by adding explicit `flyingchess` branches in room start, turn scheduling, timeout handling, move submission, settlement, and serialization. The match payload must expose `turnPhase`, `rollValue`, `movablePieceIds`, `extraRoll`, per-piece positions, and per-seat home progress. `submitMove(...)` must accept `{ action: "roll" }` and `{ action: "move", pieceId }` payloads for Flying Chess. If bot play is not implemented in this phase, `addBot` for `flyingchess` must fail closed with the exact message `飞行棋暂不支持 AI 补位`. Add `test-logic/flyingchess-logic.test.js` covering takeoff, jump/flight, collision return, extra-roll continuation, and exact-finish home logic. Extend `test-logic/backend-contract.test.js` so board-room serialization for `flyingchess` stays additive and recovery-safe.</action>
  <verify>node --test test-logic/board-config.test.js test-logic/flyingchess-logic.test.js test-logic/backend-contract.test.js && npm run check</verify>
  <acceptance_criteria>
    - `lib/board/flyingchess.js` exists and contains `52`, `6`, and `[0, 13, 26, 39]`
    - `lib/board/manager.js` contains explicit `room.gameKey === "flyingchess"` branches
    - `lib/board/manager.js` serializes `turnPhase`, `rollValue`, and `movablePieceIds`
    - `lib/board/manager.js` contains the exact string `飞行棋暂不支持 AI 补位` when bot fill is deferred
    - `node --test test-logic/board-config.test.js test-logic/flyingchess-logic.test.js test-logic/backend-contract.test.js && npm run check` exits 0
  </acceptance_criteria>
  <done>The shared board manager can now host a classic human-multiplayer Flying Chess room with backend-owned legality and additive room serialization.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Flying Chess legality crosses the backend/browser boundary; if the browser owns rule truth, room recovery and multi-user state can diverge.
- Board room payloads are consumed by the shared `/board/[roomNo]` shell, so new match fields must stay additive and must not break existing board titles.
- Launching a staged title as live changes hub/admin/runtime behavior at once; catalog truth and room truth must remain aligned.
</trust_boundaries>

<stride_register>
- threat_id: T-15-01
  category: Tampering
  component: board config and live-title promotion
  disposition: mitigate
  mitigation_plan: centralize `flyingchess` limits, defaults, and launch-state promotion inside `lib/games/catalog.js` and lock them with board-config tests.
- threat_id: T-15-02
  category: Availability
  component: room-state synchronization
  disposition: mitigate
  mitigation_plan: keep all Flying Chess legality, turn phase, and timeout resolution inside `lib/board/manager.js` plus pure helpers rather than page-local inference.
- threat_id: T-15-03
  category: Information Disclosure
  component: board-room serialization
  disposition: mitigate
  mitigation_plan: expose only viewer-safe match state (`turnPhase`, legal actions, piece positions, progress) and keep admin/runtime internals out of the board-room payload.
- threat_id: T-15-04
  category: Denial of Service
  component: unsupported bot path
  disposition: mitigate
  mitigation_plan: fail closed for `flyingchess` bot fill with one explicit runtime message instead of allowing the shared add-bot path to half-create invalid seats.
</stride_register>
</threat_model>

<verification>
- [ ] `node --test test-logic/board-config.test.js`
- [ ] `node --test test-logic/board-config.test.js test-logic/flyingchess-logic.test.js test-logic/backend-contract.test.js`
- [ ] `npm run check`
- [ ] board-room payloads for `flyingchess` stay additive and recovery-safe
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `flyingchess` becomes a live board-family runtime without creating a new API family or room stack
- The backend owns classic Flying Chess legality and the shared board shell can read one stable match payload
</success_criteria>

<output>
After completion, create `.planning/phases/15-wave-2-delivery-set-a/15-01-SUMMARY.md`
</output>
