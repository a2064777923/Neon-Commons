---
phase: 01-backend-contract-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
  - docs/architecture/backend-contract.md
  - docs/architecture/system-architecture.md
  - docs/api/api-reference.md
  - docs/ops/deployment.md
  - AGENTS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
autonomous: true
requirements:
  - PLAT-01
user_setup: []
must_haves:
  truths:
    - Public docs explain which process owns `/api/*` and `/socket.io/*`, plus how frontend origin resolution works.
    - Planning docs stop describing `pages/api` and root `server.js` as the live contract layer.
    - Operators and future planners can see the backend contract boundary without rereading implementation files.
  artifacts:
    - docs/architecture/backend-contract.md
    - docs/api/api-reference.md
    - .planning/codebase/ARCHITECTURE.md
  key_links:
    - docs/architecture/backend-contract.md -> README.md
    - docs/architecture/backend-contract.md -> docs/ops/deployment.md
    - backend/server.js -> .planning/codebase/ARCHITECTURE.md
---

<objective>
Refresh architecture, deployment, and planning docs around the split runtime contract.

Purpose: Phase 1 is only durable if human-facing docs and planning artifacts describe the same backend boundary that the code now enforces.
Output: Updated public docs, a dedicated backend-contract architecture note, and refreshed planning/codebase map entries.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract-foundation/01-RESEARCH.md
@README.md
@docs/architecture/system-architecture.md
@docs/api/api-reference.md
@docs/ops/deployment.md
@AGENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Write a dedicated backend contract architecture note</name>
  <files>docs/architecture/backend-contract.md, docs/architecture/system-architecture.md, docs/api/api-reference.md</files>
  <read_first>backend/server.js, backend/router.js, lib/socket-server.js, lib/client/api.js, docs/architecture/system-architecture.md, docs/api/api-reference.md, .planning/phases/01-backend-contract-foundation/01-RESEARCH.md</read_first>
  <action>Create `docs/architecture/backend-contract.md` as the source-of-truth architecture note for the split runtime. Document route ownership, socket ownership, origin/env resolution, auth cookie flow, and the relationship between `backend/handlers/**`, `lib/client/api.js`, and room pages. Then update `docs/architecture/system-architecture.md` and `docs/api/api-reference.md` to reference this note and keep endpoint families, process ownership, and socket event families aligned with the implemented contract.</action>
  <verify>rg -n "backend/server.js|backend/handlers|lib/client/api.js|/socket.io" docs/architecture/backend-contract.md docs/architecture/system-architecture.md docs/api/api-reference.md</verify>
  <acceptance_criteria>
    - `docs/architecture/backend-contract.md` exists
    - `docs/architecture/backend-contract.md` contains `backend/server.js`, `backend/handlers`, and `lib/client/api.js`
    - `docs/api/api-reference.md` still lists auth, card-room, party-room, board-room, admin, and socket families after the refresh
  </acceptance_criteria>
  <done>There is one dedicated architecture note that explains the backend contract boundary and supporting docs reference it.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh operator-facing runtime and deployment docs</name>
  <files>README.md, docs/ops/deployment.md, AGENTS.md</files>
  <read_first>README.md, docs/ops/deployment.md, AGENTS.md, .env.example, nginx/doudezhu.conf, scripts/run-stack.js, lib/client/api.js</read_first>
  <action>Update `README.md`, `docs/ops/deployment.md`, and `AGENTS.md` so they describe the real split-runtime contract after Plans 01 and 02 land. Include the frontend port (`3100`), backend port (`3101`), which env vars control public API/socket origins, how `/api` and `/socket.io` should be proxied, and which commands validate the contract layer (`npm run check`, the contract tests, and the room smoke test). Keep the instructions concrete enough that a future phase can extend the backend boundary without rediscovering deployment assumptions.</action>
  <verify>rg -n "3100|3101|NEXT_PUBLIC_API_BASE_URL|NEXT_PUBLIC_SOCKET_URL|npm run check" README.md docs/ops/deployment.md AGENTS.md</verify>
  <acceptance_criteria>
    - `README.md` and `docs/ops/deployment.md` contain the split-port runtime and public-origin env guidance
    - `AGENTS.md` continues to tell future agents to preserve the frontend/backend split and references the current validation commands
    - deployment docs explicitly mention proxying both `/api` and `/socket.io`
  </acceptance_criteria>
  <done>Operator and project guidance now match the executed backend contract model.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh stale planning codebase docs</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md</files>
  <read_first>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md, backend/server.js, backend/router.js, package.json, README.md</read_first>
  <action>Rewrite the stale sections in `.planning/codebase/ARCHITECTURE.md`, `.planning/codebase/STRUCTURE.md`, and `.planning/codebase/STACK.md` so they reflect the current split runtime. Replace `pages/api/**` and root `server.js` language with `backend/server.js`, `backend/router.js`, `backend/handlers/**`, and the dedicated frontend client helper in `lib/client/api.js`. Keep the docs concise but specific enough that later planning commands do not inherit the old monolith picture.</action>
  <verify>! rg -n "pages/api" .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/STACK.md</verify>
  <acceptance_criteria>
    - targeted `.planning/codebase/*.md` files no longer describe `pages/api`
    - targeted `.planning/codebase/*.md` files mention `backend/server.js`, `backend/router.js`, or `backend/handlers`
    - `npm run check` still exits 0 after the docs refresh
  </acceptance_criteria>
  <done>Planning artifacts now describe the same backend boundary that the code and public docs describe.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Human operators and future planning workflows rely on README/docs/.planning artifacts instead of reading implementation files directly.
- Deployment environments route public traffic through frontend and backend ports or a reverse proxy.
- Agent guidance in `AGENTS.md` influences future automated changes to the repo.
</trust_boundaries>

<stride_register>
- threat_id: T-03-01
  category: Tampering
  component: planning and architecture docs
  disposition: mitigate
  mitigation_plan: Remove stale `pages/api` and root `server.js` descriptions from the planning/codebase docs so later phases cannot plan against the wrong contract layer.
- threat_id: T-03-02
  category: Misconfiguration
  component: deployment and operator docs
  disposition: mitigate
  mitigation_plan: Document exact frontend/backend ports, env vars, and proxy expectations so operators do not expose or misroute credentialed API/socket traffic.
- threat_id: T-03-03
  category: Repudiation
  component: project guidance
  disposition: mitigate
  mitigation_plan: Put the backend boundary and validation commands in durable docs and `AGENTS.md` so later changes can be reviewed against an explicit written contract.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `rg -n "backend/server.js|backend/handlers|lib/client/api.js|/socket.io" README.md docs/architecture/backend-contract.md docs/architecture/system-architecture.md docs/api/api-reference.md docs/ops/deployment.md AGENTS.md`
- [ ] `rg -n "pages/api" .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/STACK.md` returns no matches
- [ ] `npm run check`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Public docs, planning docs, and agent guidance all describe the same split-runtime backend contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract-foundation/01-03-SUMMARY.md`
</output>
