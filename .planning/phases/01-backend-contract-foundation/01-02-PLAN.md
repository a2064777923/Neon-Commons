---
phase: 01-backend-contract-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lib/client/api.js
  - pages/**/*.js
  - components/SiteLayout.js
  - tests/room-ui.spec.js
  - .env.example
  - next.config.mjs
  - test-logic/client-network-contract.test.js
autonomous: true
requirements:
  - PLAT-01
user_setup: []
must_haves:
  truths:
    - Browser REST and Socket.IO calls resolve through one backend-aware helper path instead of scattered origin logic.
    - Frontend pages, layout code, and smoke tests stop hardcoding contract strings independently.
    - Local split-port development and proxied same-origin deployment both have explicit runtime behavior.
  artifacts:
    - lib/client/api.js
    - test-logic/client-network-contract.test.js
  key_links:
    - lib/shared/network-contract.js -> lib/client/api.js
    - lib/client/api.js -> pages/**/*.js
    - lib/client/api.js -> tests/room-ui.spec.js
---

<objective>
Normalize frontend REST and socket entry points around the shared backend contract.

Purpose: Future feature work should update one frontend network layer instead of dozens of inline strings and origin assumptions.
Output: A backend-aware client helper, shared route/event usage across pages and tests, and regression coverage for runtime resolution.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract-foundation/01-RESEARCH.md
@lib/client/api.js
@.env.example
@next.config.mjs
@components/SiteLayout.js
@pages/login.js
@pages/lobby.js
@pages/room/[roomNo].js
@pages/party/[roomNo].js
@pages/board/[roomNo].js
@tests/room-ui.spec.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build a backend-aware frontend runtime helper</name>
  <files>lib/client/api.js, .env.example, next.config.mjs</files>
  <read_first>lib/client/api.js, .env.example, next.config.mjs, backend/server.js, nginx/doudezhu.conf, lib/shared/network-contract.js</read_first>
  <action>Expand `lib/client/api.js` so it becomes the single frontend network entry. Import the shared route/socket constants from `lib/shared/network-contract.js`; add explicit backend-origin resolution for split-port local development; keep `NEXT_PUBLIC_API_BASE_URL` and `NEXT_PUBLIC_SOCKET_URL` overrides; and add a same-origin-safe branch for proxied deployments when explicit public backend origins are not provided. If `next.config.mjs` needs rewrites for local ergonomics, scope them narrowly to `/api/:path*`; do not rely on rewrites for Socket.IO unless the implementation is verified to preserve websocket behavior. Update `.env.example` comments or variable descriptions so operators know which env vars control frontend/backend origin resolution.</action>
  <verify>node --test test-logic/client-network-contract.test.js</verify>
  <acceptance_criteria>
    - `lib/client/api.js` imports or references `lib/shared/network-contract.js`
    - `lib/client/api.js` contains one code path for API origin resolution and one for socket origin resolution
    - `.env.example` documents the public backend origin variables used by the frontend helper
  </acceptance_criteria>
  <done>The frontend has one runtime helper that explains exactly how API and socket URLs are resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline route and socket usage across pages, layout, and smoke tests</name>
  <files>pages/**/*.js, components/SiteLayout.js, tests/room-ui.spec.js</files>
  <read_first>pages/login.js, pages/register.js, pages/index.js, pages/lobby.js, pages/games/[gameKey].js, pages/room/[roomNo].js, pages/party/[roomNo].js, pages/board/[roomNo].js, pages/admin/index.js, components/SiteLayout.js, tests/room-ui.spec.js, lib/client/api.js, lib/shared/network-contract.js</read_first>
  <action>Remove scattered `/api/*` string literals and duplicated socket event strings from the frontend call sites wherever the shared contract/helper can provide the value. Convert page and layout fetches to use route builders or helper wrappers from `lib/client/api.js`. Update the room smoke test so it no longer assumes browser `fetch("/api/rooms")` is always valid; it should use the same backend-aware route resolution as the application code. Keep UI behavior unchanged: login, lobby polling, room joins, admin fetches, and room subscriptions must still hit the same backend routes after the refactor.</action>
  <verify>node --test test-logic/client-network-contract.test.js && npm run check</verify>
  <acceptance_criteria>
    - `rg -n '\"/api|`/api' pages components tests` shows only approved shared-helper internals, not scattered page-level literals
    - room pages and `components/SiteLayout.js` use shared helper functions for backend calls
    - `tests/room-ui.spec.js` no longer hardcodes browser `fetch("/api/rooms")`
  </acceptance_criteria>
  <done>Frontend and smoke tests consume the backend contract through shared helpers instead of ad hoc strings.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime regression coverage for frontend contract resolution</name>
  <files>test-logic/client-network-contract.test.js</files>
  <read_first>lib/client/api.js, .env.example, tests/room-ui.spec.js, pages/room/[roomNo].js, pages/party/[roomNo].js, pages/board/[roomNo].js</read_first>
  <action>Create `test-logic/client-network-contract.test.js` with `node:test`. Cover API origin resolution with and without explicit `NEXT_PUBLIC_API_BASE_URL`, socket URL resolution with and without `NEXT_PUBLIC_SOCKET_URL`, and route-builder output for representative endpoints such as login, me, room detail, board join, and admin config. After that unit coverage is green, run the high-risk browser flow `npx playwright test tests/room-ui.spec.js --workers=1` to prove the Dou Dizhu room path still works with the normalized helper.</action>
  <verify>node --test test-logic/client-network-contract.test.js && npx playwright test tests/room-ui.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `test-logic/client-network-contract.test.js` contains explicit assertions for API and socket origin resolution
    - `node --test test-logic/client-network-contract.test.js` exits 0
    - `npx playwright test tests/room-ui.spec.js --workers=1` exits 0 after the frontend refactor
  </acceptance_criteria>
  <done>The frontend contract layer has both unit coverage and a room-level smoke check.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Browser pages and smoke tests cross from the public frontend origin into backend REST and Socket.IO origins.
- Cookie-authenticated requests rely on consistent origin, credentials, and proxy configuration.
- Shared route/event constants must stay aligned across client helpers, pages, and tests.
</trust_boundaries>

<stride_register>
- threat_id: T-02-01
  category: Spoofing
  component: cookie-authenticated browser requests
  disposition: mitigate
  mitigation_plan: Centralize API/socket origin resolution so client code cannot silently mix trusted and untrusted origins for credentialed requests.
- threat_id: T-02-02
  category: Tampering
  component: page-level route literals
  disposition: mitigate
  mitigation_plan: Replace scattered inline `/api/*` and socket event strings with shared route/event helpers and back them with direct tests.
- threat_id: T-02-03
  category: Denial of Service
  component: realtime room connectivity
  disposition: mitigate
  mitigation_plan: Preserve verified Socket.IO connection behavior and run the room smoke test after helper normalization to catch broken room subscribe/join paths immediately.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/client-network-contract.test.js`
- [ ] `npm run check`
- [ ] `npx playwright test tests/room-ui.spec.js --workers=1`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend REST and socket access flow through one backend-aware helper path that supports both split-port dev and proxied deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract-foundation/01-02-SUMMARY.md`
</output>
