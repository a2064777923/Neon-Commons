---
phase: 11-availability-signals-degraded-modes
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - test-logic/admin-control-plane.test.js
  - test-logic/backend-contract.test.js
  - test-logic/hub-room-entry.test.js
  - tests/admin-console.spec.js
  - tests/hub-entry.spec.js
  - tests/arcade-party.spec.js
  - package.json
autonomous: true
requirements:
  - AVAIL-01
  - AVAIL-03
user_setup: []
must_haves:
  truths:
    - Regression suites prove degraded-state precedence and scope correctness instead of only happy-path rendering.
    - `test:logic:liveops` and `test:ui:liveops` become truthful diagnostic helpers for the new degraded-state surface without replacing the canonical critical suites.
    - The existing `verify:release` contract on `3100/3101` remains intact while still covering the widened degraded-mode behavior.
  artifacts:
    - test-logic/admin-control-plane.test.js
    - test-logic/backend-contract.test.js
    - test-logic/hub-room-entry.test.js
    - tests/admin-console.spec.js
    - tests/hub-entry.spec.js
    - tests/arcade-party.spec.js
    - package.json
  key_links:
    - test-logic/admin-control-plane.test.js -> package.json
    - test-logic/hub-room-entry.test.js -> tests/hub-entry.spec.js
    - tests/arcade-party.spec.js -> package.json
    - package.json -> existing `verify:release` flow on `3100/3101`
---

# Phase 11 Plan 03: Verify degraded-state behavior across critical room-entry and release flows

<objective>
Prove that degraded-state behavior is correct, scoped, and release-safe on the canonical stack.

Purpose: Plans 11-01 and 11-02 will create the contract and the runtime/UI behavior, but the milestone only benefits if the repo can catch regressions automatically. This plan widens deterministic node and browser coverage around degraded-state precedence, then promotes that coverage into the existing `liveops` helper commands and release rerun path.
Output: regression suites that prove degraded-mode semantics and scope boundaries, plus helper commands and release verification that include the new surface without changing the canonical `3100/3101` contract.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-availability-signals-degraded-modes/11-RESEARCH.md
@.planning/phases/11-availability-signals-degraded-modes/11-VALIDATION.md
@.planning/phases/11-availability-signals-degraded-modes/11-01-PLAN.md
@.planning/phases/11-availability-signals-degraded-modes/11-02-PLAN.md
@package.json
@test-logic/admin-control-plane.test.js
@test-logic/backend-contract.test.js
@test-logic/hub-room-entry.test.js
@tests/admin-console.spec.js
@tests/hub-entry.spec.js
@tests/arcade-party.spec.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Add deterministic degraded-state regression coverage for precedence, scope, and compatibility</name>
  <files>test-logic/admin-control-plane.test.js, test-logic/backend-contract.test.js, test-logic/hub-room-entry.test.js, tests/admin-console.spec.js, tests/hub-entry.spec.js, tests/arcade-party.spec.js</files>
  <read_first>.planning/phases/11-availability-signals-degraded-modes/11-VALIDATION.md, test-logic/admin-control-plane.test.js, test-logic/backend-contract.test.js, test-logic/hub-room-entry.test.js, tests/hub-entry.spec.js, tests/admin-console.spec.js, tests/arcade-party.spec.js</read_first>
  <action>Extend the node and browser suites so Phase 11 proves the tricky cases instead of only the happy path. At minimum, add coverage for: a family-scoped degraded mode affecting one family but not another; a live party room with `voice` blocked or degraded while room gameplay remains healthy; a blocked entry path that stops risky auto-enter actions while still rendering safe-action guidance; and precedence where `snapshot-only` recovery truth coexists with degraded-mode signals instead of being overwritten by them. `test-logic/admin-control-plane.test.js` should assert that runtime reads, writes, and audit records keep the same scope/state semantics the UI depends on. `test-logic/hub-room-entry.test.js` and `test-logic/backend-contract.test.js` should assert payload consistency across hub, room-entry, live-room detail, and recovery-aware detail flows. `tests/hub-entry.spec.js`, `tests/admin-console.spec.js`, and `tests/arcade-party.spec.js` should assert the same stable degraded selectors and safe-action behavior on the browser side.</action>
  <verify>npm run test:logic:liveops && FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/admin-console.spec.js tests/arcade-party.spec.js --workers=1</verify>
  <acceptance_criteria>
    - node tests cover degraded-state precedence and unaffected-family continuity
    - browser tests cover degraded entry, degraded admin controls, and degraded party-room voice behavior
    - snapshot-only recovery semantics remain distinct from operator-driven degraded mode in the assertions
    - `npm run test:logic:liveops && FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/admin-console.spec.js tests/arcade-party.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>The repo can now catch degraded-state contract regressions and scope mistakes before release, not just after operator reports.</done>
</task>

<task type="code">
  <name>Task 2: Promote degraded-state coverage into the existing live-ops helper commands and release rerun path</name>
  <files>package.json</files>
  <read_first>package.json, .planning/phases/11-availability-signals-degraded-modes/11-VALIDATION.md, .planning/phases/11-availability-signals-degraded-modes/11-02-PLAN.md</read_first>
  <action>Update `package.json` so the existing Phase 10 helper commands stay truthful for the new degraded-state surface. `test:logic:liveops` should include the degraded-mode control-plane coverage from `test-logic/admin-control-plane.test.js` in addition to the existing recovery and room-ops logic suites. `test:ui:liveops` should include `tests/arcade-party.spec.js` so party-room voice degradation becomes part of the narrow operator rerun path, not only the broader critical suite. Keep `test:logic:critical`, `test:ui:critical`, and `verify:release` intact as the canonical release boundary; this plan only ensures the helper commands and rerun path describe the widened Phase 11 surface truthfully. Finish by proving `npm run verify:release -- --skip-deploy` still passes against the canonical ports.</action>
  <verify>FRONTEND_BASE_URL=http://127.0.0.1:3100 npm run test:ui:liveops && npm run verify:release -- --skip-deploy</verify>
  <acceptance_criteria>
    - `package.json` `test:logic:liveops` includes `test-logic/admin-control-plane.test.js`
    - `package.json` `test:ui:liveops` includes `tests/arcade-party.spec.js`
    - `package.json` still preserves the existing `test:logic:critical`, `test:ui:critical`, and `verify:release` commands
    - `FRONTEND_BASE_URL=http://127.0.0.1:3100 npm run test:ui:liveops && npm run verify:release -- --skip-deploy` exits 0
  </acceptance_criteria>
  <done>The narrow live-ops helper commands and canonical release rerun path now include degraded-state coverage without fragmenting the shipped release contract.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Test suites and package scripts are the operator-facing evidence that degraded mode is safe to ship; if they omit key cases, runtime confidence is false.
- Helper commands must stay aligned with the broader critical/release commands or operators will rerun the wrong subset after failures.
- Precedence between recovery-only room state and operator-degraded subsystems spans both backend and browser logic, so it is easy for only one side to regress.
</trust_boundaries>

<stride_register>
- threat_id: T-11-07
  category: Repudiation
  component: degraded-state regression coverage
  disposition: mitigate
  mitigation_plan: add deterministic node and browser assertions for degraded-state precedence, scope, and compatibility instead of relying on manual spot checks.
- threat_id: T-11-08
  category: Tampering
  component: live-ops helper commands
  disposition: mitigate
  mitigation_plan: keep `test:logic:liveops` and `test:ui:liveops` aligned with the real degraded-state surface and preserve the canonical critical/release commands unchanged.
- threat_id: T-11-09
  category: Availability
  component: release rerun path on `3100/3101`
  disposition: mitigate
  mitigation_plan: prove `verify:release -- --skip-deploy` still covers the widened degraded-state surface on the canonical stack.
</stride_register>
</threat_model>

<verification>
- [ ] `npm run test:logic:liveops`
- [ ] `FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/admin-console.spec.js tests/arcade-party.spec.js --workers=1`
- [ ] `FRONTEND_BASE_URL=http://127.0.0.1:3100 npm run test:ui:liveops`
- [ ] `npm run verify:release -- --skip-deploy`
- [ ] degraded-state coverage is included in the narrow helper commands and the canonical rerun path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 11 degraded-state behavior is now release-proven on the canonical stack instead of living only in local feature tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-availability-signals-degraded-modes/11-03-SUMMARY.md`
</output>
