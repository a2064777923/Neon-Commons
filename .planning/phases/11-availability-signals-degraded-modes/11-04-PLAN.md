---
phase: 11-availability-signals-degraded-modes
plan: 04
type: execute
wave: 4
depends_on:
  - 11-02
  - 11-03
files_modified:
  - lib/shared/availability.js
  - backend/handlers/hub.js
  - backend/handlers/room-entry/resolve.js
  - backend/handlers/party/rooms/[roomNo]/index.js
  - lib/party/manager.js
  - pages/index.js
  - pages/entry/[gameKey]/[roomNo].js
  - pages/undercover/[roomNo].js
  - styles/UndercoverRoom.module.css
  - test-logic/hub-room-entry.test.js
  - test-logic/backend-contract.test.js
  - tests/undercover.spec.js
autonomous: true
gap_closure: true
requirements:
  - AVAIL-01
  - AVAIL-03
user_setup: []
must_haves:
  truths:
    - `undercover` no longer receives generic text-only voice guidance when the game still advertises `voiceEnabled: true`.
    - Hub, entry, and the dedicated Undercover room agree on the same backend-authored voice status semantics for that title.
    - The active clue speaker in Undercover can connect or unmute through the existing party voice path when voice is allowed, while blocked/degraded guidance remains explicit for everyone else.
  artifacts:
    - lib/shared/availability.js
    - backend/handlers/hub.js
    - backend/handlers/room-entry/resolve.js
    - backend/handlers/party/rooms/[roomNo]/index.js
    - lib/party/manager.js
    - pages/index.js
    - pages/entry/[gameKey]/[roomNo].js
    - pages/undercover/[roomNo].js
    - styles/UndercoverRoom.module.css
    - test-logic/hub-room-entry.test.js
    - test-logic/backend-contract.test.js
    - tests/undercover.spec.js
  key_links:
    - lib/shared/availability.js -> backend/handlers/hub.js
    - lib/shared/availability.js -> backend/handlers/room-entry/resolve.js
    - backend/handlers/party/rooms/[roomNo]/index.js -> pages/undercover/[roomNo].js
    - lib/party/manager.js -> pages/undercover/[roomNo].js
    - pages/undercover/[roomNo].js -> tests/undercover.spec.js
    - backend/handlers/hub.js -> test-logic/hub-room-entry.test.js
---

# Phase 11 Plan 04: Close the Undercover voice-truthfulness gap

<objective>
Close the Phase 11 UAT gap where `undercover` is guided as if party voice can only fall back to text, even though the title advertises `voiceEnabled: true` and should still support turn-scoped speaking.

Purpose: Phase 11 unified degraded-mode messaging for the party family, but the shared contract stopped at a family-level voice fallback. UAT exposed that this is not truthful for the dedicated Undercover route: the backend exposes party voice status, yet `/undercover/[roomNo]` has no voice affordance and no active-speaker guidance, so hub/entry safe actions drift from actual gameplay.
Output: game-aware voice guidance for Undercover across hub/entry/room surfaces, plus a dedicated room affordance that lets the active clue speaker open mic when voice is available.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-availability-signals-degraded-modes/11-RESEARCH.md
@.planning/phases/11-availability-signals-degraded-modes/11-UAT.md
@.planning/phases/11-availability-signals-degraded-modes/11-02-SUMMARY.md
@.planning/phases/11-availability-signals-degraded-modes/11-03-SUMMARY.md
@lib/shared/availability.js
@backend/handlers/hub.js
@backend/handlers/room-entry/resolve.js
@backend/handlers/party/rooms/[roomNo]/index.js
@lib/party/manager.js
@pages/index.js
@pages/entry/[gameKey]/[roomNo].js
@pages/undercover/[roomNo].js
@test-logic/hub-room-entry.test.js
@test-logic/backend-contract.test.js
@tests/undercover.spec.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Make party voice guidance game-aware for Undercover entry and discovery surfaces</name>
  <files>lib/shared/availability.js, backend/handlers/hub.js, backend/handlers/room-entry/resolve.js, pages/index.js, pages/entry/[gameKey]/[roomNo].js, test-logic/hub-room-entry.test.js</files>
  <read_first>.planning/phases/11-availability-signals-degraded-modes/11-UAT.md, lib/shared/availability.js, backend/handlers/hub.js, backend/handlers/room-entry/resolve.js, pages/index.js, pages/entry/[gameKey]/[roomNo].js, test-logic/hub-room-entry.test.js</read_first>
  <action>Extend the shared degraded-mode contract so party voice safe actions can remain backend-authored but stop assuming every title should fall back to pure text. For `undercover`, add one honest game-aware guidance path that tells players voice is still part of the room contract and that only the active speaker should open mic when voice is available. Thread that contract through the hub card and `/entry/[gameKey]/[roomNo]` payload/UI so the vocabulary shown before entry matches what the dedicated room will actually allow. Do not break the existing family-scoped controls or the current werewolf/avalon degraded-room behavior.</action>
  <verify>node --test test-logic/hub-room-entry.test.js</verify>
  <acceptance_criteria>
    - shared availability helpers can express an Undercover-safe voice action without forcing `continue-text-only`
    - hub and room-entry payloads/UI keep using backend-authored degraded vocabulary but no longer misrepresent Undercover as text-only voice fallback
    - `node --test test-logic/hub-room-entry.test.js` exits 0
  </acceptance_criteria>
  <done>Players arriving from hub or invite entry now receive truthful, game-aware voice guidance for Undercover instead of a misleading generic family fallback.</done>
</task>

<task type="code">
  <name>Task 2: Add turn-scoped voice affordances to the dedicated Undercover room</name>
  <files>backend/handlers/party/rooms/[roomNo]/index.js, lib/party/manager.js, pages/undercover/[roomNo].js, styles/UndercoverRoom.module.css, test-logic/backend-contract.test.js, tests/undercover.spec.js</files>
  <read_first>backend/handlers/party/rooms/[roomNo]/index.js, lib/party/manager.js, pages/undercover/[roomNo].js, test-logic/backend-contract.test.js, tests/undercover.spec.js</read_first>
  <action>Reuse the existing party voice signaling baseline on the dedicated Undercover route. Serialize any room metadata the page needs to know whether the current viewer is the active clue speaker, then render a voice status panel and mic controls that behave honestly: the active clue speaker can connect or unmute when voice is healthy/degraded, other seats are told to wait/listen, and blocked voice still shuts the path off cleanly with the shared degraded guidance. Add backend and browser regression coverage proving the dedicated route no longer advertises voice support without a usable mic path.</action>
  <verify>node --test test-logic/backend-contract.test.js && FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/undercover.spec.js --workers=1</verify>
  <acceptance_criteria>
    - dedicated Undercover room renders explicit voice status instead of silently ignoring the backend voice contract
    - the active clue speaker can access the mic path when voice is available, while non-active seats get wait/listen guidance
    - blocked or degraded voice still uses the shared Phase 11 vocabulary instead of bespoke room-only wording
    - `node --test test-logic/backend-contract.test.js && FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/undercover.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>Undercover now behaves consistently with its own `voiceEnabled` contract and the Phase 11 degraded-mode surface.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Hub and entry payloads influence whether users trust a room is playable; misleading safe actions create false operational guidance.
- Undercover room state and active-speaker turn ownership cross the backend/frontend boundary; if the client guesses instead of receiving truthful data, mic affordances can drift from the actual round state.
- Party voice signaling is shared across titles, so game-specific guidance must not regress werewolf/avalon behavior while fixing Undercover.
</trust_boundaries>

<stride_register>
- threat_id: T-11-10
  category: Spoofing
  component: Undercover voice affordance
  disposition: mitigate
  mitigation_plan: derive active-speaker guidance from backend room state instead of a client-only guess, and keep blocked/degraded voice enforced by the shared room payload.
- threat_id: T-11-11
  category: Tampering
  component: degraded voice safe actions
  disposition: mitigate
  mitigation_plan: centralize the new game-aware safe-action semantics in `lib/shared/availability.js` so hub, entry, and room surfaces cannot fork their own Undercover wording.
- threat_id: T-11-12
  category: Availability
  component: dedicated Undercover room
  disposition: mitigate
  mitigation_plan: keep the dedicated route on the existing party voice signaling baseline and verify the canonical `3100/3101` stack still handles the new UI path without blocking gameplay.
</stride_register>
</threat_model>

<verification>
- [ ] `npm run deploy:3100`
- [ ] `node --test test-logic/hub-room-entry.test.js`
- [ ] `node --test test-logic/backend-contract.test.js`
- [ ] `FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/undercover.spec.js --workers=1`
- [ ] `npm run verify:release -- --skip-deploy`
- [ ] hub, entry, and dedicated Undercover room all describe the same truthful voice behavior
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No existing party-room voice behavior regresses for werewolf or avalon
- Undercover no longer advertises voice support while effectively forcing text-only interaction
</success_criteria>

<output>
After completion, create `.planning/phases/11-availability-signals-degraded-modes/11-04-SUMMARY.md`
</output>
