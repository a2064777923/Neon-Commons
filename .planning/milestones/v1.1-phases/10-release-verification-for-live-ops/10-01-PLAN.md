---
phase: 10-release-verification-for-live-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - test-logic/session-recovery.test.js
  - test-logic/room-directory-persistence.test.js
  - test-logic/room-expiry.test.js
  - test-logic/hub-room-entry.test.js
  - pages/index.js
  - pages/entry/[gameKey]/[roomNo].js
  - pages/admin/index.js
  - tests/hub-entry.spec.js
  - tests/admin-console.spec.js
autonomous: true
requirements:
  - RELY-03
user_setup: []
must_haves:
  truths:
    - `test:logic:critical` covers reconnect grace, snapshot reload, stale-room cleanup, and the Phase 8 room-ops backend path instead of only the older hub/admin/gameplay suites.
    - The hub, room-entry, and admin room-ops surfaces expose stable `data-*` hooks so release smoke can assert recovery and intervention states without depending on decorative copy.
    - The critical browser surface proves one recovery-aware entry path and one real room-ops operator path against the canonical `3100` runtime.
  artifacts:
    - package.json
    - test-logic/session-recovery.test.js
    - test-logic/room-directory-persistence.test.js
    - test-logic/room-expiry.test.js
    - test-logic/hub-room-entry.test.js
    - pages/index.js
    - pages/entry/[gameKey]/[roomNo].js
    - pages/admin/index.js
    - tests/hub-entry.spec.js
    - tests/admin-console.spec.js
  key_links:
    - package.json -> test-logic/session-recovery.test.js
    - package.json -> test-logic/room-directory-persistence.test.js
    - package.json -> test-logic/room-expiry.test.js
    - pages/index.js -> tests/hub-entry.spec.js
    - pages/entry/[gameKey]/[roomNo].js -> tests/hub-entry.spec.js
    - pages/admin/index.js -> tests/admin-console.spec.js
---

# Phase 10 Plan 01: Expand logic and UI critical suites for live-ops scenarios

<objective>
Broaden the existing release-facing suites so Neon-Commons can catch recovery and operator regressions before ship.

Purpose: Phase 6 created the canonical release commands, and Phase 9 created the recovery contract. This plan connects those two realities by widening the critical logic and browser suites around reconnect, snapshot-only discovery, stale-room cleanup, and the live room operations surface that Phase 8 adds.
Output: updated release-facing node/browser coverage, smoke-stable selectors on hub and entry surfaces, and admin room-ops smoke hooks that match the Phase 8 UI contract instead of inventing a parallel verification surface.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-release-verification-for-live-ops/10-RESEARCH.md
@.planning/phases/10-release-verification-for-live-ops/10-VALIDATION.md
@.planning/phases/10-release-verification-for-live-ops/10-UI-SPEC.md
@.planning/phases/09-single-node-recovery-guardrails/09-03-SUMMARY.md
@.planning/milestones/v1.0-phases/06-verification-release-hardening/06-01-SUMMARY.md
@package.json
@pages/index.js
@pages/entry/[gameKey]/[roomNo].js
@pages/admin/index.js
@test-logic/session-recovery.test.js
@test-logic/room-directory-persistence.test.js
@test-logic/room-expiry.test.js
@test-logic/hub-room-entry.test.js
@tests/hub-entry.spec.js
@tests/admin-console.spec.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Widen the critical node gate around recovery, expiry, and live-room-ops backend coverage</name>
  <files>package.json, test-logic/session-recovery.test.js, test-logic/room-directory-persistence.test.js, test-logic/room-expiry.test.js, test-logic/hub-room-entry.test.js</files>
  <read_first>.planning/phases/10-release-verification-for-live-ops/10-RESEARCH.md, .planning/phases/10-release-verification-for-live-ops/10-VALIDATION.md, package.json, test-logic/session-recovery.test.js, test-logic/room-directory-persistence.test.js, test-logic/room-expiry.test.js, test-logic/hub-room-entry.test.js, .planning/phases/09-single-node-recovery-guardrails/09-03-SUMMARY.md</read_first>
  <action>Update `package.json` so `test:logic:critical` explicitly includes `test-logic/session-recovery.test.js`, `test-logic/room-directory-persistence.test.js`, and `test-logic/room-expiry.test.js` in the same release-facing command that already runs backend/admin/hub/gameplay logic coverage. Keep every existing critical suite file from Phase 6 in the command; this plan widens the gate and must not narrow it. If Phase 8 ships its room-ops backend assertions under one dedicated node test file such as `test-logic/live-room-ops*.test.js` or `test-logic/admin-room-ops*.test.js`, add that exact file to `test:logic:critical` instead of creating a duplicate second suite. Extend the affected node tests only as needed so the widened gate proves the exact contracts release verification depends on: reconnect grace still restores the same seat, snapshot-only discovery keeps the exact `availability` vocabulary, and stale-room cleanup still clears both manager state and persisted directory state.</action>
  <verify>npm run test:logic:critical && npm run check</verify>
  <acceptance_criteria>
    - `package.json` `test:logic:critical` contains `test-logic/session-recovery.test.js`
    - `package.json` `test:logic:critical` contains `test-logic/room-directory-persistence.test.js`
    - `package.json` `test:logic:critical` contains `test-logic/room-expiry.test.js`
    - no previously listed Phase 6 critical node suite is removed from `test:logic:critical`
    - `npm run test:logic:critical && npm run check` exits 0
  </acceptance_criteria>
  <done>The canonical node gate now fails on reconnect, snapshot reload, stale-room cleanup, and room-ops backend regressions instead of only the older shipped surfaces.</done>
</task>

<task type="code">
  <name>Task 2: Add smoke-stable recovery and live-room-ops browser assertions to the critical UI surface</name>
  <files>pages/index.js, pages/entry/[gameKey]/[roomNo].js, pages/admin/index.js, tests/hub-entry.spec.js, tests/admin-console.spec.js</files>
  <read_first>.planning/phases/10-release-verification-for-live-ops/10-UI-SPEC.md, pages/index.js, pages/entry/[gameKey]/[roomNo].js, pages/admin/index.js, tests/hub-entry.spec.js, tests/admin-console.spec.js</read_first>
  <action>In `pages/index.js`, add stable selectors that match the approved UI contract: each live feed card must expose `data-live-feed-room=\"{roomNo}\"` and `data-room-availability=\"{live|snapshot-only|draining|closed}\"`, while snapshot-only supporting copy keeps the exact suffix `· 重啟恢復中`. In `pages/entry/[gameKey]/[roomNo].js`, add `data-room-availability`, `data-entry-action=\"guest|login\"`, and `data-entry-notice=\"snapshot-only|recovery|guest-recovery\"` on the existing blocked-entry and auto-return states without changing the current utility layout or the exact blocked labels `房間恢復中` / `等待房間恢復`. In `pages/admin/index.js`, extend the Phase 8 live-room-ops markup so the directory/detail/actions expose `data-live-room-directory=\"true\"`, `data-live-room-row`, `data-live-room-detail`, `data-room-action`, `data-room-action-confirm`, `data-room-occupant`, and `data-room-occupant-action=\"remove\"` while preserving the existing `data-admin-family`, `data-game-key`, and `data-audit-row` hooks. Then extend `tests/hub-entry.spec.js` to assert recovery-aware hub/entry rendering with deterministic fixtures or route interception against `/api/hub`, `/api/room-entry/resolve`, and `/api/me`, and extend `tests/admin-console.spec.js` so it drives the Phase 8 live-room directory inspect/drain/close/remove workflows and verifies the resulting audit row and room availability states through the new hooks.</action>
  <verify>FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/admin-console.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `pages/index.js` contains `data-live-feed-room`
    - `pages/index.js` or its rendered live feed markup contains `data-room-availability`
    - `pages/entry/[gameKey]/[roomNo].js` contains `data-entry-action`
    - `pages/entry/[gameKey]/[roomNo].js` contains `data-entry-notice`
    - `pages/admin/index.js` contains `data-live-room-directory`, `data-room-action`, and `data-room-occupant-action=\"remove\"`
    - `FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/admin-console.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>The release-facing browser smoke now covers one recovery-aware entry path and one real operator intervention path through stable hooks instead of brittle copy-only checks.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- `package.json` script definitions are the operator-facing release boundary; missing suites here silently hide regressions from `verify:release`.
- Browser smoke depends on DOM hooks and exact API payload categories to decide whether recovery and operator flows are behaving correctly.
- The admin room-ops surface extends the brownfield console, so selector drift between Phase 8 UI and Phase 10 smoke would create false release signals.
</trust_boundaries>

<stride_register>
- threat_id: T-10-01
  category: Repudiation
  component: critical node suite coverage
  disposition: mitigate
  mitigation_plan: wire recovery, persistence, expiry, and room-ops backend tests into `test:logic:critical` so the canonical gate matches the actual reliability surface.
- threat_id: T-10-02
  category: Tampering
  component: browser smoke selectors
  disposition: mitigate
  mitigation_plan: assert release-critical UI state through stable `data-*` hooks instead of decorative headings or mutable marketing copy.
- threat_id: T-10-03
  category: Availability
  component: admin room-ops smoke
  disposition: mitigate
  mitigation_plan: drive the Phase 8 live-room directory and actions through the same admin page and audit feed operators use, not a detached test-only surface.
</stride_register>
</threat_model>

<verification>
- [ ] `npm run test:logic:critical`
- [ ] `npm run check`
- [ ] `FRONTEND_BASE_URL=http://127.0.0.1:3100 npx playwright test tests/hub-entry.spec.js tests/admin-console.spec.js --workers=1`
- [ ] hub, entry, and admin release assertions use stable `data-*` hooks for recovery and room-ops state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The critical logic and browser suites now cover Phase 7/8/9 live-ops and recovery risks instead of only the earlier brownfield baseline
</success_criteria>

<output>
After completion, create `.planning/phases/10-release-verification-for-live-ops/10-01-SUMMARY.md`
</output>
