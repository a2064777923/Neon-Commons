---
phase: 09-single-node-recovery-guardrails
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - lib/rooms/directory.js
  - backend/handlers/hub.js
  - backend/handlers/room-entry/resolve.js
  - backend/handlers/room-entry/shareable.js
  - backend/handlers/room-entry/guest.js
  - backend/handlers/rooms/[roomNo]/index.js
  - backend/handlers/party/rooms/[roomNo]/index.js
  - backend/handlers/board/rooms/[roomNo]/index.js
  - pages/index.js
  - pages/entry/[gameKey]/[roomNo].js
  - test-logic/hub-room-entry.test.js
autonomous: true
requirements:
  - RELY-01
  - RELY-02
user_setup: []
must_haves:
  truths:
    - Hub, room-entry, and direct room-detail flows distinguish `live` from `snapshot-only` entries instead of advertising dead rooms as playable matches.
    - Public discovery can remain informative after restart without issuing guest sessions or auto-entry into rooms the managers cannot actually serve.
    - Compatibility coverage proves the brownfield hub, room-number join, and guest-claim flows still fail closed or degrade clearly around restored and expired rooms.
  artifacts:
    - backend/handlers/hub.js
    - backend/handlers/room-entry/resolve.js
    - backend/handlers/room-entry/shareable.js
    - backend/handlers/room-entry/guest.js
    - backend/handlers/rooms/[roomNo]/index.js
    - backend/handlers/party/rooms/[roomNo]/index.js
    - backend/handlers/board/rooms/[roomNo]/index.js
    - pages/index.js
    - pages/entry/[gameKey]/[roomNo].js
    - test-logic/hub-room-entry.test.js
  key_links:
    - lib/rooms/directory.js -> backend/handlers/hub.js
    - lib/rooms/directory.js -> backend/handlers/room-entry/resolve.js
    - backend/handlers/room-entry/resolve.js -> pages/entry/[gameKey]/[roomNo].js
    - backend/handlers/hub.js -> pages/index.js
    - backend/handlers/rooms/[roomNo]/index.js -> test-logic/hub-room-entry.test.js
---

# Phase 9 Plan 03: Validate compatibility against shipped room families and discovery flows

<objective>
Make restart-recovered discovery honest and compatible with shipped browser entry paths.

Purpose: Snapshot persistence and stale cleanup are not enough by themselves. Phase 9 must also ensure the hub, room-number join, guest entry, and direct detail routes understand the difference between a live room and a snapshot-only recovery stub.
Output: additive availability semantics across hub, room-entry, and room-detail handlers, plus compatibility coverage for restored and expired room flows.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-single-node-recovery-guardrails/09-RESEARCH.md
@.planning/phases/09-single-node-recovery-guardrails/09-VALIDATION.md
@.planning/phases/09-single-node-recovery-guardrails/09-01-PLAN.md
@.planning/phases/09-single-node-recovery-guardrails/09-02-PLAN.md
@lib/rooms/directory.js
@backend/handlers/hub.js
@backend/handlers/room-entry/resolve.js
@backend/handlers/room-entry/shareable.js
@backend/handlers/room-entry/guest.js
@backend/handlers/rooms/[roomNo]/index.js
@backend/handlers/party/rooms/[roomNo]/index.js
@backend/handlers/board/rooms/[roomNo]/index.js
@pages/index.js
@pages/entry/[gameKey]/[roomNo].js
@test-logic/hub-room-entry.test.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Expose additive availability semantics across hub and room-entry discovery</name>
  <files>lib/rooms/directory.js, backend/handlers/hub.js, backend/handlers/room-entry/resolve.js, backend/handlers/room-entry/shareable.js, backend/handlers/room-entry/guest.js, pages/index.js, pages/entry/[gameKey]/[roomNo].js</files>
  <read_first>.planning/phases/09-single-node-recovery-guardrails/09-RESEARCH.md, lib/rooms/directory.js, backend/handlers/hub.js, backend/handlers/room-entry/resolve.js, backend/handlers/room-entry/shareable.js, backend/handlers/room-entry/guest.js, pages/index.js, pages/entry/[gameKey]/[roomNo].js</read_first>
  <action>Teach the shared room directory and the affected handlers to surface an additive availability field with exact values `live` and `snapshot-only`. `backend/handlers/hub.js` should stop relying only on manager-local room lists for its recovery-aware discovery payloads and should include snapshot-backed entries in a way the browser can render honestly. `backend/handlers/room-entry/resolve.js` and `backend/handlers/room-entry/shareable.js` must return the same availability vocabulary. `backend/handlers/room-entry/guest.js` must reject `snapshot-only` rooms with a clear 409-style recovery error instead of issuing a guest token into a non-live room. Update `pages/index.js` and `pages/entry/[gameKey]/[roomNo].js` only as much as needed so snapshot-only discovery remains visible but non-destructive: no auto-enter, no broken live-room CTA, and no duplicate fake recovery flow.</action>
  <verify>node --test test-logic/hub-room-entry.test.js test-logic/room-directory-persistence.test.js</verify>
  <acceptance_criteria>
    - hub and room-entry payloads expose `availability` with exact values `live` or `snapshot-only`
    - `backend/handlers/room-entry/guest.js` rejects `snapshot-only` entries before guest token issuance
    - `pages/entry/[gameKey]/[roomNo].js` does not auto-enter a room when `availability !== "live"`
    - `node --test test-logic/hub-room-entry.test.js test-logic/room-directory-persistence.test.js` exits 0
  </acceptance_criteria>
  <done>Restart-recovered discovery is now honest: players can see recovered rooms, but the browser no longer treats snapshot-only entries as fully playable live rooms.</done>
</task>

<task type="code">
  <name>Task 2: Align direct room-detail handlers with snapshot-only recovery rules and lock the compatibility surface with tests</name>
  <files>backend/handlers/rooms/[roomNo]/index.js, backend/handlers/party/rooms/[roomNo]/index.js, backend/handlers/board/rooms/[roomNo]/index.js, lib/rooms/directory.js, test-logic/hub-room-entry.test.js</files>
  <read_first>backend/handlers/rooms/[roomNo]/index.js, backend/handlers/party/rooms/[roomNo]/index.js, backend/handlers/board/rooms/[roomNo]/index.js, lib/rooms/directory.js, test-logic/hub-room-entry.test.js</read_first>
  <action>When a direct room detail request hits a missing live manager room but the shared directory still has a `snapshot-only` entry for that `roomNo`, return one explicit recovery-aware response across card, party, and board handlers instead of a generic not-found. The response must include the exact fields `error`, `availability`, `roomNo`, and `gameKey`, with `availability: "snapshot-only"`, and a clear message that the room is recovering from a single-node restart rather than actively live. Keep true missing rooms as normal 404s. Extend `test-logic/hub-room-entry.test.js` so the brownfield compatibility surface proves: recovered discovery uses one availability vocabulary, guest issuance fails closed for snapshot-only rooms, and direct room detail requests degrade consistently across all three room families.</action>
  <verify>npm run check && node --test test-logic/hub-room-entry.test.js test-logic/room-directory-persistence.test.js test-logic/room-expiry.test.js test-logic/session-recovery.test.js</verify>
  <acceptance_criteria>
    - card, party, and board detail handlers all return the same `availability: "snapshot-only"` contract when only a restored directory entry exists
    - truly missing rooms still return a normal 404 rather than the recovery-aware response
    - compatibility tests cover hub discovery, room-entry resolve/shareable, guest rejection, and direct room-detail fallback behavior
    - `npm run check && node --test test-logic/hub-room-entry.test.js test-logic/room-directory-persistence.test.js test-logic/room-expiry.test.js test-logic/session-recovery.test.js` exits 0
  </acceptance_criteria>
  <done>The shipped discovery and entry surface now handles restored and expired rooms predictably instead of drifting into dead deep links or fake live-room semantics.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Browser discovery and entry pages trust backend room metadata to decide whether to auto-enter, copy invites, or issue guest access.
- Snapshot-only entries cross a trust boundary from restart recovery metadata into player-facing flows; mislabeling them as live would create broken navigation and privilege mistakes.
- Direct room-detail handlers are separate per family, so compatibility drift would create inconsistent recovery semantics across shipped games.
</trust_boundaries>

<stride_register>
- threat_id: T-09-07
  category: Spoofing
  component: snapshot-only discovery entries
  disposition: mitigate
  mitigation_plan: expose one additive `availability` field and block auto-entry or guest issuance unless it equals `live`.
- threat_id: T-09-08
  category: Elevation of Privilege
  component: guest entry into recovered rooms
  disposition: mitigate
  mitigation_plan: reject guest issuance for snapshot-only entries before session token creation.
- threat_id: T-09-09
  category: Tampering
  component: per-family direct room-detail fallback behavior
  disposition: mitigate
  mitigation_plan: standardize the snapshot-only fallback contract across card, party, and board handlers and lock it in shared node tests.
</stride_register>
</threat_model>

<verification>
- [ ] `node --test test-logic/hub-room-entry.test.js test-logic/room-directory-persistence.test.js`
- [ ] `npm run check && node --test test-logic/hub-room-entry.test.js test-logic/room-directory-persistence.test.js test-logic/room-expiry.test.js test-logic/session-recovery.test.js`
- [ ] hub, room-entry, guest, and direct room-detail flows share one `live` / `snapshot-only` availability vocabulary
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 9 discovery and entry flows remain compatible with shipped brownfield navigation while restart-recovered rooms are labeled honestly
</success_criteria>

<output>
After completion, create `.planning/phases/09-single-node-recovery-guardrails/09-03-SUMMARY.md`
</output>
