---
phase: 07-session-recovery-presence
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - lib/socket-server.js
  - lib/rooms/directory.js
  - lib/game/room-manager.js
  - lib/party/manager.js
  - lib/board/manager.js
  - test-logic/hub-room-entry.test.js
  - test-logic/session-recovery.test.js
  - test-logic/ddz-logic.test.js
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03
user_setup: []
must_haves:
  truths:
    - Human seats move through a bounded `reconnecting` grace window instead of dropping straight from `connected` to permanent absence.
    - Re-subscribing with the same valid session restores the existing seat rather than creating duplicates or widening guest scope.
    - Dou Dizhu trustee behavior and other existing room-state rules remain intact while presence becomes richer and more explicit.
  artifacts:
    - lib/socket-server.js
    - lib/game/room-manager.js
    - lib/party/manager.js
    - lib/board/manager.js
    - test-logic/session-recovery.test.js
    - test-logic/hub-room-entry.test.js
  key_links:
    - lib/socket-server.js -> lib/game/room-manager.js
    - lib/socket-server.js -> lib/party/manager.js
    - lib/socket-server.js -> lib/board/manager.js
    - lib/rooms/directory.js -> lib/socket-server.js
    - lib/game/room-manager.js -> test-logic/ddz-logic.test.js
    - lib/party/manager.js -> test-logic/session-recovery.test.js
    - lib/board/manager.js -> test-logic/session-recovery.test.js
---

# Phase 7 Plan 02: Implement the reconnect grace state machine across shipped room families

<objective>
Turn the Phase 7 contract into real runtime behavior inside the room managers and socket layer.

Purpose: after Plan 01 defines the payload shape, this plan must make reconnect and refresh continuity deterministic across card, party, board, Undercover, and Reversi flows without widening guest privileges or breaking existing match behavior.
Output: manager-level reconnect grace handling, room serialization that reflects live transitions, and focused node coverage for recovery edge cases.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-session-recovery-presence/07-CONTEXT.md
@.planning/phases/07-session-recovery-presence/07-RESEARCH.md
@.planning/phases/07-session-recovery-presence/07-VALIDATION.md
@.planning/phases/07-session-recovery-presence/07-01-PLAN.md
@.planning/milestones/v1.0-phases/03-hub-room-expansion-framework/03-02-SUMMARY.md
@lib/socket-server.js
@lib/rooms/directory.js
@lib/game/room-manager.js
@lib/party/manager.js
@lib/board/manager.js
@test-logic/hub-room-entry.test.js
@test-logic/ddz-logic.test.js
</context>

<tasks>
<task type="code">
  <name>Task 1: Add reconnect grace tracking and presence transitions to all room managers</name>
  <files>lib/game/room-manager.js, lib/party/manager.js, lib/board/manager.js, lib/rooms/directory.js, test-logic/session-recovery.test.js</files>
  <read_first>.planning/phases/07-session-recovery-presence/07-CONTEXT.md, .planning/phases/07-session-recovery-presence/07-RESEARCH.md, lib/game/room-manager.js, lib/party/manager.js, lib/board/manager.js, lib/rooms/directory.js, .planning/milestones/v1.0-phases/03-hub-room-expansion-framework/03-02-SUMMARY.md</read_first>
  <action>Implement a shared reconnect grace state machine for human seats. When the last socket for a human player drops, transition the seat to `reconnecting` for a bounded grace window instead of flipping directly to permanent absence; after the grace window expires, move to `disconnected`. When the same valid session returns, reuse the existing seat, clear the grace window, and restore `connected` without duplicating membership. Bots must remain permanently connected. Extend any shared room-directory metadata only as needed to keep room summaries and updated timestamps coherent; do not turn this plan into cross-restart persistence. Create focused `test-logic/session-recovery.test.js` coverage for the manager-level transition rules rather than overloading every assertion into an existing mega-test file.</action>
  <verify>node --test test-logic/session-recovery.test.js</verify>
  <acceptance_criteria>
    - all shipped room managers expose the exact `connected`, `reconnecting`, and `disconnected` states for human seats
    - reconnecting a valid seat clears the grace state and does not create duplicate players or duplicate room-directory membership
    - bots never enter the reconnect grace state machine
    - `node --test test-logic/session-recovery.test.js` exits 0
  </acceptance_criteria>
  <done>The runtime has one bounded reconnect lifecycle across the shipped room families instead of ad hoc binary socket status.</done>
</task>

<task type="code">
  <name>Task 2: Wire socket lifecycle and guest scope enforcement into the new recovery state machine</name>
  <files>lib/socket-server.js, lib/game/room-manager.js, lib/party/manager.js, lib/board/manager.js, test-logic/hub-room-entry.test.js, test-logic/session-recovery.test.js, test-logic/ddz-logic.test.js</files>
  <read_first>lib/socket-server.js, lib/game/room-manager.js, lib/party/manager.js, lib/board/manager.js, test-logic/hub-room-entry.test.js, test-logic/ddz-logic.test.js</read_first>
  <action>Update Socket.IO registration and disconnect behavior so the shared recovery state machine is actually exercised in production flows. Reconnects should re-subscribe the same seated user or scoped guest cleanly, while cross-room or cross-family guest attempts still fail with the shipped scope guarantees. Keep Dou Dizhu's timeout/trustee behavior intact: a disconnected player may still enter trustee according to existing rules, but the host and client payloads should still be able to distinguish "temporarily reconnecting" from "fully disconnected." Add node coverage for wrong-room guest reconnect attempts, reconnect-success paths, and the card-room trustee edge case.</action>
  <verify>node --test test-logic/session-recovery.test.js test-logic/hub-room-entry.test.js test-logic/ddz-logic.test.js</verify>
  <acceptance_criteria>
    - reconnecting with the same valid session restores the existing seat and socket subscription path across card, party, and board families
    - wrong-room or wrong-family guest reconnect attempts still fail with the scoped guest error behavior
    - Dou Dizhu disconnects preserve existing trustee semantics while still exposing richer presence metadata
    - `node --test test-logic/session-recovery.test.js test-logic/hub-room-entry.test.js test-logic/ddz-logic.test.js` exits 0
  </acceptance_criteria>
  <done>The live socket flow now matches the Phase 7 recovery contract and preserves existing match rules while adding reconnect awareness.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Socket.IO is the live continuity boundary between browser presence and room-manager seat state.
- Guest sessions are scoped to one room/game and must not become a generic reconnect credential.
- Dou Dizhu timeout/trustee logic is gameplay-affecting and can regress if presence transitions are implemented carelessly.
</trust_boundaries>

<stride_register>
- threat_id: T-07-04
  category: Denial of Service
  component: disconnect handling
  disposition: mitigate
  mitigation_plan: introduce a bounded reconnect grace window so transient refreshes do not behave like permanent departure.
- threat_id: T-07-05
  category: Tampering
  component: seat restoration
  disposition: mitigate
  mitigation_plan: reuse the existing seat on reconnect and cover duplicate-seat regressions in focused node tests.
- threat_id: T-07-06
  category: Elevation of Privilege
  component: scoped guest reconnect
  disposition: mitigate
  mitigation_plan: keep socket scope checks strict and re-test wrong-room guest reconnect attempts.
- threat_id: T-07-07
  category: Availability
  component: Dou Dizhu disconnect behavior
  disposition: mitigate
  mitigation_plan: keep trustee behavior intact and cover the recovery/trustee interplay explicitly in node tests.
</stride_register>
</threat_model>

<verification>
- [ ] `node --test test-logic/session-recovery.test.js`
- [ ] `node --test test-logic/session-recovery.test.js test-logic/hub-room-entry.test.js test-logic/ddz-logic.test.js`
- [ ] reconnecting sessions restore seats without widening guest privileges
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 runtime behavior exposes a bounded reconnect lifecycle across the shipped room families
</success_criteria>

<output>
After completion, create `.planning/phases/07-session-recovery-presence/07-02-SUMMARY.md`
</output>
