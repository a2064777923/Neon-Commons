---
phase: 04-card-party-gameplay-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/defaults.js
  - lib/game/template-settings.js
  - lib/game/room-manager.js
  - backend/handlers/templates.js
  - backend/handlers/admin/templates/index.js
  - backend/handlers/rooms/index.js
  - pages/lobby.js
  - pages/room/[roomNo].js
  - pages/admin/index.js
  - test-logic/ddz-logic.test.js
  - test-logic/backend-contract.test.js
  - tests/room-ui.spec.js
  - tests/admin-console.spec.js
autonomous: true
requirements:
  - CARD-01
user_setup: []
must_haves:
  truths:
    - Dou Dizhu templates and room overrides can express the full supported rule surface through one shared backend contract instead of partially normalized JSON.
    - Unsupported `LAIZI` activation remains blocked until the deck/combo/runtime actually supports it.
    - Lobby, room, and admin surfaces describe the same live rule set that the room manager enforces.
  artifacts:
    - lib/game/template-settings.js
    - lib/game/room-manager.js
    - pages/lobby.js
    - pages/room/[roomNo].js
    - tests/room-ui.spec.js
  key_links:
    - lib/game/template-settings.js -> backend/handlers/admin/templates/index.js
    - lib/game/template-settings.js -> backend/handlers/rooms/index.js
    - lib/game/template-settings.js -> lib/game/room-manager.js
    - pages/lobby.js -> pages/room/[roomNo].js
    - pages/admin/index.js -> tests/admin-console.spec.js
---

<objective>
Extend Dou Dizhu through the existing template-backed room model by making the seeded rule surface real, validated, and visible.

Purpose: `CARD-01` is not satisfied by more template rows alone. The template, admin, room-create, and live room paths must all agree on which rules are supported and what they do.
Output: A shared Dou Dizhu template settings contract, runtime rule enforcement for supported fields, guarded handling for unsupported modes, and card-room/admin UI coverage that proves the richer rule surface is usable.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-card-party-gameplay-expansion/04-RESEARCH.md
@.planning/phases/04-card-party-gameplay-expansion/04-VALIDATION.md
@lib/defaults.js
@lib/game/cards.js
@lib/game/room-manager.js
@backend/handlers/templates.js
@backend/handlers/admin/templates/index.js
@backend/handlers/rooms/index.js
@pages/lobby.js
@pages/room/[roomNo].js
@pages/admin/index.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create one supported Dou Dizhu template schema and enforce it in live room runtime</name>
  <files>lib/defaults.js, lib/game/template-settings.js, lib/game/room-manager.js, backend/handlers/templates.js, backend/handlers/admin/templates/index.js, backend/handlers/rooms/index.js, test-logic/ddz-logic.test.js, test-logic/backend-contract.test.js</files>
  <read_first>.planning/phases/04-card-party-gameplay-expansion/04-RESEARCH.md, lib/defaults.js, lib/game/cards.js, lib/game/room-manager.js, backend/handlers/admin/templates/index.js, backend/handlers/rooms/index.js, test-logic/ddz-logic.test.js</read_first>
  <action>Create `lib/game/template-settings.js` as the canonical validator/normalizer for supported Dou Dizhu template settings. Keep supported live modes limited to `CLASSIC`, `ROB`, and `NO_SHUFFLE`; treat `LAIZI` as explicitly unsupported for room creation/admin activation in this phase. Normalize and validate `baseScore`, `bidOptions`, `countdownSeconds`, `autoTrusteeMinSeconds`, `autoTrusteeMaxSeconds`, `allowSpring`, `allowBomb`, `allowRocket`, `allowBots`, `maxRobMultiplier`, `bombMultiplier`, `rocketMultiplier`, `springMultiplier`, and `roomVisibility`. Use the same normalizer in `lib/defaults.js`, `backend/handlers/templates.js`, `backend/handlers/admin/templates/index.js`, `backend/handlers/rooms/index.js`, and `lib/game/room-manager.js` so template storage, room creation, and room serialization all agree. Update the room manager to honor supported rule flags in actual gameplay: bomb/rocket plays must reject when disabled, spring doubling must skip when disabled, and bidding/auto-bidding must respect configured bid ceilings instead of the current hard-coded max `3`. Extend `test-logic/ddz-logic.test.js` for rule gating and bid-ceiling behavior, and extend `test-logic/backend-contract.test.js` to assert unsupported template modes/settings do not silently pass through backend contracts.</action>
  <verify>node --test test-logic/ddz-logic.test.js test-logic/backend-contract.test.js</verify>
  <acceptance_criteria>
    - `lib/game/template-settings.js` exists and is used by both template handlers and card-room creation/runtime code
    - supported live modes remain `CLASSIC`, `ROB`, and `NO_SHUFFLE`
    - unsupported `LAIZI` activation is rejected instead of silently reaching live room state
    - `allowBomb`, `allowRocket`, `allowSpring`, and bid-ceiling settings affect real gameplay behavior rather than only stored JSON
    - `node --test test-logic/ddz-logic.test.js test-logic/backend-contract.test.js` exits 0
  </acceptance_criteria>
  <done>Dou Dizhu templates now have one enforced rule contract from admin write path through live room runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the expanded Dou Dizhu rules in lobby, room, and admin workflows</name>
  <files>pages/lobby.js, pages/room/[roomNo].js, pages/admin/index.js, tests/room-ui.spec.js, tests/admin-console.spec.js</files>
  <read_first>pages/lobby.js, pages/room/[roomNo].js, pages/admin/index.js, .planning/phases/04-card-party-gameplay-expansion/04-RESEARCH.md, .planning/phases/04-card-party-gameplay-expansion/04-VALIDATION.md</read_first>
  <action>Update `pages/lobby.js` so players can see and adjust the supported non-default Dou Dizhu rule surface before opening a room: bid ceiling, bomb/rocket/spring toggles, multiplier values, trustee window, and visibility should all reflect the selected template and stay bounded by the shared validator. Update `pages/room/[roomNo].js` to render the active rule pack in-room through visible chips, setup summaries, or equivalent room-safe UI so players can immediately tell which special rules are active or disabled. In `pages/admin/index.js`, keep the existing advanced editor workflow but add clear preview/help for supported template settings and unsupported `LAIZI` behavior so operators are not editing blind JSON. Extend `tests/room-ui.spec.js` and `tests/admin-console.spec.js` to create or mutate a non-default ruleset and prove the lobby/admin view matches the live room rule summary.</action>
  <verify>npm run check && npx playwright test tests/room-ui.spec.js tests/admin-console.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `pages/lobby.js` exposes the supported non-default Dou Dizhu rules instead of only base score / countdown / trustee / visibility
    - `pages/room/[roomNo].js` shows the active room rules without requiring users to infer them from gameplay
    - `pages/admin/index.js` gives operators clear feedback about supported template fields and unsupported `LAIZI`
    - `tests/room-ui.spec.js` and `tests/admin-console.spec.js` assert non-default rule visibility
    - `npm run check && npx playwright test tests/room-ui.spec.js tests/admin-console.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>The richer Dou Dizhu rule contract is visible and understandable across player and operator entry points.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Admin-authored template JSON crosses from backend persistence into public lobby selection and live match rules.
- Card-room create requests can override template values, so unsupported or inconsistent settings can bypass operator intent if not normalized centrally.
- Live room UI now explains more rules, so mismatches between backend behavior and displayed rules become a player-trust risk.
</trust_boundaries>

<stride_register>
- threat_id: T-04-01
  category: Tampering
  component: template settings contract
  disposition: mitigate
  mitigation_plan: centralize Dou Dizhu settings normalization/validation, reject unsupported mode activation, and cover the admin/create/runtime path with node tests.
- threat_id: T-04-02
  category: Denial of Service
  component: live card-room runtime
  disposition: mitigate
  mitigation_plan: clamp bid/rule values before room creation and ensure unsupported special-rule combinations fail closed rather than reaching match execution.
- threat_id: T-04-03
  category: Spoofing
  component: lobby / room / admin rule presentation
  disposition: mitigate
  mitigation_plan: derive displayed rule summaries from normalized room/template state and prove alignment through Playwright coverage.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/ddz-logic.test.js test-logic/backend-contract.test.js`
- [ ] `npm run check`
- [ ] `npx playwright test tests/room-ui.spec.js tests/admin-console.spec.js --workers=1`
- [ ] non-default Dou Dizhu room rules are both enforced and visibly explained
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dou Dizhu can ship richer supported templates/rules through the existing room framework without pretending unsupported modes are live
</success_criteria>

<output>
After completion, create `.planning/phases/04-card-party-gameplay-expansion/04-01-SUMMARY.md`
</output>
