---
phase: 04-card-party-gameplay-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/games/catalog.js
  - lib/party/manager.js
  - pages/games/[gameKey].js
  - pages/party/[roomNo].js
  - styles/GameLobby.module.css
  - styles/PartyRoom.module.css
  - test-logic/party-config.test.js
  - test-logic/undercover-logic.test.js
  - tests/arcade-party.spec.js
  - tests/undercover.spec.js
autonomous: true
requirements:
  - PARTY-01
user_setup: []
must_haves:
  truths:
    - Werewolf and Avalon room owners can choose richer role/phase options through the existing `/games/{gameKey}` create-room flow instead of relying on hard-coded player-count tables alone.
    - Current shipped party behavior remains the default or an explicitly available preset so existing Werewolf/Avalon rooms do not regress.
    - Shared party-manager changes do not break the already shipped Undercover flow.
  artifacts:
    - lib/party/manager.js
    - pages/games/[gameKey].js
    - pages/party/[roomNo].js
    - test-logic/party-config.test.js
    - tests/arcade-party.spec.js
  key_links:
    - lib/games/catalog.js -> pages/games/[gameKey].js
    - lib/games/catalog.js -> lib/party/manager.js
    - lib/party/manager.js -> pages/party/[roomNo].js
    - lib/party/manager.js -> test-logic/party-config.test.js
    - pages/party/[roomNo].js -> tests/arcade-party.spec.js
---

<objective>
Extend Werewolf and Avalon through room-configurable role/phase options while preserving the existing live party-room model and shared manager.

Purpose: `PARTY-01` is already partly implemented in code, but room owners cannot actually choose those richer capabilities through the shipped lobby flow. Phase 4 needs configuration, serialization, and UI visibility, not a second party runtime.
Output: A config-aware Werewolf/Avalon role-pack contract, create-room UI for those options, party-room setup summaries, and regression-safe coverage including Undercover.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-card-party-gameplay-expansion/04-RESEARCH.md
@.planning/phases/04-card-party-gameplay-expansion/04-VALIDATION.md
@lib/games/catalog.js
@lib/party/manager.js
@pages/games/[gameKey].js
@pages/party/[roomNo].js
@tests/arcade-party.spec.js
@tests/undercover.spec.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add config-aware Werewolf/Avalon role presets and phase options to the party manager</name>
  <files>lib/games/catalog.js, lib/party/manager.js, test-logic/party-config.test.js, test-logic/undercover-logic.test.js</files>
  <read_first>.planning/phases/04-card-party-gameplay-expansion/04-RESEARCH.md, lib/games/catalog.js, lib/party/manager.js, test-logic/undercover-logic.test.js</read_first>
  <action>Extend `lib/games/catalog.js` and `lib/party/manager.js` so Werewolf and Avalon room config can carry curated role presets plus the missing runtime options already implied by current code. Add a `rolePack`-style field per game instead of free-form role toggles. Keep the current richer shipped behavior as the default preset so existing rooms remain compatible, and add at least one simpler preset that reduces special-role complexity. For Werewolf, make `hunterSeconds` and `voiceEnabled` part of the supported config contract. For Avalon, make `voiceEnabled` and the selected role pack part of the contract. Update role-assignment helpers so the final role list depends on `gameKey + playerCount + rolePack` instead of only `playerCount`. Add `test-logic/party-config.test.js` coverage for config normalization, role-pack clamping by player count, enabled-role serialization, and default compatibility. Keep `test-logic/undercover-logic.test.js` green as a shared-manager regression guard.</action>
  <verify>node --test test-logic/party-config.test.js test-logic/undercover-logic.test.js</verify>
  <acceptance_criteria>
    - `lib/party/manager.js` derives Werewolf/Avalon roles from normalized room config rather than only fixed player-count tables
    - the shipped richer Werewolf/Avalon behavior remains available as the default or an explicitly supported preset
    - Werewolf supports `hunterSeconds` and party voice mode as room-configured options
    - `test-logic/party-config.test.js` exists and covers role-pack normalization / distribution
    - `node --test test-logic/party-config.test.js test-logic/undercover-logic.test.js` exits 0
  </acceptance_criteria>
  <done>Werewolf and Avalon now have a real room-level config contract for richer role and phase behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Expose party role presets in the lobby and room UI without breaking existing party flow</name>
  <files>pages/games/[gameKey].js, pages/party/[roomNo].js, styles/GameLobby.module.css, styles/PartyRoom.module.css, tests/arcade-party.spec.js, tests/undercover.spec.js</files>
  <read_first>pages/games/[gameKey].js, pages/party/[roomNo].js, styles/GameLobby.module.css, styles/PartyRoom.module.css, lib/games/catalog.js, lib/party/manager.js</read_first>
  <action>Update `pages/games/[gameKey].js` so Werewolf and Avalon creators can choose the supported role preset and any newly supported room options instead of only timers and player count. Explicitly expose Werewolf `hunterSeconds` and party `voiceEnabled` so the create-room surface matches the runtime contract. Update `pages/party/[roomNo].js` to show the selected role pack, enabled special roles, and room voice mode in a visible setup summary without replacing the existing Werewolf/Avalon phase boards. Keep `/party/{roomNo}` routing and live room actions unchanged. Extend `tests/arcade-party.spec.js` to create at least one non-default preset room and assert the selected setup appears in-room, and keep `tests/undercover.spec.js` green so shared party-manager/UI work does not regress Undercover.</action>
  <verify>npm run check && npx playwright test tests/arcade-party.spec.js tests/undercover.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `pages/games/[gameKey].js` exposes a Werewolf/Avalon role preset selector
    - `pages/games/[gameKey].js` exposes Werewolf `hunterSeconds` and party voice mode where supported
    - `pages/party/[roomNo].js` shows the selected role pack or enabled roles in the room setup summary
    - `tests/arcade-party.spec.js` asserts non-default party config visibility
    - `npm run check && npx playwright test tests/arcade-party.spec.js tests/undercover.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>Players can now configure and understand richer Werewolf/Avalon rooms through the shipped party-room entry flow.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Party-room config now influences secret role assignment, phase timing, and voice behavior inside a shared live room manager.
- The same party manager also powers Undercover, so Werewolf/Avalon refactors can regress an unrelated shipped title.
- The create-room UI and live room UI must describe the same chosen role preset, or players will enter rooms with the wrong expectation.
</trust_boundaries>

<stride_register>
- threat_id: T-04-04
  category: Tampering
  component: party config normalization
  disposition: mitigate
  mitigation_plan: clamp role presets by game and player count, keep presets curated, and cover normalization/distribution in `test-logic/party-config.test.js`.
- threat_id: T-04-05
  category: Information Disclosure
  component: shared party manager
  disposition: mitigate
  mitigation_plan: keep Undercover as a regression check whenever party-manager state shaping changes, and avoid widening secret-role serialization accidentally.
- threat_id: T-04-06
  category: Spoofing
  component: party lobby / room setup summary
  disposition: mitigate
  mitigation_plan: derive displayed preset and enabled-role badges from normalized room config, then assert them in party Playwright coverage.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/party-config.test.js test-logic/undercover-logic.test.js`
- [ ] `npm run check`
- [ ] `npx playwright test tests/arcade-party.spec.js tests/undercover.spec.js --workers=1`
- [ ] Werewolf and Avalon room creators can choose richer options without breaking existing party-room routes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Werewolf and Avalon can ship richer room-level role/phase options through the current party-room model without regressing Undercover
</success_criteria>

<output>
After completion, create `.planning/phases/04-card-party-gameplay-expansion/04-02-SUMMARY.md`
</output>
