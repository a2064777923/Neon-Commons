---
phase: 03.1-new-game-delivery-wave-1
plan: 02
type: execute
wave: 2
depends_on:
  - "03.1-01"
files_modified:
  - lib/solo/sokoban.js
  - components/solo/SokobanBoard.js
  - pages/games/sokoban.js
  - styles/Sokoban.module.css
  - test-logic/sokoban-logic.test.js
  - tests/sokoban.spec.js
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - `推箱子` becomes a true direct-launch title at `/games/sokoban` and never pretends to use room numbers, invites, or multiplayer shell copy.
    - Sokoban move rules stay deterministic: walls block movement, crates push one tile at a time, double-pushes are illegal, and completion only occurs when every crate is on a goal.
    - The first solo title feels shippable now, with a small built-in level pack, reset loop, and keyboard/mobile controls rather than a placeholder card.
  artifacts:
    - lib/solo/sokoban.js
    - pages/games/sokoban.js
    - tests/sokoban.spec.js
  key_links:
    - lib/solo/sokoban.js -> components/solo/SokobanBoard.js
    - lib/solo/sokoban.js -> test-logic/sokoban-logic.test.js
    - pages/games/sokoban.js -> components/solo/SokobanBoard.js
    - pages/games/sokoban.js -> tests/sokoban.spec.js
---

<objective>
Ship `推箱子` as the first playable solo title in the family arcade.

Purpose: Wave 1 needs at least one real direct-launch game so the new-game push is not limited to room-based additions.
Output: A deterministic Sokoban logic module, a dedicated `/games/sokoban` page, compact solo UI controls, and smoke coverage proving the title can be launched and played without room flow.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-CONTEXT.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-RESEARCH.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-VALIDATION.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-01-PLAN.md
@lib/games/catalog.js
@pages/index.js
@pages/games/[gameKey].js
@styles/GameLobby.module.css
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build the Sokoban rules engine and bundled level pack</name>
  <files>lib/solo/sokoban.js, test-logic/sokoban-logic.test.js</files>
  <read_first>.planning/phases/03.1-new-game-delivery-wave-1/03.1-RESEARCH.md, lib/games/catalog.js, test-logic/chinesecheckers-logic.test.js</read_first>
  <action>Create `lib/solo/sokoban.js` as a pure rules module for the direct-launch title. Export a small built-in level pack of at least 4 handcrafted stages plus helpers that create a session state, apply directional movement, reject illegal wall or double-crate pushes, detect solved boards, reset a level, and advance to the next bundled stage. Track at minimum `levelId`, `moveCount`, `pushCount`, `solved`, player coordinates, crate coordinates, and goal coordinates so UI code does not have to infer rules from raw tiles. In `test-logic/sokoban-logic.test.js`, add coverage for blocked wall movement, single-crate push into open space, illegal double push, solved-state detection, and reset/next-level behavior.</action>
  <verify>node --test test-logic/sokoban-logic.test.js</verify>
  <acceptance_criteria>
    - `lib/solo/sokoban.js` exists and exports a bundled level pack plus pure move/reset helpers
    - illegal moves do not mutate the board state
    - solved-state detection only passes when every crate sits on a goal tile
    - `node --test test-logic/sokoban-logic.test.js` exits 0
  </acceptance_criteria>
  <done>Sokoban gameplay logic is deterministic and testable outside the page runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the `/games/sokoban` direct-launch page and solo smoke coverage</name>
  <files>components/solo/SokobanBoard.js, pages/games/sokoban.js, styles/Sokoban.module.css, tests/sokoban.spec.js</files>
  <read_first>pages/index.js, pages/games/[gameKey].js, styles/GameLobby.module.css, .planning/phases/03.1-new-game-delivery-wave-1/03.1-01-PLAN.md, lib/solo/sokoban.js</read_first>
  <action>Create a dedicated `pages/games/sokoban.js` page instead of forcing Sokoban through the multiplayer lobby shell. The page must render the exact title `推箱子`, the exact helper copy `直接開始，不需房號`, a compact board component in `components/solo/SokobanBoard.js`, keyboard controls for arrow keys / WASD, tap-friendly direction buttons for mobile, and visible progression controls with exact labels `重開本關`, `上一關`, and `下一關`. Show live move/push counters and an obvious solved state so the page feels finished rather than experimental. Add `tests/sokoban.spec.js` as a focused Playwright smoke that proves Sokoban can be opened from its real route, accepts player input, and can reset or advance without any room-number flow or login gate.</action>
  <verify>npm run check && node --test test-logic/sokoban-logic.test.js && npx playwright test tests/sokoban.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `pages/games/sokoban.js` exists
    - the page visibly contains `推箱子` and `直接開始，不需房號`
    - the page visibly contains `重開本關` and `下一關`
    - `tests/sokoban.spec.js` exists and exercises the real Sokoban route
    - `npm run check && node --test test-logic/sokoban-logic.test.js && npx playwright test tests/sokoban.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>Sokoban is now a real direct-launch game in the arcade rather than a greyed family card.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Solo gameplay now lives entirely in browser state, so rule integrity must come from pure logic helpers rather than ad hoc component mutations.
- The family arcade now mixes direct-launch and room-based titles, so Sokoban must not accidentally surface join-room or invite semantics.
- Mobile and keyboard input both mutate the same board state and can drift if the page duplicates move logic.
</trust_boundaries>

<stride_register>
- threat_id: T-03.1-02
  category: Tampering
  component: Sokoban board state
  disposition: mitigate
  mitigation_plan: centralize all move validation in `lib/solo/sokoban.js` and cover illegal move cases in `test-logic/sokoban-logic.test.js`.
- threat_id: T-03.1-03
  category: Spoofing
  component: launch semantics
  disposition: mitigate
  mitigation_plan: ship Sokoban on a dedicated `/games/sokoban` route with direct-launch copy and no room-number entry widgets.
- threat_id: T-03.1-04
  category: Repudiation
  component: page/UI state drift
  disposition: mitigate
  mitigation_plan: keep board state, counters, solved state, and level transitions derived from pure helpers so UI smoke and logic tests validate the same rules contract.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/sokoban-logic.test.js`
- [ ] `npm run check`
- [ ] `npx playwright test tests/sokoban.spec.js --workers=1`
- [ ] Sokoban launches directly from `/games/sokoban` without room-number or invite copy
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `推箱子` is visibly playable now as the first solo game in the family arcade
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-new-game-delivery-wave-1/03.1-02-SUMMARY.md`
</output>
