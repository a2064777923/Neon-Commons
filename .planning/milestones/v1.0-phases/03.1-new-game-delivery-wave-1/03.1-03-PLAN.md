---
phase: 03.1-new-game-delivery-wave-1
plan: 03
type: execute
wave: 2
depends_on:
  - "03.1-01"
files_modified:
  - lib/board/reversi.js
  - lib/board/manager.js
  - pages/games/[gameKey].js
  - pages/reversi/[roomNo].js
  - styles/ReversiRoom.module.css
  - test-logic/reversi-logic.test.js
  - tests/reversi.spec.js
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - `黑白棋` becomes a real board-family game with room creation at `/games/reversi` and room/invite landing at `/reversi/{roomNo}`.
    - Reversi legality, flipping, forced-pass, and winner scoring are enforced server-side rather than inferred only in the client.
    - Room-number, share-link, guest/private-room, and bot-fill behavior stay compatible with the existing board-family contract instead of creating a second room system.
  artifacts:
    - lib/board/reversi.js
    - lib/board/manager.js
    - pages/reversi/[roomNo].js
    - tests/reversi.spec.js
  key_links:
    - lib/board/reversi.js -> lib/board/manager.js
    - lib/board/manager.js -> pages/reversi/[roomNo].js
    - lib/games/catalog.js -> pages/games/[gameKey].js
    - pages/reversi/[roomNo].js -> tests/reversi.spec.js
---

<objective>
Ship `黑白棋` as the first new room-based board title in Wave 1.

Purpose: Wave 1 needs a low-risk multiplayer game that proves the current board-room stack can absorb fresh gameplay without regressing existing titles.
Output: A Reversi rules engine, board-room manager support, dedicated room route, and smoke coverage for create/join/play flow.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-CONTEXT.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-RESEARCH.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-VALIDATION.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-01-PLAN.md
@lib/games/catalog.js
@lib/board/manager.js
@pages/games/[gameKey].js
@pages/board/[roomNo].js
@tests/board-games.spec.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the Reversi rules engine and integrate it with board-room state</name>
  <files>lib/board/reversi.js, lib/board/manager.js, test-logic/reversi-logic.test.js</files>
  <read_first>.planning/phases/03.1-new-game-delivery-wave-1/03.1-RESEARCH.md, lib/board/manager.js, test-logic/chinesecheckers-logic.test.js, lib/games/catalog.js</read_first>
  <action>Create `lib/board/reversi.js` as a pure engine for standard 8x8 Reversi. Export helpers for initial board creation, legal move discovery, directional flipping, pass detection, end-state detection, and score calculation. In `lib/board/manager.js`, integrate `reversi` as a first-class board game: create a Reversi match on room start, reject illegal moves server-side, auto-pass when the next seat has no legal move, finish the game when neither side can move or the board is full, and add a simple deterministic bot that prefers corners, then edges, then best flip count. Update board-room directory entries to derive `detailRoute` from game metadata `detailRoutePrefix` so Reversi invites resolve to `/reversi/{roomNo}` instead of the generic board route. Add `test-logic/reversi-logic.test.js` coverage for opening legal moves, multi-direction flips, forced-pass behavior, and winner scoring.</action>
  <verify>node --test test-logic/reversi-logic.test.js</verify>
  <acceptance_criteria>
    - `lib/board/reversi.js` exists and contains pure Reversi move/score helpers
    - `lib/board/manager.js` accepts `reversi` and derives room detail routes from metadata for board-family games
    - illegal Reversi moves are rejected server-side
    - `node --test test-logic/reversi-logic.test.js` exits 0
  </acceptance_criteria>
  <done>Board-room state can now host Reversi with deterministic legality, flipping, pass, and result handling.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the Reversi lobby-to-room flow and gameplay smoke</name>
  <files>pages/games/[gameKey].js, pages/reversi/[roomNo].js, styles/ReversiRoom.module.css, tests/reversi.spec.js</files>
  <read_first>pages/games/[gameKey].js, pages/board/[roomNo].js, styles/GameLobby.module.css, styles/BoardRoom.module.css, lib/board/manager.js, lib/board/reversi.js</read_first>
  <action>Update `pages/games/[gameKey].js` so room creation and quick join respect `meta.detailRoutePrefix` when present instead of hardcoding `/board` or `/party`. Create a dedicated `pages/reversi/[roomNo].js` room page that reuses the existing room shell expectations but renders an 8x8 disc board, visible legal-move hints, current score, ready/add-bot controls, and match result overlay tuned for Reversi. Keep the existing room-number and invite entry contract intact: private invite deep links for Reversi must ultimately land on `/reversi/{roomNo}` after entry resolution or direct create-room redirect. Add `tests/reversi.spec.js` as a Playwright smoke that logs in, creates a Reversi room from `/games/reversi`, verifies the dedicated room URL, makes at least one legal move, and confirms invite/deep-link routing lands on the same dedicated room path.</action>
  <verify>npm run check && node --test test-logic/reversi-logic.test.js test-logic/chinesecheckers-logic.test.js && npx playwright test tests/reversi.spec.js tests/board-games.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `pages/reversi/[roomNo].js` exists
    - `pages/games/[gameKey].js` derives room redirects from `detailRoutePrefix` when available
    - creating or resolving a Reversi room lands on `/reversi/{roomNo}`
    - `tests/reversi.spec.js` exists and proves a real Reversi move can be made
    - `npm run check && node --test test-logic/reversi-logic.test.js test-logic/chinesecheckers-logic.test.js && npx playwright test tests/reversi.spec.js tests/board-games.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>Reversi is now a real invite-safe board-room title with its own dedicated room experience.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Reversi gameplay now crosses from browser input into server-enforced board legality and back into real-time room sync.
- Board-family room links rely on `detailRoutePrefix`; a mismatch would misroute create/join/share flows even if gameplay logic is correct.
- New board logic must coexist with Gomoku and Chinese Checkers inside one room manager without regressing those shipped games.
</trust_boundaries>

<stride_register>
- threat_id: T-03.1-05
  category: Tampering
  component: Reversi move legality
  disposition: mitigate
  mitigation_plan: validate flips, pass states, and winner scoring in `lib/board/reversi.js` and enforce them in `lib/board/manager.js`.
- threat_id: T-03.1-06
  category: Spoofing
  component: room-entry routing
  disposition: mitigate
  mitigation_plan: derive Reversi room detail routes from metadata `detailRoutePrefix` and cover the dedicated `/reversi/{roomNo}` path in Playwright smoke coverage.
- threat_id: T-03.1-07
  category: Repudiation
  component: board-family regressions
  disposition: mitigate
  mitigation_plan: keep the existing `tests/board-games.spec.js` and `test-logic/chinesecheckers-logic.test.js` in the verify command so Reversi additions cannot silently break shipped board titles.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/reversi-logic.test.js`
- [ ] `npm run check`
- [ ] `node --test test-logic/chinesecheckers-logic.test.js`
- [ ] `npx playwright test tests/reversi.spec.js tests/board-games.spec.js --workers=1`
- [ ] Reversi create-room, quick-join, and invite-entry all land on `/reversi/{roomNo}`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `黑白棋` works as a real board-room title without regressing existing board flows
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-new-game-delivery-wave-1/03.1-03-SUMMARY.md`
</output>
