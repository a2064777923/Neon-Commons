---
phase: 03.1-new-game-delivery-wave-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/games/catalog.js
  - lib/admin/control-plane.js
  - components/game-hub/GameIcon.js
  - pages/index.js
  - test-logic/hub-room-entry.test.js
  - test-logic/admin-control-plane.test.js
  - tests/hub-entry.spec.js
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - `推箱子`, `黑白棋`, and `誰是臥底` are promoted from `即將推出` to live discovery entries with real routes instead of placeholder cards.
    - `UNO 類`, `你畫我猜`, `飛行棋`, `保齡球`, and `迷你賽車/碰碰車` stay visibly greyed `即將推出` in the family arcade.
    - Homepage and admin discovery payloads distinguish room-based live titles from direct-launch solo titles without showing fake room-number copy for Sokoban.
  artifacts:
    - lib/games/catalog.js
    - lib/admin/control-plane.js
    - pages/index.js
    - tests/hub-entry.spec.js
  key_links:
    - lib/games/catalog.js -> lib/admin/control-plane.js
    - lib/games/catalog.js -> components/game-hub/GameIcon.js
    - lib/admin/control-plane.js -> test-logic/admin-control-plane.test.js
    - pages/index.js -> tests/hub-entry.spec.js
---

<objective>
Establish the shared launch contract for Wave 1 by promoting the selected live titles and keeping the rest of the approved pool intentionally greyed as `即將推出`.

Purpose: The phase cannot execute cleanly until the family arcade, discovery payload, and CTA copy all agree on which new titles are really playable now.
Output: Catalog metadata for the Wave 1 live subset, discovery-state synthesis that understands those titles, homepage CTA/copy that handles both direct-launch and room-based entries, and targeted contract coverage.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-CONTEXT.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-RESEARCH.md
@.planning/phases/03.1-new-game-delivery-wave-1/03.1-VALIDATION.md
@lib/games/catalog.js
@lib/admin/control-plane.js
@components/game-hub/GameIcon.js
@pages/index.js
@backend/handlers/hub.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Promote the Wave 1 live subset in shared metadata and capability synthesis</name>
  <files>lib/games/catalog.js, lib/admin/control-plane.js, components/game-hub/GameIcon.js, test-logic/hub-room-entry.test.js, test-logic/admin-control-plane.test.js</files>
  <read_first>.planning/phases/03.1-new-game-delivery-wave-1/03.1-CONTEXT.md, .planning/phases/03.1-new-game-delivery-wave-1/03.1-RESEARCH.md, lib/games/catalog.js, lib/admin/control-plane.js, components/game-hub/GameIcon.js, test-logic/hub-room-entry.test.js, test-logic/admin-control-plane.test.js</read_first>
  <action>Promote the exact Wave 1 live subset `sokoban`, `reversi`, and `undercover` inside `lib/games/catalog.js`. Set their exact routes to `/games/sokoban`, `/games/reversi`, and `/games/undercover`; set `launchState: "live"` and `isShipped: true`; set `supportsShareLink: false` for `sokoban` and `true` for `reversi` / `undercover`; set `detailRoutePrefix: "/reversi"` for `reversi` and `detailRoutePrefix: "/undercover"` for `undercover`; and extend `BOARD_GAME_KEYS` to include `reversi` and `PARTY_GAME_KEYS` to include `undercover`. Keep `uno`, `drawguess`, `flyingchess`, `bowling`, and `miniracers` as `launchState: "coming-soon"` with no playable route. In `lib/admin/control-plane.js`, keep room-based shipped titles capability-managed but stop silently dropping live titles from new families: update capability family grouping so shipped `solo` titles such as `sokoban` can still appear in capability responses instead of being omitted. Preserve the exact discovery state labels `可立即遊玩`, `暫停新房`, and `即將推出`. In `components/game-hub/GameIcon.js`, add explicit SVG/icon cases for `sokoban`, `reversi`, and `undercover` so the new live cards do not fall back to the 斗地主 art. Add logic coverage proving `sokoban`, `reversi`, and `undercover` resolve as live/playable discovery items while `uno`, `drawguess`, `flyingchess`, `bowling`, and `miniracers` remain `coming-soon`.</action>
  <verify>node --test test-logic/hub-room-entry.test.js test-logic/admin-control-plane.test.js</verify>
  <acceptance_criteria>
    - `lib/games/catalog.js` contains the exact routes `/games/sokoban`, `/games/reversi`, and `/games/undercover`
    - `lib/games/catalog.js` contains `launchState: "live"` for `sokoban`, `reversi`, and `undercover`
    - `lib/games/catalog.js` still contains `launchState: "coming-soon"` for `uno`, `drawguess`, `flyingchess`, `bowling`, and `miniracers`
    - `components/game-hub/GameIcon.js` contains explicit cases for `sokoban`, `reversi`, and `undercover`
    - `node --test test-logic/hub-room-entry.test.js test-logic/admin-control-plane.test.js` exits 0
  </acceptance_criteria>
  <done>The Wave 1 live subset is now promoted in shared metadata, and discovery/capability synthesis agrees on which new games are actually playable.</done>
</task>

<task type="auto">
  <name>Task 2: Update homepage CTA logic for direct-launch vs room-based live titles</name>
  <files>pages/index.js, tests/hub-entry.spec.js</files>
  <read_first>pages/index.js, tests/hub-entry.spec.js, .planning/phases/03.1-new-game-delivery-wave-1/03.1-CONTEXT.md, .planning/phases/03.1-new-game-delivery-wave-1/03.1-RESEARCH.md, lib/games/catalog.js, lib/admin/control-plane.js</read_first>
  <action>Update `pages/index.js` so the family arcade no longer treats every live title as a room-number game. Keep room-based live titles using the existing CTA `前往大廳`, keep paused room-based titles using the exact CTA `房號／邀請仍可進`, and add a direct-launch CTA `立即遊玩` for `sokoban`. Replace zero-room fallback copy for direct-launch titles with the exact text `直接開始，不需房號`; keep room-based live cards using room-count or room-entry-oriented copy. Do not change the existing `即將推出` chips for the deferred pool. In `tests/hub-entry.spec.js`, add assertions that homepage visibly renders `推箱子`, `黑白棋`, and `誰是臥底` as live cards and that `UNO 類` plus at least one heavier deferred title (use the exact text `你畫我猜`) still appear with `即將推出` visible on the homepage.</action>
  <verify>npm run check && npx playwright test tests/hub-entry.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `pages/index.js` contains the exact CTA text `立即遊玩`
    - `pages/index.js` contains the exact helper text `直接開始，不需房號`
    - homepage still renders the exact state label `即將推出`
    - `tests/hub-entry.spec.js` contains the exact title assertions `推箱子`, `黑白棋`, `誰是臥底`, `UNO 類`, and `你畫我猜`
    - `npm run check && npx playwright test tests/hub-entry.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>The family arcade can now distinguish direct-launch and room-based new live titles without misleading copy.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Family discovery data crosses from shared catalog and control-plane synthesis into public homepage cards and admin capability responses.
- Promoting placeholder entries to live routes creates a risk of surfacing dead links or incorrect state labels before the actual games exist.
- Direct-launch solo titles and room-based multiplayer titles now coexist in the same family arcade, so CTA/copy mismatches can misroute users.
</trust_boundaries>

<stride_register>
- threat_id: T-03.1-01
  category: Tampering
  component: shared discovery metadata
  disposition: mitigate
  mitigation_plan: promote only the exact live subset `sokoban`, `reversi`, and `undercover`; leave all other approved titles explicitly `coming-soon`; and cover both in hub/admin logic tests.
- threat_id: T-03.1-02
  category: Spoofing
  component: homepage CTA semantics
  disposition: mitigate
  mitigation_plan: reserve `立即遊玩` for direct-launch Sokoban, keep room-based CTAs distinct, and assert the exact CTA/copy strings in homepage smoke coverage.
- threat_id: T-03.1-03
  category: Repudiation
  component: admin capability view of new live titles
  disposition: mitigate
  mitigation_plan: ensure capability responses do not silently omit shipped titles from new families, and cover visibility through `test-logic/admin-control-plane.test.js`.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/hub-room-entry.test.js test-logic/admin-control-plane.test.js`
- [ ] `npm run check`
- [ ] `npx playwright test tests/hub-entry.spec.js --workers=1`
- [ ] homepage clearly distinguishes playable-now Wave 1 titles from deferred `即將推出` titles
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The family arcade and capability layer now agree on the exact Wave 1 live subset
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-new-game-delivery-wave-1/03.1-01-SUMMARY.md`
</output>
