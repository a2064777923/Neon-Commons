---
phase: 05-board-gameplay-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/games/catalog.js
  - lib/board/manager.js
  - pages/games/[gameKey].js
  - pages/board/[roomNo].js
  - styles/GameLobby.module.css
  - styles/BoardRoom.module.css
autonomous: true
requirements:
  - BOARD-01
user_setup: []
must_haves:
  truths:
    - Gomoku rooms can be created with either standard opening or center-opening, and the backend enforces the selected opening rule from canonical room config.
    - Chinese Checkers rooms expose live target-camp progress or equivalent race-state guidance without changing the existing board geometry or room-entry flow.
    - Board lobby cards and live board rooms show the same active board settings from one canonical summary path.
  artifacts:
    - lib/games/catalog.js
    - lib/board/manager.js
    - pages/games/[gameKey].js
    - pages/board/[roomNo].js
  key_links:
    - lib/games/catalog.js -> pages/games/[gameKey].js and pages/board/[roomNo].js share one board config summary vocabulary.
    - lib/board/manager.js -> pages/board/[roomNo].js carries canonical board config and Chinese progress in serialized room payloads.
---

<objective>
Extend the shared board-room capability surface with one low-risk Gomoku option and one Chinese Checkers interaction expansion.

Purpose: Deliver visible board-family expansion without changing routes, board geometry, or live-room sync architecture.
Output: Normalized Gomoku opening-rule support, Chinese Checkers progress serialization, and coherent board config/progress UI in the board lobby and board room.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-board-gameplay-expansion/05-CONTEXT.md
@.planning/phases/05-board-gameplay-expansion/05-RESEARCH.md
@.planning/phases/05-board-gameplay-expansion/05-VALIDATION.md
@lib/games/catalog.js
@lib/board/manager.js
@pages/games/[gameKey].js
@pages/board/[roomNo].js
@styles/GameLobby.module.css
@styles/BoardRoom.module.css
</context>

<threat_model>
- **T-05-01:** Malformed or unsupported board option values could create server/client rule drift.
  **Mitigation:** Normalize board config in `lib/board/manager.js` and accept only explicit enum values for Gomoku opening rules.
- **T-05-02:** New board options could accidentally mutate live matches after room creation.
  **Mitigation:** Keep new settings inside canonical `room.config` created at room creation time; do not add any mid-match mutation path.
- **T-05-03:** Lobby and room UI could present different labels or stale state for the same board option.
  **Mitigation:** Add one shared board config summary helper in `lib/games/catalog.js` and serialize Chinese progress from the backend room payload.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Normalize new board capability fields in the shared runtime</name>
  <files>lib/games/catalog.js, lib/board/manager.js</files>
  <read_first>lib/games/catalog.js, lib/board/manager.js, pages/games/[gameKey].js, pages/board/[roomNo].js</read_first>
  <action>Add Gomoku `openingRule` support with exact enum values `"standard"` and `"center-opening"` in `lib/games/catalog.js` default config helpers and normalize it in `lib/board/manager.js`. Export a shared `getBoardConfigSummary(gameKey, config)` helper that returns human-readable board config chips using canonical labels, including `"标准开局"` or `"天元开局"` plus `"{N} 秒/手"` for Gomoku and Chinese Checkers. In `lib/board/manager.js`, enforce center-opening in the Gomoku move path by rejecting the first move unless it lands on row `7`, col `7`, with the exact error text `"天元开局首手必须落在棋盘中心"`. Also add Chinese Checkers progress serialization that returns per-seat `goalReached`, `goalTotal`, `remaining`, `campLabel`, `targetCampLabel`, and `pieceAccent` values derived from the existing positions map and target camp metadata.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - `lib/games/catalog.js` contains `openingRule` and both `"standard"` and `"center-opening"`
    - `lib/games/catalog.js` exports `getBoardConfigSummary`
    - `lib/board/manager.js` contains the exact string `天元开局首手必须落在棋盘中心`
    - `lib/board/manager.js` serializes `goalReached`, `goalTotal`, and `remaining` for Chinese Checkers progress
  </acceptance_criteria>
  <done>Canonical board config and progress data exist in the shared runtime with Gomoku opening-rule enforcement on the server.</done>
</task>

<task type="auto">
  <name>Task 2: Surface board settings and race-state cues in lobby and room UI</name>
  <files>pages/games/[gameKey].js, pages/board/[roomNo].js, styles/GameLobby.module.css, styles/BoardRoom.module.css</files>
  <read_first>pages/games/[gameKey].js, pages/board/[roomNo].js, styles/GameLobby.module.css, styles/BoardRoom.module.css, lib/games/catalog.js, lib/board/manager.js</read_first>
  <action>Update `pages/games/[gameKey].js` so Gomoku rooms expose a labeled `开局规则` selector with exact options `"标准开局"` and `"天元开局"`, and board public-room cards render `getBoardConfigSummary(room.gameKey, room.config)` chips the same way party rooms already render their selected setup. Update `pages/board/[roomNo].js` to render active board config chips in the HUD or info dock for board rooms, and for Chinese Checkers render per-seat progress cards using the serialized `goalReached`, `goalTotal`, `remaining`, `campLabel`, and `targetCampLabel` fields without replacing the existing board layout. Update the board-room hint copy so Gomoku explains `天元开局` before the first move when active.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - `pages/games/[gameKey].js` contains the exact label `开局规则`
    - `pages/games/[gameKey].js` calls `getBoardConfigSummary(`
    - `pages/board/[roomNo].js` references `goalReached` and `goalTotal`
    - `pages/board/[roomNo].js` contains the exact string `天元开局`
  </acceptance_criteria>
  <done>Players can see the selected board settings before joining and can read live Chinese Checkers progress from the board room without changing routes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] `node --test test-logic/board-config.test.js` passes after the matching validation plan lands
- [ ] Manual spot-read confirms the board lobby and board room use the same labels for active board settings
</verification>

<success_criteria>

- All tasks completed
- Gomoku opening-rule selection is normalized and enforced from canonical room config
- Chinese Checkers progress is serialized and rendered without changing the current board geometry
- Board lobby cards and board-room HUD show matching board config language
</success_criteria>

<output>
After completion, create `.planning/phases/05-board-gameplay-expansion/05-01-SUMMARY.md`
</output>
