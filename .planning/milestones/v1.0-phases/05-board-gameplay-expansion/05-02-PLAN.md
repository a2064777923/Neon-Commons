---
phase: 05-board-gameplay-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - test-logic/board-config.test.js
  - test-logic/chinesecheckers-logic.test.js
  - tests/board-games.spec.js
autonomous: true
requirements:
  - BOARD-01
user_setup: []
must_haves:
  truths:
    - Board expansion is covered by automated node tests for config enforcement and Chinese progress serialization.
    - Browser smoke proves Gomoku opening-rule UI and Chinese Checkers progress UI work without breaking create-room, start, and move sync.
    - Shared board endpoints and real-time flow remain stable under the new board capability surface.
  artifacts:
    - test-logic/board-config.test.js
    - test-logic/chinesecheckers-logic.test.js
    - tests/board-games.spec.js
  key_links:
    - test-logic/board-config.test.js -> lib/board/manager.js verifies new config normalization and Gomoku enforcement logic.
    - tests/board-games.spec.js -> pages/games/[gameKey].js and pages/board/[roomNo].js verifies the new UI surface in live browser flows.
---

<objective>
Lock in regression coverage for the new board options and interaction surfaces introduced in Phase 5.

Purpose: Ensure board-family expansion ships with explicit logic and browser protection around realtime sync, canonical config, and UI visibility.
Output: New node-based board regression coverage plus an updated Playwright board smoke suite.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-board-gameplay-expansion/05-CONTEXT.md
@.planning/phases/05-board-gameplay-expansion/05-RESEARCH.md
@.planning/phases/05-board-gameplay-expansion/05-VALIDATION.md
@lib/board/manager.js
@lib/games/catalog.js
@test-logic/chinesecheckers-logic.test.js
@tests/board-games.spec.js
</context>

<threat_model>
- **T-05-04:** The new board capability could appear to work in the UI while backend normalization or Gomoku opening enforcement silently regresses.
  **Mitigation:** Add explicit node coverage for normalization, error handling, and serialized Chinese progress.
- **T-05-05:** Browser smoke could miss a live-room desync where the new board settings render but moves or progress do not update correctly.
  **Mitigation:** Extend Playwright smoke to exercise the new Gomoku opening selection and confirm Chinese Checkers progress remains visible during live moves.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add node regressions for board config and serialization</name>
  <files>test-logic/board-config.test.js, test-logic/chinesecheckers-logic.test.js</files>
  <read_first>lib/board/manager.js, lib/games/catalog.js, test-logic/chinesecheckers-logic.test.js</read_first>
  <action>Create `test-logic/board-config.test.js` to cover the exact Gomoku `openingRule` enum contract, malformed-value normalization back to `"standard"`, server rejection of a non-center first move when `openingRule` is `"center-opening"`, acceptance of the center move, and Chinese Checkers progress serialization fields `goalReached`, `goalTotal`, and `remaining`. Extend `test-logic/chinesecheckers-logic.test.js` with a room-state or helper-level assertion that target-camp progress increases when a seat's piece reaches its target camp.</action>
  <verify>node --test test-logic/board-config.test.js test-logic/chinesecheckers-logic.test.js</verify>
  <acceptance_criteria>
    - `test-logic/board-config.test.js` exists
    - `test-logic/board-config.test.js` contains the exact string `center-opening`
    - `test-logic/board-config.test.js` contains the exact string `天元开局首手必须落在棋盘中心`
    - `test-logic/chinesecheckers-logic.test.js` contains `goalReached` or `remaining`
  </acceptance_criteria>
  <done>Board node regressions cover the new config contract and serialized Chinese progress behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Extend browser smoke to cover new board surfaces end to end</name>
  <files>tests/board-games.spec.js</files>
  <read_first>tests/board-games.spec.js, pages/games/[gameKey].js, pages/board/[roomNo].js</read_first>
  <action>Update `tests/board-games.spec.js` so the Gomoku flow explicitly selects the `"天元开局"` option before creating the room, proves a non-center opening move does not satisfy the rule, then places the first stone on the center cell. Extend the Chinese Checkers flow so it asserts the progress UI is visible and still updates while the board move sync succeeds. Keep the existing create-room, ready, add-bot, and live-move smoke path intact while adding the new assertions.</action>
  <verify>npx playwright test tests/board-games.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `tests/board-games.spec.js` contains the exact string `天元开局`
    - `tests/board-games.spec.js` still contains `[data-gomoku-cell=\"7-7\"]`
    - `tests/board-games.spec.js` contains a progress-related assertion for Chinese Checkers
  </acceptance_criteria>
  <done>The browser smoke suite exercises the new board option and progress UI without regressing the existing board-room flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] `node --test test-logic/board-config.test.js test-logic/chinesecheckers-logic.test.js test-logic/client-network-contract.test.js`
- [ ] `npx playwright test tests/board-games.spec.js --workers=1`
</verification>

<success_criteria>

- All tasks completed
- Automated node coverage protects the new board config and progress payloads
- Board browser smoke covers the new Gomoku opening rule and Chinese Checkers progress UI
- Shared board route and move-sync behavior remain green after the board expansion changes
</success_criteria>

<output>
After completion, create `.planning/phases/05-board-gameplay-expansion/05-02-SUMMARY.md`
</output>
