---
phase: 03-hub-room-expansion-framework
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - pages/**/*.js
  - components/MatchResultOverlay.js
  - styles/Arcade.module.css
  - styles/GameLobby.module.css
  - styles/UtilityPages.module.css
  - styles/MatchResultOverlay.module.css
  - tests/hub-entry.spec.js
  - tests/arcade-party.spec.js
  - tests/board-games.spec.js
autonomous: true
requirements:
  - HUB-01
  - ROOM-01
user_setup: []
must_haves:
  truths:
    - The homepage visibly becomes a family-based arcade with a universal `遊戲入口` command dock, compact family bands, and distinct `暫停新房` / `即將推出` states.
    - Family lobbies, invite entry, and login handoff all use the same room-entry contract while preserving dedicated create-room pages and local quick join.
    - Guest-eligible invite flows surface the approved login-or-guest intercept and a post-match `登入並同步本局` prompt without enabling guest entry in unsupported card/economy flows.
  artifacts:
    - pages/index.js
    - pages/entry/[gameKey]/[roomNo].js
    - components/MatchResultOverlay.js
    - tests/hub-entry.spec.js
  key_links:
    - backend/handlers/hub.js -> pages/index.js
    - lib/shared/network-contract.js -> pages/entry/[gameKey]/[roomNo].js
    - pages/login.js -> pages/entry/[gameKey]/[roomNo].js
    - components/MatchResultOverlay.js -> pages/party/[roomNo].js
    - components/MatchResultOverlay.js -> pages/board/[roomNo].js
    - pages/index.js -> tests/hub-entry.spec.js
---

<objective>
Ship the Phase 3 frontend surfaces that consume the new discovery and room-entry contracts.

Purpose: Phase 3 is not complete until players can actually see the family arcade, use the universal entry point, and experience the invite/guest flow defined in `03-UI-SPEC.md`.
Output: A new family-based homepage, a backend-driven entry page, upgraded family lobbies and login handoff, guest sync prompt wiring, and end-to-end smoke coverage for the new flow.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hub-room-expansion-framework/03-CONTEXT.md
@.planning/phases/03-hub-room-expansion-framework/03-RESEARCH.md
@.planning/phases/03-hub-room-expansion-framework/03-UI-SPEC.md
@.planning/phases/03-hub-room-expansion-framework/03-PATTERNS.md
@pages/index.js
@pages/lobby.js
@pages/games/[gameKey].js
@pages/login.js
@pages/party/[roomNo].js
@pages/board/[roomNo].js
@components/MatchResultOverlay.js
@styles/Arcade.module.css
@styles/GameLobby.module.css
@styles/UtilityPages.module.css
</context>

<tasks>
<task type="auto">
  <name>Task 1: Rebuild the homepage into the Phase 3 family arcade and universal command dock</name>
  <files>pages/index.js, styles/Arcade.module.css</files>
  <read_first>.planning/phases/03-hub-room-expansion-framework/03-UI-SPEC.md, pages/index.js, styles/Arcade.module.css, lib/client/api.js, lib/shared/network-contract.js, backend/handlers/hub.js, components/game-hub/GameIcon.js</read_first>
  <action>Per D-01, D-02, D-03, D-04, D-07, D-14, D-15, D-16, and the `Visual Direction`, `Homepage Information Architecture`, and `Component Contracts` sections of `03-UI-SPEC.md`, rebuild `pages/index.js` and `styles/Arcade.module.css` to consume `API_ROUTES.hub()` instead of manually fan-out fetching six room lists. Render the central module with exact heading `遊戲入口` and exact mode labels `房號加入`, `貼上邀請`, `分享我的房`. Replace the flat `HUB_ORDER` grid with family bands for `經典牌桌`, `推理派對`, `棋盤對戰`, `單人闖關`, and `輕量 3D`, using exact state labels `可立即遊玩`, `暫停新房`, and `即將推出`. Keep side rails secondary, keep live room feed visible, and ensure paused cards display the exact helper copy `目前不開新房，已有房號或邀請可直接加入。` while upcoming cards show teaser tags rather than room counts. Preserve the strong clipped-corner neon style already established by the current homepage and global tokens; do not collapse this into a dashboard table layout.</action>
  <verify>npx playwright test tests/hub-entry.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `pages/index.js` calls `API_ROUTES.hub()`
    - homepage renders the exact heading `遊戲入口`
    - homepage renders the exact family label `遊戲家族`
    - homepage renders `暫停新房` and `即將推出` as distinct visible states
    - `npx playwright test tests/hub-entry.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>The homepage now behaves like the approved family arcade rather than a flat launcher, and the universal entry dock is the main focal point.</done>
</task>

<task type="auto">
  <name>Task 2: Wire invite entry, family-lobby consistency, login return paths, and guest sync prompts</name>
  <files>pages/entry/[gameKey]/[roomNo].js, pages/lobby.js, pages/games/[gameKey].js, pages/login.js, pages/party/[roomNo].js, pages/board/[roomNo].js, components/MatchResultOverlay.js, styles/GameLobby.module.css, styles/UtilityPages.module.css, styles/MatchResultOverlay.module.css</files>
  <read_first>.planning/phases/03-hub-room-expansion-framework/03-UI-SPEC.md, pages/lobby.js, pages/games/[gameKey].js, pages/login.js, pages/party/[roomNo].js, pages/board/[roomNo].js, components/MatchResultOverlay.js, lib/shared/network-contract.js, lib/client/api.js, backend/handlers/room-entry/resolve.js, backend/handlers/room-entry/guest.js, backend/handlers/room-entry/guest-sync.js</read_first>
  <action>Per D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, and D-15, create `pages/entry/[gameKey]/[roomNo].js` as the universal deep-link landing page. It must call the room-entry resolve contract on load, auto-enter logged-in real users, show the approved guest/login intercept when `guestAllowed === true`, and route login users through `/login?returnTo=/entry/{gameKey}/{roomNo}` instead of always sending them to `/`. Update `pages/login.js` so successful login respects `returnTo`. Update `pages/lobby.js` and `pages/games/[gameKey].js` to keep local room-number fast join per D-08, preserve dedicated create-room surfaces per D-06, add `返回遊戲家族`, surface `暫停新房` banners when the backend says new rooms are paused, and add `複製邀請` actions that copy the exact deep-link path `/entry/{gameKey}/{roomNo}`. Extend `components/MatchResultOverlay.js` plus `pages/party/[roomNo].js` and `pages/board/[roomNo].js` so guest-finished matches show exact CTA labels `登入並同步本局` and `稍後再說`, calling `API_ROUTES.roomEntry.guestSync()` after the user returns through login. Keep Dou Dizhu login-only: do not surface guest invite CTAs or post-match guest sync in `pages/room/[roomNo].js`.</action>
  <verify>npx playwright test tests/hub-entry.spec.js tests/arcade-party.spec.js tests/board-games.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `pages/entry/[gameKey]/[roomNo].js` exists and calls the room-entry resolve API
    - `pages/login.js` reads and honors `returnTo`
    - `pages/lobby.js` and `pages/games/[gameKey].js` contain `返回遊戲家族` and `複製邀請`
    - `components/MatchResultOverlay.js` can render `登入並同步本局`
    - Dou Dizhu room flow does not expose guest CTAs
    - `npx playwright test tests/hub-entry.spec.js tests/arcade-party.spec.js tests/board-games.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>Invite deep links, per-family lobbies, login handoff, and guest-result sync prompts now share one coherent room-entry experience.</done>
</task>

<task type="auto">
  <name>Task 3: Lock in Playwright regression coverage for homepage family states and invite entry</name>
  <files>tests/hub-entry.spec.js, tests/arcade-party.spec.js, tests/board-games.spec.js</files>
  <read_first>tests/arcade-party.spec.js, tests/board-games.spec.js, tests/room-ui.spec.js, pages/index.js, pages/entry/[gameKey]/[roomNo].js, pages/games/[gameKey].js, pages/lobby.js</read_first>
  <action>Create `tests/hub-entry.spec.js` with two exact smoke paths: one verifies the homepage renders `遊戲入口`, family bands, and visible `暫停新房` / `即將推出` states; the second verifies a logged-out private invite deep link lands on the entry intercept and can proceed through an eligible guest-or-login branch. Update `tests/arcade-party.spec.js` and `tests/board-games.spec.js` so they assert the new family arcade heading/state surface and the presence of `複製邀請` or family-lobby return paths without removing the existing room-create happy paths. Keep the tests narrow and deterministic: one homepage flow, one invite-entry flow, and the existing party/board create-room smoke checks.</action>
  <verify>npm run check && node --test test-logic/admin-control-plane.test.js test-logic/hub-room-entry.test.js && npx playwright test tests/hub-entry.spec.js tests/arcade-party.spec.js tests/board-games.spec.js --workers=1</verify>
  <acceptance_criteria>
    - `tests/hub-entry.spec.js` exists
    - `tests/hub-entry.spec.js` contains the exact text assertions `遊戲入口`, `暫停新房`, and `即將推出`
    - `tests/arcade-party.spec.js` and `tests/board-games.spec.js` still create and enter live rooms after the homepage/lobby rewrite
    - `npm run check && node --test test-logic/admin-control-plane.test.js test-logic/hub-room-entry.test.js && npx playwright test tests/hub-entry.spec.js tests/arcade-party.spec.js tests/board-games.spec.js --workers=1` exits 0
  </acceptance_criteria>
  <done>Phase 3 homepage and room-entry behavior is now guarded by targeted smoke coverage alongside the existing family-room regression suite.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Browser users move from public homepage discovery into authenticated room-entry and invite/guest flows.
- `returnTo` navigation crosses from untrusted URL query state into post-login redirects.
- Guest sync prompts bridge temporary guest sessions into logged-in account actions after a match ends.
</trust_boundaries>

<stride_register>
- threat_id: T-03-01
  category: Tampering
  component: homepage state presentation
  disposition: mitigate
  mitigation_plan: render all family/state badges from backend hub payloads rather than page-local heuristics, and cover exact state strings in `tests/hub-entry.spec.js`.
- threat_id: T-03-02
  category: Spoofing
  component: login return path
  disposition: mitigate
  mitigation_plan: constrain `returnTo` handling to internal entry-page paths and cover successful invite-login handoff in the new smoke test.
- threat_id: T-03-03
  category: Elevation of Privilege
  component: guest UI affordances
  disposition: mitigate
  mitigation_plan: only surface guest CTAs when the backend resolve payload marks `guestAllowed === true`, and keep Dou Dizhu/login-only paths free of guest affordances.
- threat_id: T-03-04
  category: Repudiation
  component: guest match claim UX
  disposition: mitigate
  mitigation_plan: wire `登入並同步本局` to the backend guest-sync endpoint instead of a fake local-only toast so the claim is recorded against a real user account.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] `node --test test-logic/admin-control-plane.test.js test-logic/hub-room-entry.test.js`
- [ ] `npx playwright test tests/hub-entry.spec.js tests/arcade-party.spec.js tests/board-games.spec.js --workers=1`
- [ ] homepage shows family states and invite entry can reach an eligible guest/login intercept
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Players can discover, enter, and share rooms through the family arcade without breaking existing live room workflows
</success_criteria>

<output>
After completion, create `.planning/phases/03-hub-room-expansion-framework/03-03-SUMMARY.md`
</output>
