---
phase: 03-hub-room-expansion-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/games/catalog.js
  - lib/admin/control-plane.js
  - lib/shared/network-contract.js
  - backend/handlers/hub.js
  - backend/handlers/admin/capabilities/index.js
  - test-logic/hub-room-entry.test.js
autonomous: true
requirements:
  - HUB-01
user_setup: []
must_haves:
  truths:
    - Frontend and backend can fetch one family-based discovery payload that includes shipped games, paused-new-room states, and upcoming titles from the user's approved pool.
    - Shipped-game `可立即遊玩` and `暫停新房` states still come from the Phase 2 control plane instead of a new parallel capability source.
    - Homepage and admin consumers no longer need to hand-assemble discovery state from multiple unrelated data sources.
  artifacts:
    - lib/games/catalog.js
    - backend/handlers/hub.js
    - test-logic/hub-room-entry.test.js
  key_links:
    - lib/games/catalog.js -> lib/admin/control-plane.js
    - lib/games/catalog.js -> backend/handlers/hub.js
    - lib/admin/control-plane.js -> backend/handlers/admin/capabilities/index.js
    - backend/handlers/hub.js -> test-logic/hub-room-entry.test.js
---

<objective>
Introduce the structured discovery metadata contract that Phase 3 needs before entry-flow and UI work can be built safely.

Purpose: Phase 3 cannot deliver a family-based hub, paused/new-room semantics, or future-title surfacing if discovery logic remains page-local inside `pages/index.js`.
Output: A richer game-family catalog, control-plane-backed discovery state helpers, a unified `/api/hub` payload, and contract coverage for discovery synthesis.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-control-plane-expansion/02-CONTEXT.md
@.planning/phases/03-hub-room-expansion-framework/03-CONTEXT.md
@.planning/phases/03-hub-room-expansion-framework/03-RESEARCH.md
@.planning/phases/03-hub-room-expansion-framework/03-UI-SPEC.md
@.planning/phases/03-hub-room-expansion-framework/03-PATTERNS.md
@lib/games/catalog.js
@lib/admin/control-plane.js
@lib/shared/network-contract.js
@pages/index.js
@backend/handlers/admin/capabilities/index.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expand shipped + upcoming discovery metadata and synthesize Phase 3 card states</name>
  <files>lib/games/catalog.js, lib/admin/control-plane.js</files>
  <read_first>.planning/phases/03-hub-room-expansion-framework/03-CONTEXT.md, .planning/phases/03-hub-room-expansion-framework/03-UI-SPEC.md, lib/games/catalog.js, lib/admin/control-plane.js, pages/index.js, pages/games/[gameKey].js</read_first>
  <action>Per D-01, D-02, D-03, D-04, D-05, D-14, D-15, and D-16, expand `lib/games/catalog.js` from a shipped-title helper into the Phase 3 discovery source. Add explicit family definitions for `card`, `party`, `board`, `solo`, and `light-3d`; add discovery attributes for shipped titles (`familyKey`, `hubOrder`, `roomRoute`, `detailRoutePrefix`, `supportsShareLink`, `guestMode`, `launchState`); and add the exact upcoming pool `UNO 類`, `誰是臥底`, `你畫我猜`, `黑白棋`, `飛行棋`, `推箱子`, `保齡球`, `迷你賽車/碰碰車` as catalog entries with `launchState: "coming-soon"`. Treat those future titles as metadata-only entries in Phase 3 per D-05: do not create fake gameplay handlers, room managers, or create-room flows for them in this plan. In `lib/admin/control-plane.js`, add pure helpers that synthesize hub card state from catalog + capability data and return exact values `playable`, `paused-new-rooms`, and `coming-soon`, plus exact labels `可立即遊玩`, `暫停新房`, and `即將推出`. Keep shipped-game availability sourced from `capabilities`; upcoming entries must stay catalog-defined and must not write fake future-game booleans into `system_configs`. Set `guestMode: "login-only"` for `doudezhu` and `guestMode: "invite-private-only"` for shipped party/board titles so D-11 and D-12 can be enforced without breaking current score-connected card rooms.</action>
  <verify>node --test test-logic/hub-room-entry.test.js</verify>
  <acceptance_criteria>
    - `lib/games/catalog.js` contains the exact Chinese titles `UNO 類`, `誰是臥底`, `你畫我猜`, `黑白棋`, `飛行棋`, `推箱子`, `保齡球`, and `迷你賽車/碰碰車`
    - `lib/admin/control-plane.js` contains the exact strings `playable`, `paused-new-rooms`, and `coming-soon`
    - `lib/admin/control-plane.js` contains the exact labels `可立即遊玩`, `暫停新房`, and `即將推出`
    - shipped titles still derive `暫停新房` from capability state rather than a second persisted source
  </acceptance_criteria>
  <done>Phase 3 now has one normalized discovery metadata source for shipped and upcoming titles, plus exact hub card states derived from the approved control-plane model.</done>
</task>

<task type="auto">
  <name>Task 2: Add a unified public hub payload and extend capability responses with discovery metadata</name>
  <files>lib/shared/network-contract.js, backend/handlers/hub.js, backend/handlers/admin/capabilities/index.js, test-logic/hub-room-entry.test.js</files>
  <read_first>lib/shared/network-contract.js, backend/router.js, backend/handlers/me.js, backend/handlers/admin/capabilities/index.js, lib/games/catalog.js, lib/admin/control-plane.js, pages/index.js, test-logic/admin-control-plane.test.js</read_first>
  <action>Per D-01, D-07, D-14, D-15, D-16, and the `Arcade Command Dock` contract in `03-UI-SPEC.md`, add `API_ROUTE_PATTERNS.hub = "/api/hub"` and `API_ROUTES.hub()` in `lib/shared/network-contract.js`. Create `backend/handlers/hub.js` as a public `GET` endpoint returning one JSON payload with the exact top-level keys `families`, `liveFeed`, `featuredRooms`, `leaderboardPreview`, `universalEntry`, and `capabilitySummary`. `families[*].items[*]` must already include exact fields `gameKey`, `familyKey`, `state`, `stateLabel`, `route`, `supportsShareLink`, `guestMode`, `roomCount`, and `sharePath`. Extend `backend/handlers/admin/capabilities/index.js` `GET` responses so each capability item also includes `familyKey`, `familyLabel`, `state`, `stateLabel`, `supportsShareLink`, and `guestMode`. Use only public room summaries in `/api/hub`; do not leak private-room details into the public payload. Add `test-logic/hub-room-entry.test.js` coverage for `/api/hub`, upcoming-card presence, and the exact behavior where a disabled shipped game still reports `暫停新房` while live public rooms remain visible/joinable.</action>
  <verify>node --test test-logic/hub-room-entry.test.js && npm run check</verify>
  <acceptance_criteria>
    - `lib/shared/network-contract.js` contains `hub: "/api/hub"` and `API_ROUTES.hub()`
    - `backend/handlers/hub.js` exports a `GET` handler contract for `/api/hub`
    - `/api/hub` payload contains the exact keys `families`, `liveFeed`, `featuredRooms`, `leaderboardPreview`, `universalEntry`, and `capabilitySummary`
    - `backend/handlers/admin/capabilities/index.js` includes `state`, `stateLabel`, `supportsShareLink`, and `guestMode` in capability items
    - `node --test test-logic/hub-room-entry.test.js && npm run check` exits 0
  </acceptance_criteria>
  <done>The backend now exposes a single discovery payload that both homepage and admin consumers can trust, without reassembling state in page code.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Public homepage consumers cross from browser UI into `/api/hub`, which must expose discovery-safe room data without leaking private invite details.
- Discovery state is synthesized from catalog code and Phase 2 control-plane config; contradictions between those sources would mislead the UI.
- Admin capability reads now feed frontend discovery state, so metadata shape drift would break frontend/backend consistency.
</trust_boundaries>

<stride_register>
- threat_id: T-01-01
  category: Tampering
  component: discovery state synthesis
  disposition: mitigate
  mitigation_plan: keep shipped-game pause/playability derived from `lib/admin/control-plane.js` helpers and cover exact state labels in `test-logic/hub-room-entry.test.js`.
- threat_id: T-01-02
  category: Information Disclosure
  component: `/api/hub`
  disposition: mitigate
  mitigation_plan: build hub payloads from public room summaries only and exclude private-room-only details from the public discovery endpoint.
- threat_id: T-01-03
  category: Repudiation
  component: admin/frontend capability metadata drift
  disposition: mitigate
  mitigation_plan: extend `backend/handlers/admin/capabilities/index.js` with the same discovery metadata fields used by `/api/hub` so both surfaces read one normalized shape.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/hub-room-entry.test.js`
- [ ] `npm run check`
- [ ] `/api/hub` uses public room summaries only and still shows `暫停新房` when a shipped game is disabled for new rooms
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 discovery metadata exposes shipped and upcoming titles through one backend-owned family hub contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-hub-room-expansion-framework/03-01-SUMMARY.md`
</output>
