---
phase: 02-admin-control-plane-expansion
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - lib/shared/network-contract.js
  - backend/handlers/admin/logs/index.js
  - backend/handlers/admin/players/[id]/adjust.js
  - backend/handlers/admin/capabilities/index.js
  - backend/handlers/admin/runtime/index.js
  - pages/admin/index.js
  - styles/UtilityPages.module.css
  - test-logic/admin-control-plane.test.js
  - tests/admin-console.spec.js
  - docs/api/api-reference.md
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
user_setup: []
must_haves:
  truths:
    - Admin mutations leave a readable backend/UI trace without requiring direct DB access.
    - Capability, runtime, and player changes use a consistent audit payload with before/after data.
    - Automated coverage protects the new-room-only semantics and the grouped admin workflow.
  artifacts:
    - backend/handlers/admin/logs/index.js
    - test-logic/admin-control-plane.test.js
    - tests/admin-console.spec.js
    - docs/api/api-reference.md
  key_links:
    - backend/handlers/admin/capabilities/index.js -> backend/handlers/admin/logs/index.js
    - backend/handlers/admin/runtime/index.js -> backend/handlers/admin/logs/index.js
    - pages/admin/index.js -> backend/handlers/admin/logs/index.js
    - test-logic/admin-control-plane.test.js -> tests/admin-console.spec.js
---

<objective>
Add lightweight audit visibility, guardrails, and regression coverage for the admin control plane.

Purpose: Phase 2 is only operationally safe if admin changes leave a structured trace, stay behind explicit read APIs, and have automated coverage for both backend behavior and operator workflows.
Output: A recent-changes admin API, standardized audit payloads, a lightweight audit feed in the console, and backend/UI regression coverage for the new control plane.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-control-plane-expansion/02-CONTEXT.md
@.planning/phases/02-admin-control-plane-expansion/02-RESEARCH.md
@backend/handlers/admin/players/[id]/adjust.js
@backend/handlers/admin/capabilities/index.js
@backend/handlers/admin/runtime/index.js
@pages/admin/index.js
@styles/UtilityPages.module.css
@lib/db.js
@docs/api/api-reference.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Standardize audit payloads and expose an explicit admin log reader</name>
  <files>lib/shared/network-contract.js, backend/handlers/admin/logs/index.js, backend/handlers/admin/players/[id]/adjust.js, backend/handlers/admin/capabilities/index.js, backend/handlers/admin/runtime/index.js</files>
  <read_first>lib/shared/network-contract.js, lib/db.js, backend/handlers/admin/players/[id]/adjust.js, backend/handlers/admin/capabilities/index.js, backend/handlers/admin/runtime/index.js, backend/handlers/admin/config/index.js</read_first>
  <action>Extend `lib/shared/network-contract.js` with `API_ROUTE_PATTERNS.admin.logs = '/api/admin/logs'` and `API_ROUTES.admin.logs()`. Add `backend/handlers/admin/logs/index.js` with a `GET` admin-only contract that returns the most recent 50 log items ordered newest-first. Update player-adjust, capability, and runtime mutations so each `admin_logs.detail` payload uses the same fields: `scope`, `target`, `before`, `after`, `reason`, and `appliesTo`. Capability and runtime writes must set `appliesTo: 'new-rooms-only'`; player adjustments must set `appliesTo: 'immediate-player-state'`.</action>
  <verify>node --test test-logic/admin-control-plane.test.js</verify>
  <acceptance_criteria>
    - `lib/shared/network-contract.js` contains `logs` under `API_ROUTE_PATTERNS.admin` and `API_ROUTES.admin`
    - `backend/handlers/admin/logs/index.js` exports an admin-only `GET` contract
    - `backend/handlers/admin/players/[id]/adjust.js`, `backend/handlers/admin/capabilities/index.js`, and `backend/handlers/admin/runtime/index.js` all contain `before` and `after` in their log detail payload
    - `node --test test-logic/admin-control-plane.test.js` exits 0
  </acceptance_criteria>
  <done>Admin mutations now leave a standardized trace that can be read without opening the database directly.</done>
</task>

<task type="auto">
  <name>Task 2: Surface lightweight recent-change visibility in the admin console</name>
  <files>pages/admin/index.js, styles/UtilityPages.module.css</files>
  <read_first>pages/admin/index.js, styles/UtilityPages.module.css, backend/handlers/admin/logs/index.js, .planning/phases/02-admin-control-plane-expansion/02-CONTEXT.md</read_first>
  <action>Update `pages/admin/index.js` to fetch `API_ROUTES.admin.logs()` after successful capability/runtime/player mutations and during initial load. Render a recent-changes panel that shows operator, action, target, and timestamp for the latest entries, plus a badge with the exact text `新房生效` for capability/runtime changes. Keep this panel informational only: do not add approval, rollback, or restore buttons. Add matching CSS classes in `styles/UtilityPages.module.css` for an audit feed list, audit rows, and trace badges.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - `pages/admin/index.js` contains `API_ROUTES.admin.logs()`
    - `pages/admin/index.js` contains `新房生效`
    - `styles/UtilityPages.module.css` contains `auditFeed`, `auditRow`, and `traceBadge`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Operators can see a lightweight change history directly in the admin console.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend and operator smoke coverage for the admin control plane</name>
  <files>test-logic/admin-control-plane.test.js, tests/admin-console.spec.js, docs/api/api-reference.md</files>
  <read_first>test-logic/backend-contract.test.js, test-logic/client-network-contract.test.js, tests/arcade-party.spec.js, tests/board-games.spec.js, docs/api/api-reference.md, pages/admin/index.js, backend/handlers/admin/logs/index.js</read_first>
  <action>Extend `test-logic/admin-control-plane.test.js` so it covers admin auth on `/api/admin/logs`, structured audit payload shape, disabled-game create failures, maintenance-mode create failures, and the fact that a previously created room still serializes after the capability flips. Add `tests/admin-console.spec.js` that logs in as `admin`, verifies grouped family sections render on `/admin`, toggles a game off, verifies the recent audit feed updates, verifies the exact `只影响新房` / `新房生效` messaging, and proves a fresh room create path is blocked while a previously opened room remains accessible. Update `docs/api/api-reference.md` to list `/api/admin/capabilities`, `/api/admin/runtime`, and `/api/admin/logs` as the explicit Phase 2 admin API surface.</action>
  <verify>node --test test-logic/admin-control-plane.test.js && npx playwright test tests/admin-console.spec.js --workers=1 && npm run check</verify>
  <acceptance_criteria>
    - `test-logic/admin-control-plane.test.js` contains `/api/admin/logs`, `系統維護中，暫停新建房間`, and `該遊戲目前未開放新房`
    - `tests/admin-console.spec.js` exists
    - `docs/api/api-reference.md` contains `/api/admin/capabilities`, `/api/admin/runtime`, and `/api/admin/logs`
    - `node --test test-logic/admin-control-plane.test.js && npx playwright test tests/admin-console.spec.js --workers=1 && npm run check` exits 0
  </acceptance_criteria>
  <done>The admin control plane now has backend regression coverage, an operator smoke test, and documented API ownership.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Audit data crosses from backend persistence (`admin_logs`) into an admin-only read API and then into the browser UI.
- Operator trust now depends on both mutation handlers and the recent-change read surface telling the same story.
- Regression coverage must span backend enforcement and admin UI workflows because mistakes in either layer can undermine traceability.
</trust_boundaries>

<stride_register>
- threat_id: T-03-01
  category: Repudiation
  component: admin mutation history
  disposition: mitigate
  mitigation_plan: Standardize `admin_logs.detail` with `scope`, `target`, `before`, `after`, `reason`, and `appliesTo`, then expose it through `/api/admin/logs` and the admin feed.
- threat_id: T-03-02
  category: Information Disclosure
  component: audit read endpoint
  disposition: mitigate
  mitigation_plan: Keep `/api/admin/logs` admin-authenticated only and cover that boundary in `test-logic/admin-control-plane.test.js`.
- threat_id: T-03-03
  category: Tampering
  component: admin workflow regressions
  disposition: mitigate
  mitigation_plan: Add backend regression tests and a Playwright smoke path that proves toggles, runtime edits, and the recent-change UI stay aligned with the control-plane contract.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/admin-control-plane.test.js`
- [ ] `npx playwright test tests/admin-console.spec.js --workers=1`
- [ ] `npm run check`
- [ ] `docs/api/api-reference.md` lists the explicit Phase 2 admin endpoints
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Admin capability, runtime, and player changes leave a readable trace and remain protected by backend/UI regression coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-control-plane-expansion/02-03-SUMMARY.md`
</output>
