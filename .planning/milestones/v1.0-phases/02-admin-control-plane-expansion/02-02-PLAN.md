---
phase: 02-admin-control-plane-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - pages/admin/index.js
  - styles/UtilityPages.module.css
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
user_setup: []
must_haves:
  truths:
    - The admin console is grouped by game family and centered on toggle-style controls.
    - Capability changes clearly communicate that they affect new rooms only.
    - Player quick actions remain available while runtime controls move into a structured operator workflow.
  artifacts:
    - pages/admin/index.js
    - styles/UtilityPages.module.css
  key_links:
    - pages/admin/index.js -> lib/shared/network-contract.js
    - pages/admin/index.js -> backend/handlers/admin/capabilities/index.js
    - pages/admin/index.js -> backend/handlers/admin/runtime/index.js
---

<objective>
Rebuild the admin console around grouped family controls and structured runtime workflows.

Purpose: The backend APIs from Plan 02-01 only help operators if the admin page exposes them through a clear game-family control surface instead of raw JSON textareas.
Output: A grouped, toggle-first admin console that keeps player actions, surfaces runtime controls, and states clearly that capability changes affect new rooms only.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-control-plane-expansion/02-CONTEXT.md
@.planning/phases/02-admin-control-plane-expansion/02-RESEARCH.md
@pages/admin/index.js
@styles/UtilityPages.module.css
@lib/shared/network-contract.js
@lib/games/catalog.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Switch admin data loading from raw config blobs to structured control-plane responses</name>
  <files>pages/admin/index.js</files>
  <read_first>pages/admin/index.js, lib/client/api.js, lib/shared/network-contract.js, backend/handlers/admin/capabilities/index.js, backend/handlers/admin/runtime/index.js, backend/handlers/admin/players/index.js</read_first>
  <action>Update `pages/admin/index.js` so the page loads `API_ROUTES.admin.capabilities()` and `API_ROUTES.admin.runtime()` alongside the existing player data. Normalize the capability response into grouped `card`, `party`, and `board` sections on the page. Remove the raw `templateDraft` and `configDraft` textareas as the primary operator workflow; if an expert fallback remains, move it behind a non-primary disclosure instead of rendering it inline by default. Keep existing admin auth/error handling and `loadData()` orchestration intact.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - `pages/admin/index.js` contains `API_ROUTES.admin.capabilities()` and `API_ROUTES.admin.runtime()`
    - `pages/admin/index.js` no longer contains the raw JSON placeholders `{"id":1,"isActive":true` or `{"key":"maxOpenRoomsPerUser","value":5}`
    - `npm run check` exits 0 after the data-loading refactor
  </acceptance_criteria>
  <done>The admin page now consumes the structured control-plane APIs instead of leading with raw JSON blobs.</done>
</task>

<task type="auto">
  <name>Task 2: Build grouped game-family toggle panels and structured runtime controls</name>
  <files>pages/admin/index.js, styles/UtilityPages.module.css</files>
  <read_first>pages/admin/index.js, styles/UtilityPages.module.css, lib/games/catalog.js, .planning/phases/02-admin-control-plane-expansion/02-CONTEXT.md</read_first>
  <action>Render one admin panel per game family with rows for each managed game key: `doudezhu` under `card`, `werewolf` and `avalon` under `party`, `gomoku` and `chinesecheckers` under `board`. Each row must have a toggle-style action, status copy, and the exact helper text `只影响新房`. Add a structured runtime panel with a numeric control for `maxOpenRoomsPerUser` and a toggle control for `maintenanceMode`. Do not reintroduce freeform JSON editing in these panels. Add CSS classes for a dense control grid, toggle rows, and runtime control blocks so the page remains readable on desktop and mobile.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - `pages/admin/index.js` contains `只影响新房`
    - `pages/admin/index.js` contains `maxOpenRoomsPerUser` and `maintenanceMode`
    - `styles/UtilityPages.module.css` contains `controlGrid`, `toggleRow`, and `runtimeControl`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The admin console shows grouped family toggles and explicit runtime controls with the right scope messaging.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve player operations and operator clarity during the console refactor</name>
  <files>pages/admin/index.js, styles/UtilityPages.module.css</files>
  <read_first>pages/admin/index.js, styles/UtilityPages.module.css, backend/handlers/admin/players/[id]/adjust.js, .planning/phases/02-admin-control-plane-expansion/02-CONTEXT.md</read_first>
  <action>Keep the existing player quick actions (`+500`, `+20`, block/unblock) visible and easy to reach after the family toggle refactor. Add summary copy that capability changes only affect new rooms and that existing live rooms are left untouched. If an expert fallback editor is retained, wrap it in a secondary disclosure such as `<details>` labeled for advanced use; it must not occupy the same visual priority as the main control panels. Preserve error handling so failed toggles or runtime edits surface a visible message without hiding the rest of the page.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - `pages/admin/index.js` still contains the player quick-action labels `+500` and `+20`
    - `pages/admin/index.js` contains copy explaining that existing rooms are not changed in place
    - `pages/admin/index.js` either contains `<details` for an expert fallback or contains no raw JSON editor state at all
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The admin console keeps practical player controls while making the new control-plane scope obvious to operators.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Admin operators use the browser UI instead of direct database edits, so the page must present safe, comprehensible actions.
- The admin page depends on structured capability/runtime APIs from Plan 02-01 and must preserve existing player mutation flows.
- Visual grouping and helper text influence whether operators accidentally expect live-room mutations.
</trust_boundaries>

<stride_register>
- threat_id: T-02-01
  category: Tampering
  component: operator workflow
  disposition: mitigate
  mitigation_plan: Remove raw JSON as the primary control path and replace it with grouped structured toggles that only send allowlisted values.
- threat_id: T-02-02
  category: Misconfiguration
  component: runtime controls
  disposition: mitigate
  mitigation_plan: Use explicit controls for `maxOpenRoomsPerUser` and `maintenanceMode` instead of expecting operators to know config keys and payload shapes.
- threat_id: T-02-03
  category: Denial of Service
  component: operator expectations around room lifecycle
  disposition: mitigate
  mitigation_plan: Display the exact `只影响新房` / existing-room helper copy in the control panels so operators do not assume toggles rewrite live room state.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run check`
- [ ] the admin page loads capability/runtime controls through structured admin routes instead of raw JSON inputs
- [ ] player quick actions remain visible in the refactored console
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The admin console is grouped by game family, toggle-first, and clear about the new-room-only effect of capability changes
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-control-plane-expansion/02-02-SUMMARY.md`
</output>
