---
phase: 01-backend-contract-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/shared/network-contract.js
  - backend/router.js
  - backend/handlers/**/*.js
  - lib/http.js
  - test-logic/backend-contract.test.js
autonomous: true
requirements:
  - PLAT-01
user_setup: []
must_haves:
  truths:
    - Every backend REST route family exposes explicit method and auth metadata instead of relying on file layout alone.
    - Shared route and socket constants fail tests if they drift from the live backend surface.
    - Unsupported methods and missing auth still return consistent JSON errors after the refactor.
  artifacts:
    - lib/shared/network-contract.js
    - test-logic/backend-contract.test.js
  key_links:
    - lib/shared/network-contract.js -> backend/router.js
    - backend/handlers/**/*.js -> test-logic/backend-contract.test.js
---

<objective>
Make the backend contract surface explicit and regression-testable.

Purpose: Later frontend, admin, and gameplay phases need a durable contract layer instead of implicit route knowledge spread across handlers and pages.
Output: A shared network-contract module, explicit handler metadata, and backend contract regression tests.
</objective>

<execution_context>
@/home/choinong/doudezhu/.codex/get-shit-done/workflows/execute-plan.md
@/home/choinong/doudezhu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract-foundation/01-RESEARCH.md
@backend/router.js
@backend/server.js
@lib/http.js
@lib/auth.js
@lib/socket-server.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create a shared network contract inventory</name>
  <files>lib/shared/network-contract.js, backend/router.js</files>
  <read_first>backend/router.js, lib/socket-server.js, backend/handlers/auth/login.js, backend/handlers/rooms/index.js, backend/handlers/party/rooms/index.js, backend/handlers/board/rooms/index.js</read_first>
  <action>Create `lib/shared/network-contract.js` as the backend contract source of truth. Export grouped REST route builders for auth, me/profile, leaderboard, templates, card rooms, party rooms, board rooms, and admin families, plus grouped socket event constants for `room`, `party`, `board`, and `voice` flows. Update `backend/router.js` so its generated route list can be validated against these constants during tests and so route records preserve contract metadata instead of only raw handler references.</action>
  <verify>node --test test-logic/backend-contract.test.js</verify>
  <acceptance_criteria>
    - `lib/shared/network-contract.js` contains `API_ROUTES` and `SOCKET_EVENTS`
    - `backend/router.js` exposes route records that include contract metadata
    - `node --test test-logic/backend-contract.test.js` exits 0 after the contract inventory is wired in
  </acceptance_criteria>
  <done>Shared route/socket constants exist and backend route records can be checked against them.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit contract metadata to backend handler families</name>
  <files>backend/handlers/**/*.js, lib/http.js</files>
  <read_first>backend/handlers/auth/login.js, backend/handlers/auth/logout.js, backend/handlers/me.js, backend/handlers/profile.js, backend/handlers/rooms/index.js, backend/handlers/admin/templates/index.js, lib/http.js, lib/auth.js</read_first>
  <action>Update every file under `backend/handlers/**` to expose contract metadata describing allowed methods, auth level (`public`, `user`, or `admin`), and the shared route key from `lib/shared/network-contract.js`. Keep request-body parsing and `405` handling routed through shared helpers in `lib/http.js` so handler families do not silently diverge on method/error behavior. Preserve existing response payload shapes unless a handler is already inconsistent with the route family.</action>
  <verify>node --test test-logic/backend-contract.test.js</verify>
  <acceptance_criteria>
    - every `backend/handlers/**/*.js` file exports contract metadata with `methods` and `auth`
    - `lib/http.js` still contains `methodNotAllowed` and shared body-parsing helpers used by handlers
    - `node --test test-logic/backend-contract.test.js` fails if any handler lacks metadata or claims a route key not present in `lib/shared/network-contract.js`
  </acceptance_criteria>
  <done>Backend handlers advertise explicit contract ownership and share one method/error baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend contract regression coverage</name>
  <files>test-logic/backend-contract.test.js</files>
  <read_first>backend/router.js, lib/shared/network-contract.js, backend/handlers/auth/login.js, backend/handlers/admin/config/index.js, lib/http.js</read_first>
  <action>Create `test-logic/backend-contract.test.js` with `node:test`. Cover three things: route inventory coverage between `lib/shared/network-contract.js` and `backend/router.js`; representative public/user/admin handler metadata; and method/auth regression behavior for representative endpoints such as `POST /api/auth/login`, `GET /api/me`, and `GET /api/admin/config`. Use lightweight mock `req`/`res` objects or direct router invocation rather than booting the full server process.</action>
  <verify>node --test test-logic/backend-contract.test.js && npm run check</verify>
  <acceptance_criteria>
    - `test-logic/backend-contract.test.js` contains assertions for public, user, and admin route families
    - `node --test test-logic/backend-contract.test.js` exits 0
    - `npm run check` exits 0 after the new test file is added
  </acceptance_criteria>
  <done>The backend contract surface is protected by a direct regression suite instead of manual inspection.</done>
</task>
</tasks>

<threat_model>
<trust_boundaries>
- Public browser traffic crosses into `backend/router.js` and then into route-family handlers under `backend/handlers/**`.
- JWT cookie auth decisions from `lib/auth.js` gate user/admin handlers and must stay aligned with route ownership.
- Shared route constants must stay synchronized between backend handlers and future frontend callers.
</trust_boundaries>

<stride_register>
- threat_id: T-01-01
  category: Spoofing
  component: handler auth metadata
  disposition: mitigate
  mitigation_plan: Require every handler family to declare `public`, `user`, or `admin` auth level and cover representative auth failures in `test-logic/backend-contract.test.js`.
- threat_id: T-01-02
  category: Tampering
  component: route inventory
  disposition: mitigate
  mitigation_plan: Fail tests when a route key exists in the shared contract but no backend handler maps to it, or when duplicate path/method combinations appear.
- threat_id: T-01-03
  category: Information Disclosure
  component: method and error handling
  disposition: mitigate
  mitigation_plan: Keep unsupported-method and malformed-body behavior centralized in `lib/http.js` so backend handlers do not leak inconsistent runtime errors.
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test test-logic/backend-contract.test.js`
- [ ] `npm run check`
- [ ] Representative public/user/admin endpoints still report expected method/auth failures through the regression test
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend routes, methods, and auth boundaries are explicit enough that later feature work can target a named contract surface
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract-foundation/01-01-SUMMARY.md`
</output>
